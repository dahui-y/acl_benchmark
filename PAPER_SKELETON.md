# 论文骨架

2026-08-08。**这份文件的用途：每个实验都要能指到它支撑的是下面哪一句话。
指不到的实验就是漂移。**

`PLAN.md` 是过程和纪律，这份是产出物的形状。

---

## 0.0 动机、问题与命名（2026-08-13 定稿 —— 顶会逻辑链，"战役"是作战语言不进论文）

**动机链**：训练自由高分辨率生成靠限制感受野换效率（窗/patch/扩张卷积），
但全家族保留同一个未审视的默认：**条件信号空间均匀** —— 每个局部视野
收到同一句完整 prompt、同一强度结构引导，而**内容空间异质**：主体区
需要主体语义与结构锚定，背景区两样都不需要却照单全收。
"均匀条件 × 异质内容"的错配是两个被反复点名的失效的共同根源：
**物体重复**（空视野收到索要主体的条件，画一个出来）与**背景过平滑**
（均匀低频拉回压平最需要新细节的区域）。一个原因，两张面孔。
已有空间化尝试（AccDiffusion 逐 patch prompt）需要独立 patch 前向，
在单次全图前向管线里没有落点，且每加一病外挂一网络。

**问题陈述**：能否在单次全图前向内、以可忽略代价、不经训练地，
让条件信号随空间内容自适应 —— 并在不需介入的图上做到可证明的零改动？

**核心洞察**：基阶段已经算出并丢弃了答案 —— 主体 cross-attention 图。
接回来当门控：文本条件按位置在全句/去主体句间混合（治重复），
结构引导强度按位置调制（还细节）。一张图，两个旋钮，零训练，
门关时逐字节等于原管线。

**标题（主推）**：*Where to Say What: Spatially Adaptive Conditioning for
Training-Free High-Resolution Image Generation*
—— v2 活着时 Conditioning 涵盖两旋钮；v2 判死只剩文本旋钮，标题不换、
范围收窄。备选（仅 v2 通过后可用）：*One Map, Two Cures: ...*；
否决：*Uniform Conditioning Is the Bottleneck*（证据只支持"根源之一"，
撑不起 the bottleneck）。

**纪律**：两个"胜过"（标准表胜 ScaleDiff、修复胜 AccDiffusion）是这个
动机的**实验后果**，写作中不得作为动机出现。

## 0. 定位

| | ScaleDiff (NeurIPS 2025) | 我们 |
|---|---|---|
| 任务 | training-free 高分辨率外推 | **同** |
| 骨干 | SDXL + FLUX (+Lumina) | **同**（FLUX 必做，见 §7） |
| 它打的缺陷 | **计算量 + DiT 兼容性**（摘要原文：*"require substantial computation or are incompatible with recent Diffusion Transformer models"*） | **全局一致性** |
| 头条 | Table 2：8 指标 x 2 分辨率 x 2 骨干 x 11 baseline | **同一张表 + 我们一行 + 一个新列** |
| Limitations | *"repetitive artifacts may still occur in background regions"* | ← **我们的入口** |

**我们和它的关系 = StyleSSP 和 StyleID 的关系**：不重做它做过的事，开在它的残余上。

**观感上的硬要求**（此前反复写偏过）：

- 标题必须含 *higher-resolution*；
- Figure 1 与 ScaleDiff 的 Figure 1 同构：4096² 并排 + 放大裁块 + 左上角标耗时；
- Abstract 第一句是**任务**，"duplication" 最早只能出现在第二句，且作为**症状**。
- **主语始终是高分辨率生成。** 物体重复是量这条轴的坐标，不是我们抢来的题目。

---

## 1. 研究动机（**任务的**，不是在位者的）

> 扩散模型只在 1024² 上训练过。要在不训练的前提下生成 4096²，
> **所有方法都只有一招：限制模型一次能看到的范围** —— 切 patch、开注意力窗、
> 扩张卷积。
>
> 但**一个受限视野在模型看来，和一整张训练分辨率的画布无法区分**。模型被训练成
> "给我一张画布，我画一个完整场景"，于是每个受限视野都独立地把 prompt 描述的
> 场景画了一遍。
>
> **让高分辨率算得动的那个机制，同时就是破坏全局物体一致性的那个机制。
> 效率是用一致性买来的，而这条账三年来没人算过。**

### 1.1 它是 general 的 —— 证据是六篇论文自己的文字

| 方法 | 效率机制 | 它自己命名的失效 |
|---|---|---|
| ScaleCrafter (ICLR24) | 扩张卷积 | 重复图案、结构畸变 |
| DemoFusion (CVPR24) | patch | **物体重复** |
| AccDiffusion (ECCV24) | patch | **物体重复**（整篇为它而写） |
| AccDiffusion v2 (2025) | patch | **物体重复** + 局部畸变 |
| Pixelsmith (2024) | slider | 重复物体（被 HiWave 点名） |
| HiWave (SIGGRAPH Asia 25) | patch DDIM 反演 | **内容重复** + 边界伪影 |
| **ScaleDiff (NeurIPS 25)** | **注意力窗** | *"repetitive artifacts may still occur in background regions"* |

**七种不同的效率机制，七篇各自命名了同一个失效。**
不是巧合 —— 是那个结构性代价换了外衣反复出现。

### 1.1a "专门针对重复"的到底有几家（2026-08-12 查原文定案）

区分"顺带缓解"与"专门诊断"，答案不是一家也不是六家，是**两家**：

| | 重复是**唯一**目标 | 有**专门的因果诊断**并宣称解决 | 顺带缓解 |
|---|---|---|---|
| **AccDiffusion v1/v2** | ✅ | ✅ | |
| **ScaleCrafter (ICLR24)** | ❌（两问题之一） | ✅ | |
| DemoFusion / FouriScale / FreeScale / HiWave / PixelRush / Pixelsmith | | | ✅ |

**ScaleCrafter 摘要原文**（不是顺带一提，必须给它这个位置，否则审稿人会拿
它来问）：*"we observe persistent problems of **object repetition and
unreasonable object structures** ... identify the crucial cause as the
**limited perception field of convolutional kernels** ... our approach can
**address the repetition issue well**"*
**AccDiffusion v1 摘要第一句**：*"This paper attempts to address the object
repetition issue in patch-wise higher-resolution image generation."*

**两家的诊断恰好互斥，各占一个因子：**
- ScaleCrafter -> 因子 (1) 视野不够全局 -> re-dilation 扩感受野；
- AccDiffusion -> 因子 (2) 文本条件不分位置 -> 逐 patch 改写 prompt。

**领域里对这个问题的专门诊断就这两条，且都是定性、二值的（有/无某因素）。**
我们的定位由此变得干净：

> **我们给出二者的定量合成：重复 ≈ R² × (1 − 主体覆盖率)。
> R² 来自视野被切成多少块（ScaleCrafter 那一维），(1 − 覆盖率) 来自这些块
> 里有多少个收到了不该收到的主体条件（AccDiffusion 那一维）。
> 它第一次回答"在什么条件下发生、发生多少"，而不只是"为什么发生"。**

这把"第三家修法"升格为"把前两家的诊断统一起来并可预测化"。

### 1.2 我们把定性抱怨变成定量的律

**律的完整形式有两个因子，只写第一个是错的（2026-08-08 修正）。**

**因子一：外推倍数。** 邻域/画布比随 R² 缩小：

| 分辨率 | KV 邻域占整图 | lone 类 excess |
|---|---|---|
| 1024² | 1 | +0.20 |
| 2048² | 1/4 | +1.00 |
| 4096² | 1/16 | **+5.40** |

**因子二：主体覆盖率 —— 失效【不是普适的】。** 基线各类：

| 类 | 主体占画面 (cov) | 基线 MAE | 有重复的 |
|---|---|---|---|
| **lone** | **1.6–10%** | **5.40** | **5/5** |
| portrait | 33–84% | 0.20 | 1/5 |
| structure | 18–74% | 0.20 | 1/5 |
| empty | — | 0.20 | 1/5 |

**特写肖像完全不重复**（`21_portrait` 三档都是 1 个人）。机制上：

> 重复要发生，需要一个受限视野**里面本来没有主体，却仍然收到完整的文本条件**
> —— 它于是把那个视野当成空白画布，照 prompt 画一个完整场景。
>
> - 主体占满画面：每个视野里都有主体的一部分 -> 画的是**延续** -> 不重复
> - 主体很小：绝大多数视野里没有主体 -> 每个都**新建** -> 重复

> ### **不含主体的邻域数 ≈ R² × (1 − 主体覆盖率)**

R² 是外推倍数（控制不了），主体覆盖率由内容决定。

**更好的自变量：空视野比例。** `cov` 依赖 head word（strip_subject 那条脆规则）
且经过逐图 min-max 归一化，绝对含义已丢。改用**机制里那个变量本身** ——
在**基图**上跑检测器，按 R×R 切块，数有多少块不含主体框：

```
空视野比例 = 不含主体框的 tile 数 / R²
```

不需要注意力图、不需要 head word、不需要跑放大阶段，完全自动。
`trigger.py` 算它与 |excess| 的秩相关。判据：**ρ > 0.5**。

### 1.2.1 由此得到的三条，都要写进论文

1. **适用范围必须明说。** Abstract 那句要加限定：*当主体只占画布一小部分时*。
   而这恰是**做 4096² 的主要动机场景** —— 要高分辨率就是为了一张细节丰富的
   大场景，主体在其中通常很小。限定之后不弱反而更贴。
2. **预先写下这个预测：1000 条随机 LAION prompt 上的 FID 大概率几乎不动。**
   只有一小部分 prompt 触发失效，平均值会稀释。**事前预测叫科学，
   等 FID 跑出来再解释叫找补。** 重复那一列必须**分层报告**。

   **风险的处理（三条，都可执行）：**

   a. **量出触发条件有多常见** —— `trigger.py` 在 LAION 那 1000 条上跑，
      只需基图（~7s/条，约 2 小时）。预注册判据：
      **≥25% 落在空视野 > 75% 的触发区 -> 条件足够常见，头条表值得跑；
      <10% -> 太罕见，必须以分层报告为主、总表为辅。**
      *（我们这 30 条是人为按六类均衡造的，比例不代表真实语料，不能拿来回答。
      在这 30 条上算出的任何触发区占比 —— 全体口径 40%、受限口径 19% ——
      都只是我们造数据时六类配比的算术结果，不是关于语料的发现，
      论文里一个都不许写。这个数只有 LAION 那 1000 条能回答。）*
   b. **分层报告**：按空视野比例分档报重复指标 —— 诚实做法，不是回避。
   c. **定性图承担一部分**：AccDiffusion v2 整个消融都是图，这条线接受这种证据。

   > **"不普适"不是致命伤：这条线上没有一篇打的是普适问题。**
   > ScaleDiff 打"计算量大 / 不兼容 DiT" —— 只在你在乎速度、用 DiT 时才是问题。
   > LSRNA 打"RGB 上采样过糊"、HiWave 打"patch 边界伪影"，都有触发条件。
   > **最直接的先例：AccDiffusion v1 (ECCV24) + v2 打的就是我们这个条件问题**
   > （它们 Fig. 9 的例子全是"小主体 + 大场景"），从没被要求证明普适。
   >
   > **而它们从没说清这个失效何时发生。我们能给出触发条件，这是它们没有的。**
   > 一个被刻画了触发条件的失效强过"有时会发生"：可预测、可安全（不该介入时
   > 逐字节不动）、并且解释了为什么它被抱怨三年却在 FID 上看不出来。
3. **适用性门是推论，不是补丁。** 方法只该在主体局部化时介入 ——
   主体铺满画面时根本不存在"没有主体的邻域"，也就没有要抑制的东西。
   这解释了 `08_sheep`（羊群被删）为什么不该被干预。

### 1.2.2 律在第二种机制上复现（2026-08-10，诚实缺口已补）

原本的缺口是：这条律只在 ScaleDiff（注意力窗）上量过，那它可能只是
ScaleDiff 的缺陷报告。DemoFusion（CVPR 2024，重叠 patch + 渐进上采样）
机制完全不同，是这条谱系的公共节点。同样 30 条 prompt、seed 77：

**因子一（外推倍数），两种机制上同向：**

| 分辨率 | ScaleDiff lone excess | DemoFusion lone excess |
|---|---|---|
| 1024² | +0.20 | +0.40 |
| 2048² | +1.00 | +2.20 |
| 4096² | **+5.40** | **+4.60** |

**因子二（空视野比例 vs |excess| 的秩相关）：**

| | n | 全部 ρ | 受限 ρ | 空视野>75% 组 | ≤75% 组 |
|---|---|---|---|---|---|
| ScaleDiff | 42 | +0.287 | +0.845 | 5.00 | 1.32 |
| DemoFusion | 13 | −0.013 | +0.686 | 4.00 | 1.40 |

**"受限"= 排除 `card==0`（人群/纹理，无声明基数）与基图零框（检测器
本身失败）两类行。** 报告纪律，写进论文时必须照办：

- **两个 ρ 都报，不许只报受限的那个。** 全部 ρ 之所以近零，是因为
  `empty` 十五行空视野恒为 100%、excess 恒为 0，把相关性拉平 —— 那是
  排除规则要处理的已知情形，不是把数据挑好看。
- **证据是 DemoFusion 的 +0.686，不是 ScaleDiff 的 +0.845。**
  排除规则是看过 ScaleDiff 那张表之后想出来的，所以 +0.845 是**探索性**
  的，只交代规则的来历；规则冻结之后才跑的 DemoFusion 是**样本外确认**。
  论文里 ScaleDiff 一列标 exploratory，DemoFusion 一列标 confirmatory。
- 预注册的 ρ>0.5 判据：受限口径下两种机制都过。

> **这条律不是 ScaleDiff 特有的** —— 因子一同向，因子二在样本外复现。
> 两种机制唯一的共同点就是"把全局条件发给受限视野"，那正是律的机制项。

### 1.3 而领域的标准表看不见它

- FID / KID / IS 把 4096² 压到 **299²** —— 物理上看不见多出来的一个人；
- FIDp / KIDp / ISp 是 patch 上的**分布距离**，不数物体；
- **AccDiffusion v2 在正文里说了两遍**：
  - §5.2 *"FID, IS, and CLIP-Score may not directly indicate the presence of
    repetitive generation or local distortion"*
  - §5.5 *"current quantitative metrics cannot intuitively reflect the extent of
    object repetition or local distortion, we provide visualizations"*
  - **它三个核心模块的消融证据全部是图。**

我们自己也量到了同一件事的两个实例：
- 同一张图缩到 1024²，4096² 上的 6/8/9 个物体只剩 1/2/1 个（**漏 80%**）；
- `23_hands` 两只手全被删，portrait 整类 CLIP 只掉 **0.25%**。

---

## 2. 研究问题

动机那条链有**两个必要条件**：

1. **视野是受限的**；
2. **完整的文本条件以全局强度施加到每一个视野。**

**按"攻哪个因子"分类（2026-08-12 修正：原稿写成"三年来只攻过第 (1) 条"，
把 AccDiffusion 的 patch-prompt 错归进第 (1) 条，与 §3.1b 已承认的
"文本条件空间化这个原则是他们的"自相矛盾。审稿人会当场抓住，这里改正）：**

| 攻第 (1) 条：让局部视野知道更多全局 | 依据 |
|---|---|
| ScaleCrafter 扩张卷积扩大感受野 | *"modifies the dilation rate ... which mitigates object repetition artifacts"* |
| DemoFusion dilated sampling 提供全局语义 | AccDiffusion v2 转述 |
| FouriScale / FreeScale 频域对齐 | *"attribute this to frequency-domain misalignment and introduce spectral interventions"* |
| HiWave 注入 DDIM 反演噪声保结构 | *"injects DDIM-inverted noise to preserve structural information and reduce repetition"* |
| PixelRush 从保结构 latent 起步 | *"inherently mitigates object repetition"* |
| （我们的 KV×2 消融也属这一类） | 实测 **+37% 时间 / +41% 显存** |

| 攻第 (2) 条：让文本条件不再全局均匀 | 代价 |
|---|---|
| **AccDiffusion v1/v2 逐 patch 改写 prompt —— 唯一一家** | **需要独立的 patch 前向**；4096² 要 1599 秒 |

> **五种机制攻"视野不够全局"，一种机制攻"文本条件不分位置"。
> 后者只有 AccDiffusion，而它的做法需要 patch 前向。
> 我们是第二个攻第 (2) 条的，也是第一个在单次全图前向内攻它的。**

这个分类还解释了为什么其余五家**在机制层面不构成正面竞争** ——
它们与我们互补（攻不同因子），只有 AccDiffusion 需要正面防守（§3.1b）。

> ### **能不能让文本条件在空间上不再均匀，且不付计算代价 ——**
> ### **使效率不必再用一致性来买？**

---

## 3. 方法

**同一次前向里让两套文本嵌入共存，按位置取用。**

cross-attention 层里同时算完整 prompt 和去主体 prompt 的 K/V，
按一张门控图逐位置混合输出：

```
out = m ⊙ out_full + (1 − m) ⊙ out_nosubj
```

门控图 `m` 来自**基阶段模型自己的主体 cross-attention** —— 它本来就算出来了，
只是被扔掉，所以**免费**。主体所在的区域拿完整 prompt，其余区域拿只有景物的条件。

**代价：77 个 token 上多一次 K/V 投影 = +3.9% 时间 / +1.7% 显存。**

### 3.1 为什么不是两种更显然的做法（都是测出来的，不是猜的）

**① 压主体 token 的注意力权重（v0）→ 无效，幻影 5 → 5。**

CLIP 文本编码器是**因果**的，主体语义摊在整句上。实测注意力质量：
`distant 6.34% / lone 5.67% / hiker 5.52% / snow 5.30%`，前十名加起来才 49%。
压掉 `hiker` 一个 token，质量重新分配给 `lone / standing / ridge`，
而它们的向量里照样带着"有个人站在山脊上"。

> **干预必须作用在【嵌入】上，不能作用在【注意力权重】上。**

**② AccDiffusion 的 patch-content-aware prompt → 结构上不可用。**

它给每个 patch 换一串 token，需要**独立的 patch 前向**；
ScaleDiff 只有**一次全图前向**，没有地方喂第二句话。

**这是我们和它最像、也最需要防守的地方。** 防守 = "结构上不可用" + 数字，
不是"我们不一样"。机制（同一句 prompt 施于所有局部视野导致重复）**是 AccDiffusion
命名的，我们继承并引用**。

### 3.1a AccDiffusion 的原文：数据集、以及他们自己承认的缺口（2026-08-11 核实）

**四句原文**（经 arXiv HTML 版取得，**进论文前需对 PDF 再核一遍**）：

> **v1**：*"We randomly selected 10,000 images from the Laion-5B dataset as our
> real images set and randomly chose 1,000 text prompts from Laion-5B as inputs."*
> **v2**：*"For quantitative comparison, we randomly selected 10,000 images from
> the Laion-5B dataset as the real image set and used 1,000 randomly chosen
> text prompts from Laion-5B as input."*

数据集与我们同源（Laion-5B，1000 条 prompt）。**但真图参考集他们用 10,000 张，
ScaleDiff 用 1,000 张 —— 这条线内部本来就不统一。** 指标 FID / IS / CLIP
+ 裁块版 FIDc / ISc（ScaleDiff 叫 FIDp / KIDp），分辨率 1024–4096。

> **v1**：*"FID, IS, and CLIP-Score **do not intuitively reflect the degree of
> repeated generation** in the resulting images."*
> **v2**：*"Note that FID, IS, and CLIP-Score **may not directly indicate the
> presence of repetitive generation** or local distortion in the generated images."*

**一篇整本为物体重复而写的 ECCV 论文，两个版本都写下这句话，然后重复只用
定性图（红框标复制体）来证明。** 这把 §6.9.0a 那套论证从"我们的辩解"
升级成**在位者自己写在正文里的公认缺口**：标准表看不见重复 -> 没人能靠它
证明修好了 -> **我们提供第一个不需真值、不需标注的重复量化指标**。
引用时要把这两句原样放进 related work，它比我们自己论证十句都有力。

**区别一览（防守靠"结构上不可用 + 数字"，不靠"我们不一样"）：**

| | AccDiffusion v1/v2 | 我们 |
|---|---|---|
| 机制家族 | **patch-based**（DemoFusion 一系） | **单次全图前向**（ScaleDiff，注意力窗口） |
| 怎么改文本条件 | 给每个 patch 换一串 token | 同一次前向里两套嵌入共存，按门控图逐位置混合 |
| 结构前提 | **需要独立的 patch 前向** | ScaleDiff 只有一次全图前向，**没有地方喂第二句话** |
| 4096² 耗时 | **1599 s** | ~117 s（**见下：14 倍不是我们的功劳**） |
| 施加范围 | 无条件 | **有适用性门，关闭时逐字节等于原版** |
| 重复的度量 | **无，只有定性图** | **Δdelta**，无需真值/标注 |

机制本身（同一句 prompt 施于所有局部视野导致重复）**是 AccDiffusion 命名的，
我们继承并引用**，不假装是自己发现的。

#### 3.1b "那不就是重复了他们的工作？" —— 最大的风险，正面记（2026-08-11）

**先给不粉饰的答案：只有"同一个问题换个骨干重做一遍"是不够格的。**
若我们手上只有这个，"这是 AccDiffusion 在 ScaleDiff 上的适配"一句就够拒稿。
所以必须把归属分清。

**不是我们的（要让出去，不能含糊）：**
- **问题的命名**（物体重复）与**成因的定性解释**（同一句 prompt 施于所有
  局部视野）—— 都是 AccDiffusion 的，引用。
- **速度。** 1599 s -> 117 s 里绝大部分是 **ScaleDiff 本身快**（113 s），
  不是我们做的。**我们唯一能声称的是 +3.9%。**
  **把"14 倍"当卖点是拿别人的功劳** —— 上一版表里我就是这么写的，改掉。
  正确表述：*把重复抑制带进一个已经很快的管线，代价可忽略。*

**是我们的（四条，强弱不同，按强度排）：**
1. **把定性成因变成定量、且可事前预报的律**：重复 ≈ R²×(1−主体覆盖率)，
   在**两个不同机制家族**上都成立（注意力窗口 + patch），
   **只用基图就能算** —— 放大之前就知道会不会出问题。他们没有。
2. **适用性门。** 能预报才能决定**要不要介入**，关闭时逐字节等于原版；
   AccDiffusion 无条件施加。这让"非触发零代价"从希望变成可证。
3. **重复的量化指标 Δdelta。** 他们两版都在正文承认标准指标反映不出重复
   （§3.1a 原文），然后只用红框图。**这是在位者自己承认的空位。**
4. **机制本身**（一次前向里两套嵌入按门控图共存）。**四条里最弱** ——
   regional prompting 一类工作存在，区别在于它们要用户指定区域、
   目的是可控生成，而我们的门控图来自基阶段模型自己算好的 cross-attention。

**结构性弱点与对策（比任何措辞辩护都管用）：**

现在的布局是**我们在 ScaleDiff 上做、AccDiffusion 在 DemoFusion 上做，
两条线不相交**，读者只能听我们自己说"结构上不可用"。而律已经在
DemoFusion 上复现过（§1.2.2），基础设施是现成的。所以：

> **把我们的方法也在 DemoFusion 上跑一遍，与 AccDiffusion 正面比。**
> 同骨干、同问题、同批 prompt —— 若我们用 **+3.9%** 的代价拿到与它相当
> 或更好的重复抑制，这就不是"移植"，而是**同一问题上更好的方法**。

配套：**在 ScaleDiff 上真的尝试移植 AccDiffusion 的 patch-content-aware
prompt，报告它要付的代价或为何不可行** —— 把"结构上不可用"从断言变成实验。
**这两条不做，这篇论文的定位就始终是站不稳的。**

**顺带的协议决定**：他们用 10,000 张真图、ScaleDiff 用 1,000 张，
我们手上有 **3,424 张干净真图**。主表按 ScaleDiff 协议用 1,000 张
（我们要和它比），**另加一行用全部 3,424 张的稳健性检查** —— 更大参考集
压低 FID 的小样本偏差，且已经在手，不额外花钱。这是当初多取候选的又一个回报。

### 3.2v 方法 v2 提案：一张门控图，第二个旋钮（2026-08-12，预注册，未跑）

**失效分类学（按机制家族分病，这本身是论文一节）**：
patch 家族病 = {重复, 局部畸变}（畸变来自"每 patch 拿错误局部 prompt
独立去噪"，窗口家族没有这个病灶）；**窗口家族病 = {重复, 过平滑}**。
过平滑的病灶在源码里：`x_guided = x_pred + scale·(lowpass(x_ref) −
lowpass(x_pred))`（refine，L29），scale **时间上有日程、空间上是常数**；
LFM 两分支都不含新细节，新细节只能在这段被全图同强度约束的去噪里长。

**v2**：v1 的同一张门控图 m，把引导系数按位置调制：
G = g_bg + (1−g_bg)·m，主体区照旧、背景区放松。

**对偶观察（2026-08-13，用户点出 v2 的双病结构后补）**：
两个家族的"第二个病"是**同一个旋钮拧错方向** —— patch 家族锚太松
（各 patch 自由发挥 -> 局部畸变），窗口家族锚太紧（均匀低频拉回 ->
背景过平滑）。AccDiffusion v2 的答案是全局加锚（外挂 ControlNet，
加网络加代价）；我们的答案是**锚的正确用量本来就是空间函数** ——
主体紧、背景松，且这个空间信息免费（cross-attention）。
这句话同时解释了为什么他们的 ControlNet 方案搬到 ScaleDiff 上没有意义：
ScaleDiff 不缺锚，缺的是局部地松开锚。相关工作里按"对偶"写，
不按"并列"写。

**互锁 = 立身之本**：单独放松背景引导会打开重复的闸门；只有背景的
文本条件不再索要主体（v1 的门）时放松才安全。两个旋钮互为前提，
与 AccDiffusion "加一个病挂一个 ControlNet" 形成结构对照 ——
我们加一个病复用同一张免费的图，代价仍 ~+4%。

**四臂对照（method_v2.py，互锁必须用对照臂证出来）**：
base / v1（只文本门）/ v2only（只放松引导，**预测重复回潮**）/
v2（全套，**预测细节回来且不回潮**）。

**预注册判据**：P1 v2 臂背景 Laplacian 能量比 v1 高 ≥10%；
P2a v2only 比 base 多 ≥1 个物体（放松确实招重复 = 互锁前半）；
P2b v2 ≤ v1 + 0.5（门压得住）；P3 耗时 ≤ base×1.06。
**判死**：P1 不过 -> 过平滑主因不在 Structure Guidance（LFM 初始化 /
restart 窗长是另两个嫌疑），v2 撤，论文退回 v1+评测三柱；
P2a 不过 -> 互锁故事不成立，v2 降级为独立旋钮；P2b 不过 -> g_bg 扫小。

**打谁**：v2 直指 ScaleDiff 输掉的 KIDp/ISp —— patch 指标对"背景是
插值糊还是真细节"最敏感，目标是拿 DemoFusion 的细节而不拿它的重复。

工程注记：靠 `torch.is_tensor(scale)` 区分引导调用（张量）与 LFM 构造
（字面量 1，L544/L571）——**依赖上游写法，升级管线必须复查**；
每臂打印调制次数核对（stage=2 实测 40，各 gbg<1 臂一致）。
SpatialGuidance 已过 5 项无 GPU 自检（等价性 / 判别 / m=1 与原式数值
一致 / m=0 缩到 g_bg / 还原）。

#### 3.2v-R 首测结果（2026-08-13，gbg=0.4，3 条 lone）：**P1 0/3，判死支触发**

| idx | base | v1 | v2only | v2 | v2 vs v1（需 ≥+10%） |
|---|---|---|---|---|---|
| 0 雪山谷地 | 3.1 | 3.1 | 3.0 | 3.0 | −3% |
| 2 红色荒漠 | 18.6 | 17.7 | 17.5 | 17.7 | ±0% |
| 4 大峡谷 | 24.4 | 22.8 | 23.5 | 21.6 | −5% |

机制执行无误（gbg<1 臂调制 40 次、gbg=1 臂 0 次；P3 过：+2.2~2.6%），
但**背景处把引导强度砍到四成，背景高频纹丝不动** ——
放松的那个约束不是压细节的那只手。事后看数学上讲得通：
refine 只替换低频带，x_pred 的高频原样穿过，我们赌的"对采样轨迹的
间接抑制"实测 ≈ 0。**预注册判死支按原文触发：过平滑主因不在
Structure Guidance；头号嫌犯移交 LFM 初始化**（latents_LFM 的
randn_like 噪声量空间均匀 —— 细节"原料"的分配与内容无关）。

**封棺（同日，gbg=0 极端点）：P1 又是 0/3，且方向反了 —— 死刑签字。**
背景引导 1.0 -> 0.4 -> 0 的背景高频走势：idx0 3.1/3.0/2.9、
idx2 18.6/17.7/17.9、idx4 24.4/21.6/20.7（单调降，最清晰）。
**全关不但不恢复细节，反而更糊** —— lowpass(基图) 一直在往背景里
"送"基图结构；关掉后只剩 LFM 的平滑内容 + 去噪器的均值回归。
**结构引导不是压细节的手，是托细节的手。** 假说的因果箭头画反了：
过平滑不是"细节被拽住"，是"细节从未被生成" —— LFM 初始化的噪声
原料空间均匀，低信息区去噪天然趋平。嫌犯正式移交 LFM 初始化，
证据正反两面（放松无效 + 全关更糟）。

**v2（空间化结构引导强度）按预注册撤。** 收获一条可进机制分析的
负结果：结构引导对背景细节是**净正贡献** —— 这条线没人量过。
没死的：v1 与 Campaign B 无涉；题目按"v2 判死也成立"选的，不动。
四臂 harness 与 SpatialGuidance 补丁完好，直接复用给后继假说。

候选 v2b：空间化 **LFM 噪声注入量**（背景多给原料、主体少给）。
**今天不动** —— 需要自己的预注册，且 P2a 的走向会改写其判据：
若 --armdelta 显示 v2only_g0（背景引导全关）都不招重复回潮，
互锁故事整个不成立，v2b 就只是独立细节旋钮，卖点重估。
P2a/P2b 待 vlm_count --armdelta 定案（检测器列已停用）。

### 3.14 v1 真考首轮：触发集 delta +0.59 -> +0.03，重灾样本清零（2026-08-13）

31/34 条 Parti 触发集（342/536/1445 无可数主体合法跳过），门开 s=1，
同 seed 同管线与基线臂逐条对照：

| | 基线臂 | v1 臂 |
|---|---|---|
| delta 均值 | +0.59（可比 31 条 ≈ +0.61） | **+0.03** |
| >=1 条数 | 13 | 6 |
| >=3 条数 | 2（1059 四卡车、331 四女神） | **0** |

**约 95% 削减，两个 >=3 重灾样本全灭。** 白捡的两条硬证据：
① **31/31 基础阶段逐像素同基线** —— "门只在放大阶段介入"实测成立，
   零代价主张的一半落地；
② 专名摘除规则精确生效（263 剩 "the Washington Monument"、
   331 剩 "helicopters"）。

**去污染定稿（同日）**：v1 臂的主体缓存起初没带修订（263 用回泛化词
'statue' 读出 −1 虚警），--resubject 补齐后重数 4 条：

| idx | 基线 | v1 | | idx | 基线 | v1 |
|---|---|---|---|---|---|---|
| 331 | +3 | **0** | | 263 | +1 | **0（1->1 主体完好）** |
| 1059 | +3 | +1 | | 157/553 | +1 | 0 |
| 738 | +2 | +1 | | 474/734/956 | +1 | +1 **未动** |
| 1187/1527 | +2 | 0 | | 994 | +1 | +1（画中狗，语义边界） |

均值 +0.61 -> **+0.10**；基线 12 条泄漏：7 清零、2 大削、3 未动；
>=2 样本 5 -> 0。**旗舰展品三要素齐**（263：基线长第二尊、v1 不长、
基础阶段逐像素同）——就差放大图画质终审。

**残留形态学 = 第二圈迭代的靶子**：清不掉的全是 +1 级小泄漏
（474/734/956），+2/+3 级全灭 —— 假说：大泄漏死于门控图明确的空区，
+1 藏在弥散/过渡带里（末样本过渡带 0.86）。验证：拉这三条的
gate_cov 与 mask 对照残留位置；坐实则第二圈方向 = 收紧 rel 门
（k 升/width 收）或门控图放大阶段刷新。
**唯一疑似误伤：56 花环 −3（0 -> −3）**，等放大图裁；
成本账以 manifest 逐条 sec 精算为准（粗算 87s 含加载/替代编码）。

**病灶特写终审（同日，arm_compare --crop 四张）**：
- **331 加冕旗舰**：基线 4096 在对岸水线**凭空立起一尊完整小女神**，
  基图同位置干净，v1 除掉 —— 三格齐活，即论文方法图；
- **738 实锤**：空旷间歇泉盆地远景多出微型红卡车，v1 清除；
- **583 实锤**：整面挡风布爬满铁丝网纹理 + 耙状怪物件，v1 基本清净
  （右下轻微残纹）—— "图与数字必须并列报"的现场证据；
- **263 反转，撤出展品**：放大后第二尊女神**基图里就完整存在**
  （SDXL 基础阶段的地标重复，不是放大阶段长的）。两笔认账：
  ① 早先低分辨率眼睛终审判错（"基图没有"是分辨率骗的）；
  ② VLM 的 +1 是**清晰度阈值伪影**——基图那尊在识别阈值边缘（读 1），
  基线精修磨得更像（读 2），v1 没那么锐（读 1）。配对设计消不掉
  "含混副本跨识别阈值"效应 -> 仪器注意事项 + 眼睛核账入协议；
  263 转岗为"基础阶段也犯地标重复"的机制旁证。
- 56 花环放大后左右完整一致，−3 属密集小物（15 朵，超出 <=6 运行域）
  的计数方差，**误伤嫌疑撤销**。
**实锤展品（双章）：331 / 738 / 583 / 1187；待定位：1527、1059 残留。**

### 3.2vb 方法 v2b 预注册：空间化 LFM 噪声原料（2026-08-13，写在跑之前）

**假说**：过平滑 = 细节从未被生成。放大阶段从
`add_noise(latents_LFM, randn, t_restart)` 起步（管线 L547-548），
这勺噪声就是细节的"原料"，而它空间均匀 —— 又一个"均匀处理 ×
异质内容"的实例。v2b：背景多给原料、主体照旧 ——
`noise' = noise · (1 + (γ−1)(1−m))`，m 是同一张免费门控图。
γ=1 时逐字节等同原版（乘 1.0 是位级恒等）。

**依据**：① 封棺实验正反两面（放松引导无效、全关更糟）指认原料侧；
② PixelRush 已证**均匀**噪声注入治过平滑（他们的 few-step regime），
我们做空间自适应版。**动机不写互锁**（armdelta P2a 1/6 已出局），
v2b 身份 = 独立细节旋钮。

**四臂**（复用 method_v2 目录，base/v1 两臂 manifest 命中直接复用）：
base / v1 / v2bonly_γ（只加噪声，s=0）/ v2b_γ（噪声+门，s=1），
γ ∈ {1.5, 2.0}，3 条 lone 诊断 prompt（选型用，不进论文）。

**预注册判据**：
P1' v2b 臂背景 Laplacian ≥ v1 × 1.10；
P2' v2b 的 armdelta ≤ v1 + 0.5（噪声不招重复 —— armdelta 上
    v2only 都不回潮，风险本来就低）；
P3' 耗时 ≤ base × 1.06；
**眼睛必审**：长出来的是真纹理还是彩色噪点/伪影（Laplacian 分不出）。
**判死**：γ=2 下 P1' 仍 0/3 -> 原料假说也死，过平滑章以三条负结果
收笔（引导放松无效 / 引导全关更糟 / 原料加倍无效），
论文收缩为 v1 单旋钮 + 机制分析；不再有 v2c，该病记为
"在该管线内未能定位到可空间化的旋钮"。

#### 3.2vb-R 首测结果（2026-08-13）：P1' 数字全过，眼睛全否 —— 修订终测

γ=1.5/2.0 背景 Laplacian ×17~×160（P1' 形式上 3/3 过），但放大图上
**两个 γ 全是彩色纸屑雨**，idx4 原生裁剪零语义。机制：乘 γ 突破
调度器在 t_restart 的 SNR 假设 —— 流形外注入，restart 后 20 步
消化不掉过剩方差，残余烙成纸屑。Laplacian ×160 量的是纸屑。
预测复盘：γ=2 必挂 ✓；γ=1.5 押"临界"✗（实际全崩，比预测糟）。
γ=2 时宇航员沾噪点 -> 门控图弥散（过渡带 0.86）主体保护不完全，
再一票投给"门锐化"。

**意外的正面证据**：鹰/登山者/宇航员在纸屑风暴里基本完好 ——
门控图的空间选择性被事故可视化："往哪儿打"对，"打什么"错。
（鹰图可当机制示意。）

**修订终测（预注册，打完结章）**：γ=1.1 单点。外推依据：残余能量随
(γ²−1) 收缩，γ=1.1 过剩方差为 γ=1.5 的 1/6，Laplacian 预计落回
v1 的 1.5~3×。**双判据**：眼睛无可见噪点 且 背景 Laplacian 在
v1 × [1.1, 3.0] 带内（>3× 即判纸屑）。不过 -> 原料旋钮记为
**无甜点区**（加方差 != 加信息，效果与伪影同源），过平滑章三负收笔，
不再有下一发。诚实预判：大概率"无收益与无伪影同时到达"。

#### 3.2vb-R2 γ=1.1 结果（2026-08-13）：**原料假说证实** —— 甜点区存在

Laplacian 三条全落带内：×1.61 / ×2.20 / ×2.16（预注册带 [1.1, 3.0]）。
**idx4 裁剪：雾散了，露出成层岩壁、冲蚀沟、深色灌木 —— 有语义的
地质结构，不是噪点**；构图仍被结构引导锚住。"无甜点区"的诚实预判
**错了**：γ=1.5 是给太多消化不掉，γ=1.1 消化掉大半 ——
过平滑 = 原料饥饿获得正面证据。残余尾巴：岩壁上散布白色小斑点
（过剩方差残迹），严格按"眼睛无可见噪点"判**接近未全净**。

**收尾（工程微调，不是假设检验，做完定死不再扫）**：
γ=1.05 对照一发 -> 斑点消且 >=1.1× 取 1.05；掉出带取 1.1 并记斑点
为已知代价（候选解：门控图锐化 —— 本来就是 v1 第二圈的活）。
P2'（armdelta，v2bonly_g1.1 臂）与全图三行眼审并行。

**对论文**："一张图、两个旋钮"整体复活，且旋钮二自带完整机制弧线：
引导洗清（两负）-> 病根定位原料侧 -> 流形外注入失败（三负）->
微量注入证实。§0.0 措辞不必收缩；Campaign A 拿回武器
（v2b 直指 FIDp/KIDp/ISp —— 背景从雾变岩理正是 patch 指标最敏感的）。

#### 3.2vb-R3 P2' + γ=1.05 对照（2026-08-13）：**互锁在原料旋钮上复活**；工作点定格 γ=1.1

armdelta（VLM 权威计数）：v2bonly_g1.1（只加料无门）= **+3/+2/0**，
v2b_g1.1（加料+门）= +1/0/0。**背景多给原料，重复就回来（互锁前半
2/3）；门开着压得住（后半 2/3）。** v2 讲不成的互锁故事在 v2b 上
有数据了：两个病通过"原料"耦合，旋钮二必须配旋钮一 ——
"一张图两个旋钮"升级为互锁系统。
待核：idx0 的 v2b 残留 +1（门未压到底 -> 又一票门锐化）；
v2bonly 的 +3 需眼睛确认是真人影不是雪地白斑入账。

γ=1.05 剂量不足：×1.00/×1.45/×1.10，P1' 1/3。剂量-响应陡
（1.05->1.1 过剩方差翻倍）。**按预注册规则执行：工作点定死 γ=1.1，
白斑记为已知代价（候选清理 = 门控图锐化，v1 第二圈的活），不再扫。**

#### 3.2vb-R4 四臂全图质量审计（2026-08-13）：**伪影场景依赖，v2b 未达展品线**

用户看图问"质量怎么这么差"—— 拆三个来源：
① 左两列（base/v1）的雾/糊 = **病本身**（过平滑在原生分辨率露馅，
   整图缩略与标准 FID 都看不见 —— 正是 FIDp 打 ScaleDiff 的那列）；
② 右两列的碎屑 = v2b γ=1.1 的真实残留，且**场景依赖**：
   峡谷岩壁长出真结构（岩层/冲蚀沟），**纯空天空长出垃圾**
   （idx2 火星行：天空飘碎石、地面撒杂物 —— 这行放进定性对比会被
   审稿人打死）；idx0 山脊上看到 v2bonly 多出的小人影
   （互锁的照片证据 ✓）；
③ 接触表 700px + JPEG 的混叠放大"差"感，论文图用原生裁剪。

**用户复核加签（同日，说得比我狠但对）**：v2b 当前工作点**综合成图
质量低于 v1**—— 不止伪影：idx0 上把 v1 已清零的重复又带回 +1
（门没完全压住加料引来的重复）。**出货配置 = 纯 v1**（E4/展品/
标准表全部只带 v1）。v2b 重新上船的硬门槛：修复后同时满足
重复 ≤ v1（零倒退）+ 无可见伪影 + 细节增益保持，差一条不上船；
救不回则按 fallback 出货 v1 单旋钮 + 过平滑机制分析章
（两负 + 原料证实 + 互锁数据作为"分析"而非"功能"呈现）。

**战略定位改判：v2b = 已验证机制、待工程收敛，现阶段不进任何展品。**
清理杠杆：1) 门控图锐化（一改吃三伤口：v1 残留 +1 / v2b 主体保护 /
白斑边界）；2) 注入限制在"基图有中频内容"区域（天空不给料）——
记档，等锐化结果再决定。Campaign A 排期不据此冒进：v2b 先过
自己的定性关，才谈 FIDp。**主力转 E4/DemoFusion（只依赖 v1）**，
v2b 打磨为第二战线。

#### 3.14b v1.1 重录判死 + **真病根：门从来只是"半开"**（2026-08-14）

预注册 R1（过渡带 < 0.3）**没过，且方式极有价值**。band_stats
（主体覆盖, 背景<0.3, 过渡带）：263 (0.42, **0.00**, **1.00**) ->
重录后 (0.42, 0.00, 1.00)；474 同；734 0.72->0.74；956 0.69->**0.79**。
**263/474 的低权重区是 0.00 —— 整张门控图没有一个位置被判为背景。**

> **门从未真正打开或关闭过，它在整幅画面上一直是"半开"（权重挤在
> 0.5 附近）。v1 的 +0.61->+0.10 是在这种状态下取得的 —— 效果上限
> 远未触及。**

**病根不是分辨率，是平均**：record 把 ~70 个 attn2 层 × 50 步一锅
平均，深层注意力本就弥散，平均后 r 处处落在均值 ±10% 内，经
linear 归一化 `((r−k)/width+0.5)` 全挤进 [0.3,0.7]。重录只提分辨率
（64->256），**平的图放大还是平的**，故毫无改善 -> 判死支执行：
分辨率不是残留 +1 的原因。

**一个病根解释三个悬案**：残留 +1（背景从未真正拿到去主体条件）、
v2b 白斑溅到主体（主体从未被真正保护）、过渡带 0.86。

**修法三档（gate_map_probe.py，只跑基础阶段 ~8s/张）**：
① 选层（只用某分辨率的 attn2）；② 选时间步（布局形成期）；
③ **分位数归一化**（直接规定背景/主体面积，与 r 平不平无关 ——
无 GPU 自检实测：极平图上 linear 过渡带 100% vs quantile **13%**）。
纪律：配置在**诊断集**上选（§6.9 允许，脚手架正是干这个的），
选定后才上触发集验证一次。

#### 3.14c v1.2 选层：把"半开"的门修锐（2026-08-14，探针已出数，触发集待验）

`gate_map_probe.py` 只跑基础阶段（~8s/张），把 record 拆到
**逐层**，直接量每层单独的对比度（max/p50，**不是 p95** —— 2% 面积
的主体会被 p95 整个吞掉，实测在一张真掩码上读出 1.00 的假平）：

| | 对比度 max/p50 |
|---|---|
| 最锐的单层 | **4.24** |
| 最平的单层 | 1.07 |
| **全层平均（= v1 一直在用的）** | **1.50** |

**平均本身就是病根**：把 4.24 和 1.07 混在一起，得到的是 1.50。
不是深层注意力"本来就弥散"到没法用 —— 是好层被坏层稀释了。

离线搜 top-K 组合（诊断集上选，§6.9 允许）：

| K | 对比度 | 逐样本主体覆盖 idx0 / idx8 / idx20 |
|---|---|---|
| 2 | 3.99 | 24% / 19% / 35% |
| **4** | **3.68** | **25% / 19% / 42%** |
| 8 | 3.10 | 27% / 19% / 42% |
| 16 | 2.50 | 30% / 19% / 33% |
| 全层（v1） | 1.50 | 41% / 18% / 43% |

**K=4 是甜点**，选中四层：
`up_blocks.0.attentions.{1,0}.transformer_blocks.3.attn2`、
`down_blocks.2.attentions.1.transformer_blocks.4.attn2`、
`up_blocks.0.attentions.0.transformer_blocks.2.attn2`
（记在 `gate_layers.json`，含实测对比度，便于复现）。

比对比度更能说明问题的是**覆盖的自适应性**：idx0 是单主体小目标，
idx20 是人像近景。全层平均给出 41% vs 43% —— 几乎不分，而且**顺序
是反的**（小目标反而被判得更大）；top4 给出 25% vs 42%，顺序正确、
量级合理。**门第一次会"看图说话"了。**

工程护栏：选层名单若对不上 UNet 的处理器键名，门控图一张都录不到，
map 恒为 None，weights() 返回 None，输出与基线**逐字节相同** ——
一次"看起来跑了、其实什么都没做"的静默失败。故 `v1_trigger_run.py`
在装处理器后硬校验名单，对不上直接 SystemExit。

**预注册判据**（写在跑之前）：
- R1 过渡带（0.3~0.7 面积占比）从 1.00 降到 < 0.5，且低权重区 > 0.1
  （即**真的存在被判为背景的位置**）；
- R2 触发集 34 条 delta 均值 <= v1 的 **+0.10**，且 474/734/956
  三条残留 +1 至少清掉两条；
- R3 不误伤：delta = −1 的条数不增加；基础阶段仍 31/31 像素同一。
- R1 过、R2 不过 -> 选层不是残留 +1 的原因，转 v1.2 多变体门；
  R1 不过 -> 归一化才是瓶颈，换分位数归一化（探针实测极平图
  linear 100% vs quantile 13%）。

#### 3.14d v1.2 判后：R1 过、R3 破、R2 表面平 —— 但**度量本身有缺陷**（2026-08-14）

**预注册对账**（判据写在 §3.14c，跑之前）：
| | 判据 | 实测 | |
|---|---|---|---|
| R1 | 过渡带 <50% 且背景 >10% | 20/31 行；过渡带 100%→**40%**，背景 0%→**38%** | **过** |
| R2 | 签名均值 ≤ +0.10 且 474/734/956 清两条 | **+0.10**（分毫未动）；734 清了，474/956 没清 | **不过** |
| R3 | delta≤−1 的**条数**不增加 | v1 有 1 条（56 是 **−3**），v1.2 有 2 条（734、1119，各 −1） | **破** |

**先纠正一个错读**：均值没动**不等于**门没做事。31 行里 **5 行**结局变了
（56 −3→0、738 +1→0、734 +1→**−1**、1119 0→−1、1257 0→+1），
签名变化合计 `+3−2−1−1+1 = 0`，**精确抵消**。
-> 门有作用但**方向随机**，指向"门错"（错位/过强）而非"门无效"。

**H2 判死**（v12_diag.py，Fisher 双侧 p=0.631）：残留失败并未集中在
11 条半开行 —— 开门行失败率 20%、半开行 9%，方向甚至相反。
**注意混淆**：门开得大恰恰发生在主体小、空视野大的行，那正是律的高段
= 本来最难的行。故此检验对 H1 的支持力弱于表面，**判决仍归 --uniform**。

> **⚠️ 度量缺陷（本轮最重要的发现，与方法无关）**
> 两臂 delta 总和**都是 +3**：v1 = 6 个 +1 减**一个 −3**；
> v1.2 = 5 个 +1 减**两个 −1**。行为完全不同，签名均值相同。
> **负 delta（主体被毁）在均值里冲抵正 delta（重复）** ——
> 一个"把主体全删光"的方法能在这把尺上刷出漂亮数字。

**拆成两列后结论反转**：

| | Rep⁺ 重复 | Dmg⁻ 主体误伤 |
|---|---|---|
| 基线 | **0.61** | 0.00 |
| v1 | 0.19 | 0.097 |
| **v1.2** | **0.16** | **0.065** |

**v1.2 在两条轴上都优于 v1**，签名均值把这件事完全遮住了。

**关于"看到结果后改度量"的自律**：这接近 p-hacking，必须交代清楚。
之所以正当，是因为缺陷**结构性**（允许误伤冲抵重复的度量在任何数据上
都是错的），不是挑了个有利读法；且它对我们**并非全是好消息** ——
Dmg⁻ 这一列以前不存在，现在必须公开承认 v1 曾把 56 的主体毁掉 3 个实例。
**已在 AccDiffusion 闸门实验之前锁死**（vlm_count.py --delta 与
v12_diag.py 同时输出两列），此后所有方法共用同一把尺。
**判赢条件改为：两列都要变好。**

**地板效应（对任何后续结论的置信上限）**：基线残留正超额 19 个实例，
v1.2 只剩 **5 个**。n=31 下分辨"5 与 4"统计上做不到。
-> **主结果 +0.61→+0.16（Rep⁺，19→5 个实例）是结实的**，
但**继续在这 31 条上比配置就是在噪声里选优**。
下一步不是接着磨门，是**扩样本 + 打闸门**。扩样本可负担：
v12 全程 44 分钟 / 31 张（约 1.4 分钟一张），且 evf 在**基图**上算，
筛选便宜；扩到 ~120 条约 3 小时一臂。

#### 3.14e 方法线判死：干预家族在 Rep⁺≈0.23 处有硬墙（2026-08-14）

**预注册判决实验**（pareto.py，判据写在看到数字之前）：同一选层 top4 下
扫强度 s=0.7/1.0/1.3 画基准曲线，看 v2a2（s=1.0 + 推拉）落在哪。

| 配置 | Rep⁺ | Dmg⁻ | 总误差 |
|---|---|---|---|
| 基线 ScaleDiff | 0.588 | 0.000 | 0.588 |
| **s=0.7** | **0.258** | **0.000** | **0.258** |
| s=1.0 | 0.226 | 0.032 | 0.258 |
| s=1.3 | 0.226 | **0.129** | 0.355 |
| v2a2（s=1.0+推拉） | 0.161 | 0.161 | 0.323 |

**判决：v2a2 高于曲线 +0.129 -> 方法线停掉。**

三条读数，缺一不可：

**① 强度在 s=1.0 之后饱和。** s=1.0→1.3 的 Rep⁺ **一点没降**（都是
0.226），误伤翻四倍。这不是权衡，是纯亏。**该干预家族在 Rep⁺≈0.23
处有一堵硬墙。**

**② 判决的技术细节须如实交代（对推拉略有利，不改结论）**：强度扫描
**从未到达 Rep⁺=0.161**，故脚本在该处的"插值"实为向左平推外插 ——
对曲线最有利的假设。严格说，推拉**做到了强度做不到的事**，只是兑换比更差：

    s 0.7→1.0   Rep⁺ −0.032 / Dmg⁻ +0.032   = 1:1
    s 1.0→1.3   Rep⁺  0.000 / Dmg⁻ +0.097   = 纯亏
    加推拉      Rep⁺ −0.065 / Dmg⁻ +0.129   = 1:2

即推拉不是"同一个旋钮"，是**一个更差的旋钮**。判死成立。

**③ 反而撞出了最干净的配置**：**s=0.7 -> Rep⁺ 0.258、Dmg⁻ 0.000**，
基线降 **56%**、**零主体误伤**、+3.9% 成本、门关时逐字节不动。
这是本项目至今最干净的一个点，且是在做判死实验时顺手测出来的
（此前每个版本都带误伤）。**保留为组件级结果，不再作为论文主角。**

**结构性解释（写进新论文的消融）**：
> 空视野区域里结构引导几乎不提供信息，**文本是唯一的条件信号**。
> 只动文本条件的干预因此只有一个自由度 —— 你只能在"给它主体"和
> "给它更少的东西"之间选，没有第三个选项。这解释了为什么
> **七次尝试（v0/v1/v1.1/v1.2/v2/v2b/v2a）全部撞在同一堵墙上**。

**判死后的处置**：
- ❌ 只扔一样：**"我们的门是更好的重复抑制方法"这个定位**；
- ✅ 全部保留：律（R²×(1−覆盖)）、触发率 <3%、检定过的计数器、
  触发集与四臂已跑好的图、**门关时逐字节相同**这一性质
  （它正是"条件触发"的技术前提）、AccDiffusion 那句
  "existing quantitative metrics are unable to..."（§9.5f）、
  以及现成的定性素材（ScaleDiff 把天际线塔变成女神像；
  AccDiffusion 在 734 凭空造出一艘发光大船）。

### 3.15 适用性门（v1.1，方法的一部分，不是补丁）

> **介入 ⟺ 基图空视野比例 > τ 且基图检出主体 ≥ 1。**

门就是律的触发条件本身：主体铺满画面时不存在"没有主体的邻域"，没有要抑制的
东西。门关的行**逐字节等于原版** —— "绝不碰不该碰的图"是硬性质（P2 实测 5/5，
出回归的行全部被关掉）。代价：基图上跑一次检测器，~2s（+2.7%）。

**阈值 τ 的诚实状态（2026-08-09）：**
- 预注册的 0.5 **失败**（落在 lone 群内部，关掉了 04 的真实改善）；
- 数据里两群分离：不该干预的行 ≤31%，该干预的行 ≥44%，
  **τ ∈ (5/16, 7/16] 是平台**，区间内四条判据全过；
- 临时取 **3/8**（16 块中 >6 块为空才介入），**必须在新数据上验证后才算数** ——
  这已是同一批 30 条上的第五轮评估（v0/v1/v2/门×3 阈值），
  **方法就此冻结，一切后续决定只在新数据上做**。

leak_check 的阴性结果（文本相似度探不到 holding->手 的蕴含泄漏，
CLIP 文本空间量的是场景共现）说明泄漏门不可行；但门关掉那些行之后
泄漏没有作用的机会 —— strip_subject 只需在门开的行上干净，而那些行本来就干净。

### 3.2 v2 是阴性结果，进消融表

把门控图的归一化从逐图 min-max 换成 `r = m/mean(m)`（相对均匀分布的倍数）：
**所有类别的 cov 都塌到 ~50%**，因为"高于全图平均的位置占多少"对任何平滑正值场
天然就是 40–60%。**把有信息的那一维归一化掉了**，lone MAE 2.00 → 3.00。

---

## 4. 贡献（照 ScaleDiff 的三条式）

1. **命名并量化了高效高分辨率外推的残余失效**：物体重复由**邻域/画布比**支配，
   而非分辨率本身；并量出了打断局部性的汇率（+37% 时间 / +41% 显存）。
2. **提出单次前向内的逐位置双条件机制**，以 **+4%** 的代价抑制重复 ——
   对照 AccDiffusion v2 的 **14 倍**（1599s vs 113s）。
3. **给出这个任务上第一个物体重复的定量测量**。协议本身在 T2I 计数线里是成熟的
   （CountGen 等用 GroundingDINO/YOLO/DETR 数数报 MAE），**我们不是发明指标，
   是第一次把它用到高分辨率外推上**；没被用过是有原因的 —— 直接套会漏检 80%，
   必须做**尺度匹配分块**。

---

## 5. 目标数字（Table 2，SDXL 4096²）

### 5.0 "标准评估不超越，是不是就不成立"（2026-08-11，用户提出，必须正面回答）

问题问得对：主方向是 high-resolution image generation，若标准表一列都不赢，
这篇还算不算数。分四层答，**其中第三层是止损点，现在就定，不等四天后**。

**一、这条线的门槛从来不是横扫。** ScaleDiff 自己 KIDp 输、ISp 输；
AccDiffusion v2 与 DemoFusion 的 FID 差是 **64.64 vs 65.06 = 0.42 分**。
FID 在 4096² 上被基座模型主导，方法间差异本就淹在协议差异里
（何况我们的基线行是自己复现的，绝对值与发表值不可比，§7.1.5）。

**二、更直接的先例：AccDiffusion v1 (ECCV24) 整篇就是为"物体重复"写的**，
v2 的整个消融都是定性图。**以重复为主线的论文在这个社区被接受过。**
我们相对它是**严格更强**：他们没有重复的定量指标，我们有一个
**不需真值、不需标注**的 Δdelta。

**三、真正该瞄的是 patch 三列，不是 FID。** 跨度 26% 对 5%，没饱和，
在位者输两列（§6.9.0a 已给出机制：畸形复制体在原生分辨率裁块上留得下痕迹）。
**但这是机制推理，没测过。** 所以预注册止损点：

> **若 tune 上触发率 < 10%，且触发子群上 patch 三列也落在噪声内
> -> 这条路在 LAION-aesthetic 上打不出数字。** 那时的正确反应不是硬写，
> 而是换承载面：CoCoCount 那批带场景短语的 prompt
> （`fetch_count_bench.py` 已预注册"门该开"的预测）。

**四、加固。**（原稿把这两条包装成"绕开标注池"，是把项目开始就定死的约束
又拿出来重讲一遍 —— 没有标注员是前提，ScaleDiff 本身也没有 user study，
不是需要解决的问题。去掉那层包装后，两条的分量差别很大：）

1. **必需 —— 拿有真值的数据验证我们的指标。** 理由与标注员无关：
   **Δdelta 是我们自己新造的量，新指标必须证明它测的是它声称测的东西。**
   CoCoCount / GenEval 的计数写在 prompt 里，是现成真值。
   "在有真值处验证、再用到无真值处"是新指标被接受的标准路径。
2. **可选 —— VLM 当裁判**（Qwen2.5-VL-7B，本地）出偏好胜率。
   这条线的论文都没有它，不该与第 1 条并列。

ScaleDiff 自己**并没有横扫** —— 它 KIDp 输、ISp 输。所以门槛是**不输**，不是横扫。

| | FID↓ | KID↓ | IS↑ | FIDp↓ | KIDp↓ | ISp↑ | CLIP↑ | Time↓ | **Rep↓** |
|---|---|---|---|---|---|---|---|---|---|
| DemoFusion | 65.06 | .0041 | 19.13 | 41.29 | .0079 | 19.59 | 32.61 | 1005 | ? |
| AccDiffusion v2 | 64.64 | .0037 | 18.56 | 40.92 | .0083 | 18.42 | 32.34 | 1599 | ? |
| **ScaleDiff** | **61.87** | **.0025** | **19.56** | 38.89 | .0080 | 20.41 | **33.04** | **113** | **5.40** |
| **我们（目标）** | ≈61.9 | ≈.0025 | ≈19.6 | **争取赢** | **争取赢** | **争取赢** | ≈33.0 | ~117 | **2.00** |

- **FID / KID / IS / CLIP**：不实质性倒退即可。方向未知 —— 去掉幻觉主体可能反而更好，
  也可能因高频 −1.1% 而略差。**必须测，不能假设。**
- **FIDp / KIDp / ISp**：竞争带跨度 38.89–48.97（**26%**，是 FID 那 5% 的五倍），
  **没饱和，且 ScaleDiff 输两列** —— 这是标准表里的现成机会。
- **Time**：117 秒和 113 秒在那一列里肉眼无差；真正的对照是"修重复要付多少"。
- **Rep**：新列。基线 5.40 → 2.00（**−63%**），动态范围是 FID 那条轴的 **12 倍**。

### 5.1 支撑图：效率 ↔ 一致性的汇率

我们自己曲线上的两个点，**必须明说，藏了反而弱**：

| 打断局部性的做法 | 效果 | 代价 |
|---|---|---|
| KV 邻域 ×2 | 部分修复 | **+37% 时间 / +41% 显存** |
| **改文本条件（我们）** | 更大幅修复 | **+3.9% / +1.7%** |

---

## 6. 尺子（三把，都有阳性对照）

| 尺子 | 阳性对照 | 能说什么 | **不能**说什么 |
|---|---|---|---|
| 计数器 MAE | 五条预注册预测过 4.5 条；阈值 0.50–0.80 是平台 | 物体级重复 | — |
| CLIP score | 配对 29.96 / 错配 2.38，判对 **30/30** | 语义没变（+0.02%） | **细节** |
| 频谱高频 | 双三次 **−94.6%** | 细节没塌（−1.1%） | 语义 |

**CLIP 对质量免疫**：高斯模糊 r=8 实测 **+1.18%**（糊了反而涨）—— 半径 8px 缩到
320² 只剩 0.6px，根本没进视觉塔。**±1% 是噪声。**

> ⚠️ 别把这条扩大解释成"领域指标看不见细节"。AccDiffusion v2 自己把 CLIP 定位成
> additional ***alignment*** metric，他们测细节用 FID_c（裁 10 个原分辨率 patch，
> 那是看得见的）。**领域只对物体级计数盲，缺口就这一条。**

### 6.0 计数器不是我们发明的 —— 直接引用已发表协议（2026-08-10）

这是对"你的 metric 自己造的"最直接的回答：**T2I 计数那条线早已把
"现成检测器 → 数目标类实例 → 与要求的数字比"确立为标准协议**，
两篇都有开源码、都在会议上：

| | venue | 检测器 | 判据 |
|---|---|---|---|
| GenEval | NeurIPS 2023 D&B | Mask2Former(COCO), conf 0.3 | 检出数 == 要求数 |
| CountGen / Make It Count | **CVPR 2025** | YOLOv9e (ultralytics 默认) | `is_success = (count == expected)` |

我们与他们的差别只有两处，都写进正文：

1. **粒度**：他们报二值正确率，+9 和 +1 都只算"错"。我们报 excess 的 MAE，
   因为幅度是主张的一部分。**两个都报**（acc 对齐这条线，MAE 说明幅度）。
2. **分辨率 —— 这一处是论据，不是限制**。YOLOv9e 默认 `imgsz=640`：
   4096² 直接喂进去，60 px 的重复物体缩完只剩 9 px，**与 FID 压到 299²
   是同一个失明机制**。所以唯一的偏离是分块推理，并由 `scale_check.py`
   的降采样一致性背书。`cross_detector.py` 把"不分块"那一档也跑出来，
   让这句话有数字：**领域现成的计数协议在 4096² 上也测不到这个失效。**

**实测结果（2026-08-10，seed 77/1234/2025）：**

**lone（15 行 = 5 prompt × 3 seed）：**

| | 基线 MAE | 我们 MAE | 降幅 | 基线 acc | 我们 acc |
|---|---|---|---|---|---|
| GroundingDINO 分块（主） | 5.20 | 1.67 | **−67.9%** | 0% | 7% |
| **YOLOv9e 分块**（CountGen 那把） | 4.60 | 1.40 | **−69.6%** | 0% | **40%** |
| YOLOv9e 原样（不分块） | 1.27 | 0.47 | −63.2% | 27% | 67% |

**共同子集（两把尺子都测得了的 15 prompt × 3 seed = 45 行）—— 同口径：**

| | 基线 MAE | 我们 MAE | 降幅 | 基线 acc | 我们 acc |
|---|---|---|---|---|---|
| GroundingDINO 分块 | 1.87 | 0.73 | **−60.7%** | 53% | 53% |
| YOLOv9e 分块 | 2.00 | 0.80 | **−60.0%** | 49% | **64%** |
| YOLOv9e 原样（不分块） | 0.47 | 0.22 | −52.4% | 71% | 82% |

**empty（负对照）基线假阳性**：不分块 0.00 / YOLO 分块 0.13 / GDINO 分块 0.27。

**两把尺子的一致性（共同子集）：**

| | Pearson | Spearman | 平均绝对差 |
|---|---|---|---|
| 绝对计数（n=90） | +0.747 | +0.544 | 0.90 个物体 |
| **A/B 差值（n=45）** | **+0.869** | **+0.751** | **0.60** |
| 改善方向一致的行 | **84%** | | |

四条要写进论文的读数：

1. **同口径下两把尺子差 0.7 个百分点**（−60.7% vs −60.0%）。YOLOv9e 按
   ultralytics 默认阈值跑、一个参数没调。结论不依赖任何单一检测器。
2. **A/B 差值比绝对计数更一致**（r 0.869 vs 0.747）—— 正是该有的样子：
   两个检测器对**绝对物体数**分歧较大（我们不主张这个），
   对**介入带来的变化**高度一致（我们主张的是这个）。
3. **acc 与 MAE 的分歧是估计量的性质，不是挑口径。** GDINO 下 acc
   53%→53% 纹丝不动而 MAE 降 60.7%：我们把"错得离谱"变成"错一点"，
   很少变成"完全正确"。原因是 acc 要求恰好相等，一个误检就翻面，
   而 GDINO 负对照上的假阳性是 0.27/张，多数行永远碰不到相等；
   假阳性 0.13 的 YOLO 就给出 49%→64%。**MAE 主报，acc 附报，
   并把这条脆弱性写明** —— 读者可自行验证，不是找补。
4. **现成协议原样跑是"衰减"，不是"全盲"** —— 1.27 / 4.60 = 27.6%，
   **低估 3.6 倍**，方向仍对（−63.2%）。§1.3 按"低估"讲，不许写成看不见。
   代价那一边也有数：分块把 empty 上的假阳性从 0.00 抬到 0.13–0.27。

COCO 覆盖（已核对）：**5 条 lone + 5 条 empty 全在 COCO 类内**
（person/boat/bird）—— 主结果与负对照全覆盖；有基数的 20 行里 15 行可测，
eye/hand/building/bridge/staircase 落在 COCO 外，这正是主尺子用开放词表
GroundingDINO 的理由。

> **自造的 CLIP 框级核验（`box_verify.py`）降级为附录敏感度分析。**
> 实测三条否决理由：① 它没解决当初的例子 —— 03/2025 里船 (2159×688)
> 和正下方的倒影 (1214×501) 核验分 +0.0799 / +0.0403 **都判保留**，
> 负例里写了 "a reflection on water" 也没用（零假设由"在冰川上查 house"
> 这类离谱错误构成，太容易，必然放过倒影这种近似正确的错）；
> ② 它删过真东西（03/1234 我们这版核验后计数 0，excess −1）；
> ③ 改善幅度随零假设定义漂：−67.9% / −69.4% / −76.8%。
> **多一个自造自由度就多一个被攻击面，而已发表协议里没有这一步。**

### 5.4 τ=0.75 在 LAION 上几乎不可达 —— 看图之后的三条改正（2026-08-11）

`trigger_vis.py` 把网格、框、逐块判定画出来看了 12 张。三件事同时成立，
**必须分开记，否则会把语料属性当成仪器故障、或者反过来**。

**一、约一半的图确实主体占满画面，evf=0 是对的。** 爆米花、靴子、橙汁、
比尔亚尼、面包房、圣诞树 —— 不管框画得准不准，人眼看也是满的。
**这部分是 `laion2B-en-aesthetic` 的语料属性**（商品/美食/室内图，
摄影惯例就是主体居中占满），不是尺子的错。

**二、另一半框明显过冲，而且偏差是单向的。** Jeep 在中间、左上森林与
左下马路本是空的，框却横跨整幅；木屋的天空与草坪同样被圈进去；
披肩两侧的纯色背景被几个游离框盖住。**过冲只会把 evf 往 0 压，
也就是系统性低估触发率** —— 所以现在这个数不能拿去回答普遍性问题。
（用户说"框不准但不影响"：对计数确实影响有限，
**但空视野比例的定义就是"多大比例的块没有被框覆盖"，框的精度即测量值本身**。）

**三、`min_score` 那一刀，数据否掉了我的推理。** 我按 §5.5 的"门要召回"
主张把它从 0.50 放到 0.30。并列看两档，实际是**一换一**：
`[9]`/`[12]` 从 nbox=0（无定义）被救回；但 `[10]` 从 evf=0.12 掉到 **0.00**
—— 低阈值在纯色背景上长出 7 个框，**恰好把我们唯一想要的信号填满**；
`[0]` 也从 1 框变 4 框。**这个量对假阳性框的敏感度高于对漏检的敏感度，
所以"门要召回"这条通则在这里不成立。** 保留 0.50，反例记在这里。

**于是最根本的一条：τ=0.75 是拿极端样本标定的，用它量真实语料必然读出 0。**
即使框完全准确，按图估：Jeep 约 6/16 块空 -> evf≈0.62；木屋 8/16 -> 0.50；
贴纸 4/16 -> 0.75。而 τ=0.75 要求 16 块里有 **12 块**空，那需要
"小主体 + 大片空场"。τ 当初定在我们那 30 条诊断 prompt 上，
**那批是为展示现象而刻意构造的**（"a lone hiker on a rocky ridge"）。

**改正（改的是做法，不是因为数字不好看）：** 我写过"τ 沿用预注册值、
不在 LAION 上重新拟合"。这条要改，理由是**律本身是连续的**
（重复 ≈ R²×(1−覆盖率)），τ 只是把连续量切成二值的一个**部署选择**，
而部署选择本就该在目标语料上按代价/收益定。正确做法：

> 在 **tune** 上按 evf 分层抽样约 150 条，跑基线 4096²，
> 量 **Δdelta 随 evf 的变化**。一跑两用：
> **(a) 在 LAION 上重建律**（§6.9.0 表里的第一行本来就要做）；
> **(b) 按代价/收益在 tune 上定 τ**。**eval 全程不回看。**

这不是"同一批数据既定阈值又出结论" —— tune split 的存在意义正是这个，
且是在看 eval 之前定的。**若连续关系在 LAION 上也不成立，那才是真正的
坏消息**，届时走 §5.0 的止损分支。

### 5.45 tune 全量结果：触发率 ~2%，止损条件已满足；随之而来的改题（2026-08-11）

**数据（560 条，box_thr 0.30 / min_score 0.50）：**

| τ | 0.50 | 0.625 | 0.75 | 0.875 |
|---|---|---|---|---|
| 触发 | 12/409 = **2.9%** | 11/409 = 2.7% | 7/409 = **1.7%** | 4/409 = 1.0% |

分位：`p0..p75 = 0.00`，`p90 = 0.25`，`p100 = 0.94` —— **一根落在 0 上的尖峰
加一条极细的尾巴**。从 0.75 降到 0.50 只从 7 条变 12 条，**没有任何阈值能让它
变成有意义的子群**。§5.4 那个"在 tune 上按 Δdelta 重定 τ"的计划到此断掉：
**τ 不是定得不对，是这个变量在这个语料上根本不激发。**
`nbox=0` 占 **27.0%**（警告触发）：那 151 条预测量无定义，即便全算触发上界
也只有 28%，但不可信 —— nbox=0 多半意味着主体铺满或是纹理/图样。

**§5.0 的止损点在触发率这一侧已满足。** 于是必须承认：

> **"在 ScaleDiff 上修 4096² 的物体重复"这个题目撑不起 CVPR** ——
> 问题在标准语料上只占 2%，机制是已知思路的变体，标准指标看不见它。
> 而且**速度不是我们的贡献**（ScaleDiff 的 113 s 是它自己的）。

#### 但这个 2% 是新题目的前半句：失效随分辨率平方放大

几何：设主体占面积比 a，在 R×R 网格上它大致覆盖 (√a + 1/R)² 的块，所以

> **evf ≈ 1 − (√a + 1/R)²**

| 主体占面积 | R=4（4096²） | **R=8（8192²）** |
|---|---|---|
| 50% | **0.08** | **0.31** |
| 25% | 0.44 | **0.61** |

**同一批图，4K 上读 0，8K 上就不是 0。** 网格越细，"框擦到块角"造成的
虚假覆盖越少，evf 越接近真实空白面积比。而律的另一半 R² 同时从 16 涨到 64
—— **两个因子同向放大**。

论文形状因此改成：

> **4K 上重复只影响 2%，所以领域可以忽略它、标准指标也照不出它；
> 但它是随分辨率平方放大的失效模式，到 8K 就成为主要障碍。
> 我们给出只用基图就能事前算出的量，定量预测这件事，
> 并以 +3.9% 的代价把它挡住。**

今天这个 2% 于是从"我们的问题不存在"变成**第一句论据**：
它解释了这个障碍为什么至今没被认真处理。而**速度不再是卖点，
"让下一个分辨率档变得可用"才是** —— 这一点正好绕开"速度不是我们的"。

外部条件也支持：检索到的公开工作（PixelRush / FreCaS / HiWave / ScaleDiff /
HiPrompt）评测点都停在 4096²，**8192² 的 training-free 生成基本无人占位**。

#### 三个门槛，按"最便宜且最可能否掉它"排序

1. **单张 4090 能不能跑 8192²。** 不成立则方向当场结束。
   `run_one.py --stages 3`（`upsample_stage` 循环本就通用，
   `pipe.vae.enable_tiling()` 已在，OOM 有捕获）。产出三个数：
   能否通过 / 显存峰值 / 单图秒数。
2. **evf 在 R=8 上是否显著高于 R=4。** `trigger_select` 已改成存框并同时
   算 R∈{2,4,8}（框只检一次，几乎免费）。**若 R=8 与 R=4 差不多，
   上面那套说法就被否了。**
3. **重复量在 8192² 是否真的比 4096² 严重。** 前两条都过才做。

### 5.47 直接测量的结果：4K LAION 上重复量 ≈ 0，预注册第二档兑现（2026-08-12）

120 条（四层各 30），ScaleDiff 基线 4096²，84 s/张、峰值 11.9 GB：

| 层 | n | 基图框 | delta_content | delta_raw | 漂移 |
|---|---|---|---|---|---|
| A evf=0 | 30 | 1.1 | **0.00** | 0.60 | 0.60 |
| B 0<evf≤.25 | 30 | 2.0 | −0.23 | 1.67 | 1.90 |
| C evf>.25 | 30 | 1.5 | **+0.27** | 2.33 | 2.07 |
| D 盲区 | 30 | 0.0 | +0.17 | 0.83 | 0.67 |

C − A = **+0.27**（功效下限 1.5）；Spearman(evf, delta_content) = **+0.128**。

**五条读法，按重要性排：**

1. **主结论（预注册第二档）**：4K LAION 上重复量各层都在噪声里 ——
   "2%" 现在有了**对问题本身的直接测量**背书，不再只靠代理。
   评测缺口事实的 LAION 半边闭合：**这条线用来出表的语料里，
   他们声称修复的失效基本不发生。**（功效注记：只能排除 ≥1.5 个物体的效应。）
2. **漂移列救了一次命**：朴素 delta_raw 读出 0.60–2.33 —— 若当初用
   朴素量，C 层会"测得" +2.33 个物体的重复，**假阳性**。降采样对齐后
   剩 +0.27。raw−content 的 0.6–2.1 与 scale_check 在诊断集上量到的
   2.00 漂移一致。**"没人能可信地量重复"本身多了一条证据：
   连最显然的测法都被检测器尺度漂移污染。**
3. **D 层（盲区）干净**（+0.17）：检测器检不出框的那 27% 里**没有藏着
   重复** —— 门不会整批漏掉失效。之前担心的方法硬伤没有兑现。
4. **律在 LAION 上既未证实也未证伪**：ρ=+0.128 方向对但弱。原因是
   **值域压缩** —— 全部样本挤在 evf≈0 附近（p90 才 0.25），没有高 evf
   样本可供检验。律的检验必须去有高 evf 的语料（CoCoCount / 诊断集），
   那里已有 +5.40 的量测。
5. **保守性警告（看图之前不下最终结论）**：delta_content 降采样后数，
   主体级大副本留得住，但柯基图那种 **1/8 尺寸的小克隆**可能掉到检测线
   以下。`hi_contact.py` 拼 C 层接触表，眼睛复核后这一节才算封口。

**自检 0/120 逐字节相同的处理**：不影响本表（已回退到同跑基图）；
嫌疑首推 laion_hi_run 照抄 run_one 开了 `vae.enable_tiling()` 而 base_run
没开（分块解码数值差）。`base_diag.py` 四方对比定案；结果决定那 560 张
基图与 evf 是否原样可用。

**对论文形状的净效果**：形状二的第一根柱子（评测缺口）LAION 半边
**已经用问题本身的测量闭合**。下一步是触发侧半边：在 CoCoCount 式
"主体+场景" prompt 上重复同样的测量，预期 delta 大。两边都有数，
"展示语料与评测语料错开"就从观察变成定理级的实证事实。

### 5.49 三个语料的 evf 并排：触发区在**所有**标准语料里都罕见（2026-08-12）

| 语料 | n（有定义） | evf 均值 (R=4) | 触发 τ=0.75 | nbox=0 |
|---|---|---|---|---|
| LAION-aesthetic | 409 | ~0.03 | **1.7%** | 27.0% |
| GenEval 裸模板 | 72 | 0.019 | **1.4%** | 10.0% |
| **CoCoCount（一半带场景短语）** | 155 | **0.014** | **0.6%** | 22.5% |

**预注册的预测失败了，而且失败得很干脆**：CoCoCount 带 50% 场景短语，
按 caption-结构假设它该右移，实测它**比裸模板的 GenEval 还低**，
p0–p90 全为 0。`cc_scene_split.py` 在基准内部把 scene/no-scene 单变量
对照判掉（聚合 p90=0 时两组都只能在 0 附近，H1 几乎不可能过）。

**被否掉的是我的中间层假设，不是律**："场景短语 -> 空视野构图"错了 ——
SDXL 在 1024 上不管有没有 "on the grass" 都倾向把物体铺满画面。
"A photo of four cups on the grass" 出来的是四个杯子占满画面、草地只是
背景纹理，不是柯基图那种大片空场。**能造出高 evf 构图的是"广景深描述"**
（我们诊断集那种 "vast forested valley, distant snow mountains"），
不是一个介词短语。

**律本身反而更清晰了：触发变量从来就是基图构图（evf），它不能从
caption 预测，只能在基图上量 —— 而这正是门的定义。** caption 只是
构图的弱代理，现在被数据剔掉了。

**对论文主张的修正（变宽了，不是变窄）**：原来那句"展示语料与评测语料
错开"升级为：

> **物体重复的触发构图（evf 高的广景 + 小主体）在现行全部第三方评测
> 语料里都占 <3% —— LAION caption、GenEval 模板、连专为场景多样性
> 设计的 CoCoCount 也一样。而它恰恰是 4K/8K 生成的现实用例
> （壁纸、海报、风景）和每篇论文展示图的构图。**

**"换承载面"的理由随预注册一起被没收**（caption_struct 的判据写死了
不成立就不得再用）。取而代之的正路：**触发评测集 = 自然语料的 evf 尾部**
—— LAION eval split 2864 条里 evf>0.5 的约 2–3%（~70–80 条），第三方
来源、按预注册的基图可算判据选出、对两臂对称。配对设计（同基图两臂）
在 n≈80 上功效足够。诊断集降为机制展示，不进主表。

**遗留疑点（要在触发子集 4096 跑里回答）**：LAION C 层（evf 0.25–0.5 为主）
delta 只有 +0.27 —— 触发可能要 evf ≥ ~0.6 才真正发力（诊断集 +5.40 的
那批 evf 在 0.75–0.94）。分层要保留 0.5–0.75 与 >0.75 两档。
另外 R=4 -> R=8 时三个语料的 evf 均值都約 3 倍（0.019->0.065 等），
方向与几何式一致，但绝对值仍小 —— 8K 论点仍不足以单独成立。

### 5.49d showcase × 3 seed：触发是"样本"的性质，不是"prompt"的性质（2026-08-12）

同 4 条展示 prompt、只换 seed，R=4 的 evf 均值：

| seed 77 | seed 78 | seed 79 |
|---|---|---|
| 0.021 | **0.172**（单张最高 0.44） | 0.000 |

**seed 方差支配一切。** 同一条 "A cute corgi on the lawn" 式 prompt，
构图从铺满（evf 0）到留出近半空场（0.44）随 seed 任意摆动。这解释了
展示图的来历：**作者采样多个 seed，广构图的那次会重复，那张进了论文**；
表格对 seed 和 prompt 求平均，把它稀释到指标噪声之下。

三个直接推论：

1. **"触发语料"这个概念本身不成立** —— 任何 prompt 级的语料都被 seed
   方差稀释。触发是 (prompt, seed) **样本**的性质，即基图的性质。
   两个月前写下的原则（"按律的自变量挑 = 按基图构图挑"）绕了一整圈
   语料弯路后被证明是**唯一**的路，没有捷径。
2. **评测协议的最终形态**：在 prompt×seed 池上生成基图 -> 按 evf 选
   触发样本 -> 分层报告。选择判据基图可算、预注册、对两臂对称
   （基图逐字节同）。语料只是"池子"，Parti 的价值 = 1632 个构图多样的
   样本来源，不再背负"该语料触发"的假设。
3. **图-表错位有了机制版本**：重复是构图条件下的逐样本失效；
   语料级甚至 prompt 级的聚合都把它稀释到看不见 —— 这就是三年来
   表格与红框图各说各话的原因。**分层（按构图）报告是唯一能看见它的
   聚合方式。**

悬而未决（Parti 尾部 4096 跑回答）：自然高 evf 样本会不会像诊断集那样
重复（+5.40），还是像 LAION 排版图那样不重复（dc≈0）—— 后者意味着
律还缺"空处是场景"这第三因子。

### 5.49b 细档重切：delta_content 全平，但 delta_raw 随 evf 单调升 5 倍（2026-08-12）

| evf 档 | n | delta_content | delta_raw |
|---|---|---|---|
| = 0 | 30 | 0.00 | 0.60 |
| (0, .25] | 30 | −0.23 | 1.67 |
| (.25, .5] | 18 | +0.44 | 1.78 |
| (.5, .75] | 5 | −0.40 | **3.00** |
| (.75, 1] | 7 | +0.29 | **3.29** |

两个事实同时成立，**指向相反**：

1. **delta_content 各档全平** —— 预注册判读走"0.5+ 也平"分支：
   先看图，别急着烧 eval 尾部的 4096。
2. **delta_raw 单调上升，档间差 5 倍**（0.60 -> 3.29）。名义上这一列减
   delta_content 是"漂移"，但漂移是检测器性质，凭什么随构图涨 5 倍？
   两种解释：
   (a) **仪器**：空处越多，4096 原生分辨率下检测器在纹理上的小假框越多
       -> delta_content 站得住，LAION 尾部真的不重复；
   (b) **信号被自己的刀削掉**：空处长出**小尺度克隆**（柯基那种 1/8 尺度），
       降采样回 1024 掉出检测线 -> delta_raw 抓到、delta_content 按设计
       抓不到 -> **LAION 尾部其实在重复，只是小**。
   `raw_extra_vis.py` 把 4096 原生框与基图框（×4）配对、未配对的标红，
   红框框住"主体的小副本"= (b)，框住纹理杂物 = (a)。**看图裁决，不猜。**

3. **尾部 caption 的构图类型也变了**：LAION evf 高的 8 条是博客题图、
   商品图、甜品特写 —— 空处是**素背景/浅景深虚化**，不是诊断集那种
   "可以画东西的场景"（山谷、草地）。若 (a) 成立，这就是尾部不燃的
   机制解释：**空视野要重复，还需要空处是"场景"而非"素底"** ——
   律可能需要第三个因子（背景的场景性）。先看图再动律。

### 5.49a scene 对照判决 + 一个不该忽略的不对称（2026-08-12）

`cc_scene_split`：H1 正式**不成立**（0.020 vs 0.010，差 0.01 < 0.05）。
方向上 scene 组是 no-scene 的 2 倍，但两组都趴在 0 上 —— 场景短语在
1024 构图上的效应至多是"从 1% 挪到 2%"这个量级，撑不起任何论证。
R=8 下同样方向、同样小（0.065 vs 0.037）。

**但有个数不该滑过去：nbox=0 在两组间差 3.5 倍（scene 35/99 vs
no-scene 10/101）。** 场景短语确实改变了**什么** —— 只是那批图落进了
检测盲区，我们的 evf 对它们无定义。两种解释都开放：
(a) 草地/马路背景让 GroundingDINO 对整句 caption 的 grounding 更易失败
（仪器相关性）；(b) 构图真的变了（物体更小/更散）导致检不出（信号被
仪器吃掉）。**分不开就记录，不选边。** 若 (b) 为真，scene 组的高 evf
样本恰好全藏在无定义堆里 —— 这条要在看 eval 尾部基图时顺带核
（盲区图人眼扫一遍就知道构图散不散）。

### 5.49e 触发集 34 对 4096² 接触表眼睛终审（2026-08-13）

先修了一个数据事故：预注册文件的 34 个 key 里 **11 个是从接触表小字
标题误读的 idx**（167->157 等），以 manifest 真 idx 按 prompt<->desc
一一重配，标签与预测一字未改（修正记录写在预注册文件内部）。

9 个可疑格逐一放大到 1024 后的终审：

| 判定 | 样本 |
|---|---|
| **实锤重复** | **263 女神+方尖碑：4096 在基座空区长出一整尊第二座自由女神（基图没有）**；583 网球场：网眼纹理克隆进空挡风布 + 多出一颗网球 |
| 疑似 +1 | 157（+1 人）、146（+1 杯）、553（+1 小象，flat 标签！）、994（+1 画框，弱） |
| 虚惊 | 838（小 STOP 牌基图就有）、1019（红蓝车配置其实一致）、331（直升机 4 vs 4） |

其余 25 对缩略图尺度下主体清单一致，全局构图被结构引导钉死。

**读数（介于预注册两档之间）**：4096 上基线的残留重复**存在、
几乎只落在高 evf 样本上（方向对），但量级是 +1 而非成片**（scenic
组均值大概率 < +1.5 门槛）。这个中间态叙事上反而最好用：
1. 病灶真实且可展示 —— 263、583 就是论文图；263 同时是 **v1 首测的
   指定靶子**（门开 -> 多余女神消失、其余逐字节不变 = 方法部分第一图）；
2. 解释了 LAION 平表：病灶是稀疏 +1 事件，均值型指标接不住，
   只有配对逐样本的 Δdelta 接得住 —— 度量设计的必要性由此获得
   独立证据；
3. 灾难性成片重复住在 seed 尾部与 DemoFusion/8K —— E4 主战场不变。

VLM delta 的任务因此更新：给 6 个逐样本判定盖章 + 出全组分布，
不再是单押组均值门槛。

**修正一（同日，用户指出后）**：上面的读数一度被我说成"ScaleDiff 上
没有大仗可打"—— **错**。同一份 ScaleDiff 代码在**构造极端构图**
（小主体+纯色真空，evf→0.9+）上照样成片重复。机制：结构引导的锚是
lowpass(基图)，纯色真空 lowpass 完是一块平色，等于没锚；语料尾部的
"空"处是天际线/海面这类有低频内容的场景，锚还在，所以只漏 +1。
**严重度是连续的，不是有无之别** —— 律的 R²×(1−覆盖) 就是这条曲线。

**修正二（同日，用户第二次指出，更要紧）**：修正一里我把诊断集的
+5.40 当成"motivation 的定量骨架"，还把诊断集极端样本列为 v1 主展品
—— **直接违反 §6.9**（那 30 条是设计脚手架，正文任何数字、任何图
都不许来自它；我们在它上面做过六轮以上选型，在做过决定的数据上
报数字是自证）。**作废。**

于是留下一个真问题：**律曲线的高段，必须在标准语料上重新取得。**
不许用我们自己写的 prompt 去拍成片重复的图。三条合法路径，
全部落在第三方语料上：

| 路径 | 怎么取得高段 | 附带价值 |
|---|---|---|
| **DemoFusion @4096，LAION/Parti prompt**（首选） | 无结构引导 -> 成片重复是该管线的公认常态（AccDiffusion 全文即证据） | **就是 E4 战场本身**：同一批图既给律的高段，又是与 AccDiffusion 的正面比 |
| ScaleDiff @2048 vs @4096，同 34 条触发集 | 律的 R 轴：R=2 -> R=4，预测严重度 ×4（0.10 -> 1.07） | **两档都是这条线的标准评测分辨率**（ScaleDiff/DemoFusion/AccDiffusion 的表都有这两列），有对手可比 |
| 标准 prompt 的 seed 分布 | 触发是 (prompt, seed) 样本的性质；报整条分布而非挑一张 | 把"图表错位"从传闻变成可复现的分布事实 |

**v1 展品三级（改正后，全部第三方语料）**：
① **DemoFusion @4096 标准 prompt 上的成片重复被门修掉**（主展品，
   与 AccDiffusion 同 regime 同数据集，正面可比）；
② **263/583**（Parti 触发集，ScaleDiff 自家管线上 +1 级细漏也修得掉
   —— 说明方法不只治重症）；
③ ScaleDiff @2048 与 @4096 的对照 —— 律的 R 轴，两档都在标准评测分辨率上。

**8192² 不复活（写死）**：三条独立理由 —— (a) §5.45 已判：8K 上
**没有对手**，这条线的表都止步 4096²，无参照的图审稿人不会采信；
(b) §7.1（表 1452 行）实测 **4090 上 8192² OOM**，未试 offload；
(c) 不需要 —— 律的高段由 DemoFusion@4096 给（有对手、同数据集），
R 轴由 2048/4096 给（都是标准列）。**我在 2026-08-13 又提过一次 8192，
是错的，此条即为封条。**

诊断集的位置维持 §6.9 原判：**只在附录的受控分析里露面，且必须同时
披露"方法选型是在这批数据上做的"；正文零数字、零图。**

### 5.49f 触发集 VLM-delta 对账：中间态坐实 + 眼睛×仪器分歧清单（2026-08-13）

仪器三关先记账：**空对照满分**（34 对重采样往返，mean|Δ|=0.00，
100% 全零 —— 配对模式下 VLM 无自噪声）；armdelta P2b 3/3（门压得住），
P2a 1/6（互锁故事出局，v2b 预注册只能写独立旋钮）。

34 对分组：**scenic +0.68（8/19 条 >=1）> flat +0.33 <= 0.5（flat 预测
正式通过）> mid +0.17**。scenic 未到 +1.5 的组均值门槛 ——
维持 §5.49e 的中间态读法：稀疏 +1 泄漏，集中在 scenic。

**逐样本层眼睛×VLM 分歧（裁决前不定案）**：
- VLM=0 而眼睛 +1：263（女神！）、157、146；
- VLM +3 而眼睛平：331（放大看过，雕像单尊 -> VLM 可疑）、
  1059（只看过 150px 缩略图 -> 眼睛可疑，盘山道克隆卡车是教科书形态）；
- VLM +2 眼睛没放大过：738、1527、1187；另有 1119 = −1（非只涨机器）。

背景事实：空对照全零 -> 这些非零全是**内容真变了**，争议只在
"副本还是细节误读"。旁证偏"泄漏为真"：若是清晰度幻觉应三组同涨，
实际涨幅跟着律走（scenic 最高 mid 最低）。
裁决协议：**仪器提名（可核查的具体主张）、人眼在有界集合上核账**
—— 13 对放大图逐条裁，三种出口（VLM 对->证据变强 / 眼睛对->delta
降级换对偏好 / 混合->逐样本真值表+人工核账率）。协议本身可进论文。

**裁决第一批（同日，jsonl 复盘 + 主体词规则）**：分歧全部追溯到
**主体词**，没有一条是数数本身错 —— ① 泛化词（263 'statue' 把方尖碑
数进基图 n_base=3，配对相消吞掉真 +1）；② 集体名词（157 'team' 数出
"2 支队伍"、1119 'family' 给出唯一的 −1）；③ 场景/抽象名词
（1445/536/342 无意义计数，按 CARD=None 老规则排除）。
修订规则（客观、全集统一、如实披露是事后修订）落地后重数 4 条：
**263 -> +1（女神盖章，基图 3->1）**、157 -> +1、1119 -> 0、
**331 专名追问下仍 1->4** —— "误伤天际线"解释被排除，要么真长出
3 尊副本（比 263 还重），要么直升机被认成女神，表 A 定。
修订后：**scenic +0.83（9/18 >=1）> flat +0.43 > mid +0.17** ——
三处修订全部让数字更贴律的方向，而依据是纯词性规则。
眼睛五枚章盖齐四枚半（146 茶杯 4->4 未决，表 B 裁）。
两个独立观察者在逐样本层收敛，且分歧全由客观规则消除 ——
仪器可信度的最强一档证据，protocol 一节照此写。

### 5.48 base_diag 定案：不是 tiling，是数值路径；内容等同（2026-08-12）

四方对比（idx 36，同 prompt 同 seed）：

| | md5 | vs base_run 像素差 |
|---|---|---|
| base_run 版 | `b634…` | — |
| plain 不分块 | `b634…` | **max=0（逐字节复现）** |
| plain 分块 | `b634…` | max=0 |
| **laion_hi（ScaleDiff 管线）** | `2942…` | **max=13 / mean=0.057** |

三个结论：
1. **我的头号嫌疑（VAE tiling）被证伪** —— tiling 开关对 1024² 逐字节无影响
   （这个尺寸下分块根本没起作用）。猜错了，测了才知道。
2. **差异来自 ScaleDiff 管线自身的数值路径**（fp32 解码顺序 / attention
   processor 包装），mean=0.057 → **同一张图**。560 张基图与 evf 照常可用；
   `laion_delta` 的自检已改为像素容差判定。
3. **"逐字节相同"这个说法今后只许用于同管线比较**（我们 arm vs 基线 arm，
   都在 Custom 管线里）—— 跨管线只能说"内容等同"。20/20 md5 复用校验
   同理：必须在 Custom 管线内做。

### 5.46 读了三篇原文之后：顶会要什么，我们的空位在哪（2026-08-11）

**PixelRush（CVPR）为什么能中 —— 它不是"又一个 training-free"。**
novelty 既不在 one-step（不稀奇）也不在 training-free（更不稀奇），
而在于**把已有范式推进到一个它会崩的 regime，诊断崩因，修好，换数量级收益**：

| 步骤 | 内容 |
|---|---|
| 洞察 | 精修只需合成高频，**跑完整反向过程是冗余的** -> partial inversion |
| 换 regime | 50 步 -> **few-step**，4K 从 5 分钟到 **20 秒**（10–35x） |
| **新 regime 引入的新失效** | ① 低步数下 patch blending 崩坏 -> seamless blending<br>② 过平滑 -> noise injection |
| 定位 | *"the first tuning-free framework for **practical** high-resolution T2I"* |

> **拥挤的是范式，不拥挤的是范式在新 regime 下的断裂处。**

**对我们不利的一条，记下来**：PixelRush 正文写
*"enabling generation at **8K and beyond** on a single GPU"*，
并称自己 *"inherently mitigates object repetition"*。
**§5.45 那个 8K 改题的前半已被占。** 但注意：这句"inherently mitigates"
**没有任何度量支撑** —— 这条线没有一篇有重复的定量指标。

**AccDiffusion 的贡献边界（原文自述）**：v1 两条 ——
*"identify the reason for repetitive generation **during patch-wise
denoising**"* + patch-content-aware prompts；dilated sampling with
interaction。v2 加 ControlNet 注入低分辨率结构治 local distortion。

**关键在那个限定语 `during patch-wise denoising`：他们的因果解释绑死在
patch 分解上。** 而 **ScaleDiff 没有 patch** —— 局部性来自注意力窗口限制
感受野，不是图像被切块。所以那句因果描述在 ScaleDiff 上**字面不成立**，
症状却照样出现（作者自承背景区有 repetitive artifacts）。

> **空位：同一症状、两种产生机制，而现有解释各自绑死在自己的机制上。**
> 我们的律（重复 ≈ R²×(1−主体覆盖率)）在 patch 家族（DemoFusion）与
> 注意力窗口家族（ScaleDiff）上都成立（§1.2.2）—— **它是机制无关的**，
> 而 AccDiffusion 的解释里根本没有 R 这个变量。

#### 5.46a 小狗图与代码细读之后的三处修正（2026-08-11，用户指出）

**一、"主体占比大 -> 不重复"是我的错误压缩，用户用 AccDiffusion 的柯基图
指了出来。** 那张图里 DemoFusion 的小柯基副本**全部落在草地上，没有一个
在狗身上** —— 重复发生在无主体视野里，律的原话。柯基占画面约 1/4~1/3，
但按 R=4 切块空块仍有 ~10/16，**evf ≈ 0.6，落在我们的 C 层触发区**。
主体"显眼"不等于"覆盖所有视野"。这张图是律的正例，不是反例。

**二、"2%"的正确读法收窄为：LAION-aesthetic caption 分布的性质，
不是现象的性质。** 柯基 prompt 是 "A cute corgi **on the lawn**" ——
主体+场景结构；LAION 中位 caption 是裸主体商品图。由此形状二磨得更锋利：

> **这条线的定性展示图全用"主体+场景"手写 prompt（触发区），
> 定量表格却全跑在 LAION caption 上（基本不触发）——
> 展示语料与评测语料是错开的，表格因此三年不显示他们声称修复的问题，
> 红框图成了唯一证据。这个错位本身就是评测缺口。**

顺带记一个没考虑过的维度：**非方形外推**（2048×4096）会让空场面积暴涨，
柯基竖幅图上重复比方形还密；我们的 evf 目前只按方形 R×R 算。

**三、代码细读（help_code/AccDiffusion/utils.py:759）——重叠比论文文字深，
第 4 条贡献必须降格。** 他们的 patch-content-aware prompt 实为：
1024 阶段末步（t==1）取存下的 cross-attention -> 每个 token 一张
`attn > mean` 的二值掩码（腐蚀+膨胀）-> 掩码盖住 patch ≥ c(=0.3) 则该词
保留在该 patch 的 prompt 里 -> 每 patch 一个词袋子集、独立 encode、
独立 patch 前向。**"用模型自己的 cross-attention 决定哪些词适用于哪里"
这个原则是他们的。** 我们机制的准确定位：

> **AccDiffusion 原则的 patch-free 实例化** —— 把"文本条件空间化"从
> "N 个 patch 前向 × N 次编码"压缩到"1 个全图前向 × 2 套嵌入"，
> 决策从 patch 级二值变为位置级连续。ScaleDiff 家族结构上装不下他们的
> 做法（`md_prompt_embeds_list` 逐 patch 喂 `encoder_hidden_states`，
> ScaleDiff 没有 patch 循环可喂）—— 这是结构贡献，
> **不能再当独立机制发明来写**。

他们的词级做法自带的粗糙处（可对比但别嘲讽）：子词 token 单独决策、
mean 阈值、词序打散成词袋 —— 我们的两套完整句嵌入不破坏句法。

**四、范围：v2 = 重复 + 局部畸变（ControlNet 注入低分辨率结构，
scale=0.5）+ 全局语义（带双射交互的 dilated sampling）。**
它的题目是"更准的外推"，重复只是一根柱子。我们 §5.6 只承诺局部模糊
不恶化 —— 这个范围差距要在论文里明说，不能装作同题。

#### 三个候选论文形状（**现在还没有资格选，见末尾**）

**形状一 换 regime（学 PixelRush）。** 把干预放进 few-step：PixelRush 是
当前最快的一极，但对重复只有一句 "inherently mitigates"、**零度量**。
若能证明它其实没修好、而我们用 +3.9% 修好，就是在最新 SOTA 上打。
前提：它的开源码（正文给了 GitHub）能跑起来。

**形状二 基准批判 + 度量 + 方法。** 三条证据链：
(1) AccDiffusion v1/v2 **自己写**标准指标反映不出重复（§3.1a 原文）；
(2) 实测 LAION-aesthetic 只有约 **2%** 的 prompt 进入失效区；
(3) 故这条线三年来关于重复的改进**全是定性的、不可验证的**。
交付：无需真值的度量 + 分层评测协议 + 在其上取胜的方法。
**这条不依赖在 FID 上赢 ScaleDiff** —— 而那件事大概率做不到。

**形状三 机制无关的统一解释。** 律在三个机制家族上成立，据此给出
任何管线都能挂的预测器与干预。最"理论"，也最依赖数字漂亮。

**倾向：二为主、三为辅、一作最强加分。但这个倾向现在不作数** ——
形状二的第 (2) 条目前**只有代理量（evf）支持**，而 `laion_hi_run` 正在测的
就是它。**若 delta 显示重复其实普遍，第 (2) 条立刻反转，整套论证要重写。**
**在那之前不锁方向。**

### 5.5 门的已知失效：触发变量依赖基图检测（2026-08-10）

`gate_eval` 此前整行跳过无基数的类，于是**门在 crowd/texture 上的行为
从没被看见过** —— 而那正是门要保护的情形。补打之后：

| | 空视野 | 框数 | 门 |
|---|---|---|---|
| `09_crowd` s77 | 81% | 2 | **开** |
| `09_crowd` s1234 | 25% | 1 | 关 |
| `09_crowd` s2025 | 0% | 2 | 关 |

**同一条 prompt 三个 seed 给出 81% / 25% / 0%。** 不是场景差异，是测量不稳：
基图 1024² 上 GroundingDINO 只找到 1–2 辆车，车找不到 -> 场景看起来空
-> 门开。而 81% 对 2 框、25% 对 1 框，说明主导量是**框覆盖的 tile 面积**
（一个大框盖十几个 tile，两个小框只盖三四个），不是框数。

> **对"很多小物体"的场景，空视野比例结构性失效。** 必须写进 limitation。

保住大部分的是 `nbox >= 1` 那条：企鹅群、花海、羊群（0 框 + 100% 空视野）
全部关门。这条设计成立。

**改法（原则先行，不在这 30 条上调）：门要的是召回（这里有没有主体内容），
指标要的是精度（有几个）—— 两个用途该用不同的检测工作点。** 门的基图检测
降阈值取高召回，阈值由 LAION 那批验证，不用这 30 条。

### 5.6 局部模糊：同一结构性问题的第二个实例

`09_crowd` 的 1:1 裁块里有一条明显发糊的斜带，**两个 arm 都有**。

- 它是这条谱系公认的第二个症状（ScaleDiff 自己的消融：去掉 LFM ->
  *"heavily oversmoothed textures"*）；
- **但我们"细节没塌"的证据是整幅图的高频能量（−1.1%），一块糊掉的区域
  会被周围锐利区域稀释 —— 我们犯了自己批评领域指标的同一个错**
  （FID 平均到 299² 看不见多一个人；全局高频平均 16M 像素看不见糊一片）。
  `sharpness_map.py` 逐 tile 测，判据：我们低于基线 10% 以上的 tile 占比
  < 5% -> 不是我们引入的。
- 本篇只承担两件事：**无害性检查** + limitation 里承认它存在。
  **不转向做去模糊** —— 那是红海（LFM / HiWave / Pixelsmith Slider /
  Diffusion-4K 的 GLCM Score 都在打它）。

> **future work 两条腿（下一篇）：**
> ① *局部失效需要局部指标* —— 重复与局部模糊是同一句话的两个实例；
> ② **空间自适应 τ** —— ScaleDiff 的 Table 4 把 τ 当一个全局标量消融，
> 明说它在"保结构（低 τ）/ 生细节（高 τ）"之间权衡。但需要的 τ 逐区域
> 不同：已清楚的区域该低 τ，糊掉的区域该高 τ。**表里全是全局 τ，没人做过。**
> 它与本篇同构（都是把治疗局部化：门决定*在哪里*抑制重复，局部 τ 决定
> *在哪里*重画细节），且是律的对偶（律说"受限视野没有主体 -> 重复"，
> 这条说"受限视野已经清楚 -> 不该再加噪"）。
> **前提**：`sharpness_map` 的问题② 必须答"糊是从基图继承的"，
> 否则机制不同，这个想法要重想。

### 6.0.5 尺寸分层（`size_sweep.py`，2026-08-10，预注册判据没过 -> 必须进正文）

**起因与检验对象，别搞混（这里曾经被我自己讲歪过一次）：**

- **起因**是在 `09_crowd` 的标注图上看见框质量很差（半个车、一框两车、
  漏检）。但**汽车不是被检验的对象** —— crowd 类 `card=None`，
  不进任何指标。那张图只是**检测器小物体弱点被看见的场合**。
- **弱点会转移，靠的是尺寸而不是场景**：那些汽车是小物体，
  而**主结果里要数的幻影同样是小物体**（4096² 上 20–60 px，同一量级）。
  同一台检测器、同一个尺寸区间 -> 汽车上暴露的弱点可能同样存在于
  我们数幻影的地方。这才是要验的。
- **所以 `size_sweep` 只跑 lone 那 15 行**（输出首行即 `15 行（['lone']）`），
  检验的问题是：**"5.20 -> 1.67 这个主结果，是不是检测器在数一堆
  它自己都看不清的东西？"**

预注册判据（8–64 base px 全区间降幅跨度 <10 个百分点）**没过**（实测 50.9），
所以主结果按尺寸分层报告，不许只报一个数。分解：

| 门槛(base px) | 基线 MAE | 我们 | 降幅 |
|---|---|---|---|
| 8 | 5.20 | 1.67 | 67.9% |
| 12 | 3.27 | 0.93 | 71.4% |
| 16 | 1.93 | 0.47 | 75.9% |
| 24 | 1.33 | 0.40 | 70.0% |
| 32 | 0.67 | 0.33 | 50.0% |
| 48 | 0.53 | 0.40 | 25.0% |

- **8–24 px 区间降幅稳定（68–76%，跨度 8 个百分点）**。剔掉最可疑的
  8–12 px 边缘小框，降幅反而升到 71–76% —— "结论靠勉强可检的小框"不成立；
- **≥32 px 的衰减是地板效应**：基线残余只剩 0.67（= 现象的 13%），
  48 px 档是 8 个物体对 6 个的差，无统计意义；
- **发现：重复物体天生是小的** —— 现象的 87% 发生在 32 base px 以下
  （4096² 上 <128 px），以远景/背景物体形态出现。这直接喂给 §1.3：
  一个 <128 px 的幻影缩到 299² 只剩 9 px，**"现象是小物体主导"与
  "标准指标失明"是同一件事的两面**。

> **一句话结论（正文可直接用）**：在 lone 场景里，只数那些足够大、
> 检测器看得清清楚楚的幻影，改善依然是 **68%–76%** —— 主结果不是
> 检测器噪声堆出来的。

### 6.0.6 `dense_check.py` 的正确读法（脚本里我自己的读法指南写反了方向）

crowd 组 v1 的 delta 差 −2.07 **不是抑制作用的补充证据** —— 它主要来自
`08_sheep` 两行（+18→0、+14→+4）：**羊群被 v1 删了**，prompt 要的就是
一群羊，"更少"在 crowd 上是损害不是改善（§1.2.1 早已记录的已知问题）。

**门正是为此存在，且已验证会关掉它**：gate_eval 里 08 三个 seed 全关门；
crowd+texture 30 行只有 5/1234、9/77 两行门开。用门重算 v1.1：
**crowd 差 = −0.33 ≈ 0** —— 门把 v1 在密集场景的损害关没了。
这是"门是律的推论"的又一实证，写进 §3.15。
基线自己的 crowd delta +7.53（09/77 从 2 辆到 52 辆）不做解读 ——
无真值，重复与"4096 合理解析出 1024 看不见的车"分不开。

### 6.1 计数器的已知边界（都要进敏感度表）

- 尺度匹配分块 `tile = width/4`：修掉 4 倍的表观尺度不对称；残余跨分辨率漂移 **2.0**
  （所以主指标改用 `excess = 4096 计数 − prompt 基数`，不跨分辨率）；
- `min_score=0.50` 是全套里唯一靠 seed 77 标定、无机制依据的参数
  （**但 0.50–0.80 是平台，不敏感**）；
- 退化框判据 = 尺寸 **加** 低分（实测退化框 0.34–0.42，真主体 0.89，两者不重叠）；
- **texture 类不可用**：GroundingDINO 对满屏纹理不给框；
- 主指标只覆盖 prompt 声明了基数的 **20/30** 条；
- **逐行计数不准，这一条要在正文明说，不能只放脚注。**
  实例 `03/1234` 我们这版一行里犯了两个方向相反的错：主船清晰可见
  （`figures/fig_03_lone_s1234.png`）却一个框都没有（漏检），
  同时两个小框里有一个不是船（误检）。测出 2，真值 1–2 ——
  **两个错恰好抵消。靠抵消得到的正确不是正确。**
  （附带结论：那条船没被方法弄坏，是检测器漏检。）

  > **我们主张的量是 45 行的聚合 MAE 与 A/B 差值，不是任何单行的计数**，
  > 而这两个量经过了独立检测器的交叉验证（§6.0）。

- **⚠️ 由此引出的真问题，尚未验证：漏检在两个 arm 之间是否对称？**
  若 GroundingDINO 更容易漏掉**我们这版**的主体，计数就被系统性压低，
  看起来更好 —— 那是尺子偏心，不是方法有效。`subject_track.py` 查它：
  基图（两 arm 逐字节相同）上的主体框 ×4 作锚点，看每个 arm 的 4096²
  里有没有框覆盖它。预注册判据：两 arm 命中率之差 ≤ 2/45 行 -> 无差别
  偏差；超出 -> **MAE 必须连同命中率一起报**。
  漏检行再用锚点区域的像素差（零假设 = 两边都命中的行）区分
  "检测器漏检"与"主体真被改动"。

- **empty 上的假阳性率（0.13–0.27）是下界，不是全场估计。**
  空场景纹理少，非空场景（树线、浪、岩壁）更容易骗到检测器。
  论文里按下界表述。

---

## 6.9 那 30 条不进论文（2026-08-10，界限写死）

**它们是设计脚手架，不是实验章节的一节。**

我们在这 30 条上做过的决定：v0/v1/v2 选型、τ=3/8、门的规则、
`min_score=0.50`、退化框规则、ρ 的排除规则……**六轮以上。**
在做过决定的数据上报数字不是评测，是自证。哪怕过程写得再透明，
"我们自己造了 30 条 prompt，然后在上面证明提升了 68%" 这句话站不住。

> **默认位置：不进论文。** 它的产出是**律、方法、门、以及被验证过的仪器**
> —— 那些是真实产出，但它们的**证据**必须在 LAION 上重新建立。

诊断集唯一可能露面的地方，是附录里一段明确标注的受控分析
（例如 portrait vs lone 的对照，说明失效不普适），且必须同时披露
"方法选型是在这批数据上做的"。**正文的任何数字都不许来自它。**

### 6.9.0 于是 LAION 那一跑要扛的东西（全部要在那里重建）

| 要重建的 | 怎么做 |
|---|---|
| **律本身**（空视野比例 vs excess 的相关） | 基线 arm + 基图，声明基数子集。**样本外，比 30 条上的强** |
| 触发区占比（§1.2.1a 的预注册问题） | 1000 条基图的空视野比例分布 |
| 计数改善 | 声明基数子集上的 MAE + CountGen 式正确率 |
| 标准表 | FID/KID/IS/FIDp/KIDp/ISp/CLIP，两个 arm，ScaleDiff §4.1 协议 |

#### 6.9.0a 标准表是护栏，不是展示台 —— 预测写在跑之前（2026-08-11）

问题（用户提的，问得很准）：未经挑选的 eval-1000 里本来就含触发样本，
那我们在标准表上会不会因此更好？

**直接回答：大概率不会，而且必须事前这么说，事后再讲就不算数。** 三层理由：

**一、稀释是两重的，而且不是"乘 p"那么简单。** 设触发率 p，非触发那 (1−p)
逐字节相同、对任何指标贡献严格为零。但 FID 不是逐图分数的平均，
所以不能线性外推。写开均值项，令 $d=\mu_{\text{base}}-\mu_{\text{real}}$、
$\delta=p(\mu_{B_1}-\mu_{B_0})$：

$$\|d+\delta\|^{2}=\|d\|^{2}+2\,d\cdot\delta+\|\delta\|^{2}$$

一阶项 $2p\,d\cdot(\mu_{B_1}-\mu_{B_0})$ 对 p **线性**，但系数是"修正方向"
与"基线相对真图的偏差方向"的内积。两者正交时效应退到 $p^{2}$，等于没有。
**先乘 p，再乘一个我们没有理由相信很大的对齐系数。**

**二、更根本的：标准指标在原理上就看不见重复。**

| 指标 | 预测 | 理由 |
|---|---|---|
| FID / KID | **噪声内不动** | 两边都缩到 299²，4096² 上的六个登山者缩完仍是"一张有登山者的合理照片" |
| IS | **噪声内不动** | 测类别分布的置信与多样性，与个数无关 |
| CLIP | **噪声内不动** | 对计数本就弱，且 LAION caption 极少声明数量，连"应该是一个"都没有 |
| **FIDp / KIDp / ISp** | **该主动瞄准** | 见下 |

**这里要修正我先前一句过宽的话。** 我一度写下"patch 指标是局部的、重复是
全局的，所以也看不见"。**只对了一半：** 全局 FID 缩到 299² 确实盲；但
patch 指标在**原生分辨率的裁块**上算，而 4096² 上被重画出来的复制体往往
**是畸形的**（多出的肢体、不连贯的结构），那种裁块与真实照片裁块的
Inception 特征是有差的。经验上也支持：patch 三列的竞争带跨度
38.89–48.97 = **26%**，是 FID 那 5% 的**五倍**，**没饱和，且在位者输两列**
（§5）。所以正确写法是**逐列给不同预测**，不是一句话盖全表。

方向上仍要提醒：前四列对我们**只可能不利**（动了图就可能偏离真图分布，
修好重复却不会被它奖励），**它们的角色是证明没搞坏别的**；
真正该去赢的是 patch 三列。

**三、于是论文靠这个结构成立**（把"看不见"变成证据链的一环）：

> 重复量（Δdelta）在触发子群显著下降 **而** 标准指标在噪声内不动
> -> **实证**标准指标对这一失效模式不敏感（不是断言）
> -> 这条线三年来的评测不完整
> -> 我们的指标补上，且不需标注、不需真值。

这比"FID 也好了 0.3"强 —— 后者审稿人会直接怀疑是噪声。

**配套的两条硬要求：**

1. **必须先量噪声地板。** 基线换一个 seed 再跑一遍 1000 条，报两次之间的
   FID/KID 波动。**没有这个数，"在噪声内不动"是空话，"好了 0.3"也无法判读。**
2. **触发子群上报 KID 不报 FID。** 子群 n ≈ p×1000 可能只有一两百，
   FID 在小 n 上偏差极大；KID 有无偏估计量，小样本才站得住。

**预注册的三种结局及其读法：**

| 标准表 | 重复量 | 读法 |
|---|---|---|
| 噪声内不动 | 显著下降 | **预期结局。**按上面的结构写，指标盲区本身是贡献 |
| 明显变好 | 显著下降 | **先怀疑不是我们的功劳** —— 查触发率是否异常高、查是否有别的路径改了非触发图（门泄漏） |
| 明显变差 | 显著下降 | 修复引入了别的伪影。**如实报，并给出代价换收益的取舍曲线**，不藏 |

**τ 如果在 LAION 上定、又在 LAION 上评，就是同一个错误再犯一次。**
所以取数时**先切不相交的 split**（`fetch_eval_prompts --tune 200`）：

- **tune split（200 条）** —— 门阈值 τ、门的检测工作点、一切还需要标定的
  东西，只许在这上面定；
- **eval split（1000 条）** —— 评测。**方法冻结前一次都不许回看。**

切分在取数时完成、写进 JSON，早于任何标定 —— 数据落地后再切会有
"看过才切"的嫌疑。

**LAION 上怎么算计数 —— 两个独立集合，不混用（2026-08-10 定稿）。**

先看实际数据长什么样。aesthetic 子集的 caption 是网页 alt 文本：

```
Download Burger 1.0.17 APK File for Android
Tremendous Richmond 6 Piece Bedroom Set In Dark Grey Oak Finish
2pcs/Set Kids Girls Clothes Baby Girl Floral Ruffled Tops
```

**和这条线定性图里的 prompt（"astronaut on mars"）完全两个世界** ——
定量表跑在这种文本上、定性图另用精选 prompt，是这条线的共同现状。
FID 可比性不受影响，但它**直接支持 §1.2.1a 那条预注册预测**：
这批 prompt 里"大场景 + 小主体"几乎不出现，FID 上我们大概率几乎不动。

第一版词法筛在这种文本上**实测 5 例错 4 例**：

| 抽出的 | 原文 | 错在哪 |
|---|---|---|
| `[10 mustang]` | 10 Great Mustang Movies to Watch | 是 10 部电影 |
| `[6 bedroom]` | Richmond 6 Piece Bedroom Set | 是 6 件套（量词） |
| `[1 world]` | Named One Of World's Most Liveable | "one of" 是部分格 |

**换判据而不是补规则：能数的只有检测器能检的东西。** 中心词必须落在
**COCO-80** 里 —— 非任意且有引用（GenEval 的 counting 用 Mask2Former(COCO)，
CountGen 的脚本用 YOLOv9e(COCO)），另加单复数一致 + 量词/部分格排除，
并补人称同义词（man/child/hiker/surfer… -> person）与
`a single / lone / solitary X`（**这正是我们的律针对的句式**）。

**但改进后的词法筛仍然不能用 —— 8 例错 6-7 例：**

| 抽出的 | 原文 | 错在哪 |
|---|---|---|
| `[2 car]` | Tonka Jeep - GR **2**-2431 - Model **Cars** | 型号里的数字 |
| `[2 bed]` | House Plan - **2 Beds** 2 Baths | 户型说明，图是房子外观 |
| `[7 person]` | Country house - **7 persons**, 1 bedroom | 可住 7 人，图里没人 |
| `[1 cup]` | Portion Control **1-Cup** Container | cup 是容量单位 |
| `[5 person]` | The Top **5 Toys** for **Girls** | 5 个玩具 |

> **根因不是正则不够好：LAION alt-text 里的数字绝大多数不描述画面。**
> 再加规则只是打地鼠。

**而这条线根本没人从 caption 挖计数 —— 我一路在造轮子：**

| | venue | 形式 |
|---|---|---|
| **GenEval** counting | NeurIPS'23 D&B | 静态 `evaluation_metadata.jsonl`，模板 "a photo of three dogs"，N∈{2,3,4}，COCO 类 |
| **CoCoCount** | CVPR'25 (CountGen) | `dataset/create_data_CoCoCount.py` 生成，"A photo of four donuts **on the road**"（带场景短语） |

计数写在 prompt 里，**不存在抽错的可能**。于是：

| | 数据 | 用途 | 可比性 |
|---|---|---|---|
| **随机集** 1000+200 | LAION aesthetic，无筛选 | FID/KID/IS/FIDp/KIDp/ISp/CLIP | 与 ScaleDiff §4.1 一致 |
| **计数集** | **GenEval + CoCoCount**（外部，可引用） | 计数 MAE + CountGen 正确率 | 与计数那条线一致 |

**两个基准同时用，本身就是律的又一次检验（事前预测）：**

- **CoCoCount 带场景短语** -> 主体可能只占画面一小部分 -> 空视野比例高
  -> **按律应当有重复，我们的方法应当有效**；
- **GenEval 是裸模板** -> 主体多半占满画面 -> 空视野比例低
  -> **按律本就不该有重复，门应当关闭、逐字节不动**。

两条都成立才说明律和门都对。GenEval 那批若也大幅变化，说明门没起作用，
要回头查阈值。两批的空视野比例可以在基图上直接量，不需要额外假设。

**crowd / texture 这两类连诊断集里都只是门的负对照**，
`card=None`，我们对拥挤场景没有定量结论 —— limitation 照此写，
不许写成"不影响结论"。

### 6.9.1 已放弃的分支：拖影/局部各向异性

`smear_check.py` 的三条预注册预测：P1（无主体 tile 高频增益更大，
0.0361 vs 0.0289）与 P2（各向异性更高，0.4533 vs 0.4337）过，
分层控制后仍在 4/5 档成立；**P3（我们的介入降低它）明确没过**
（0.0360 vs 0.0361，0.4533 vs 0.4533，差在第四位小数）。

按预注册，拖影退回 limitation 一句话。**且不再投入**，三条理由：

1. P3 失败本身是信息：我们改的是交叉注意力，拖影纹丝不动 ->
   **驱动力在 τ / Structure Guidance 那条轴上，与注意力正交**；
2. 各向异性分不开"拖影"与"内容本来就有方向"（马路、水波、田垄）——
   最后一档 0.3637 vs 0.4650 的大差距很可能是内容类型造成的，
   要分开必须再比基图各向异性（`grids()` 算了却丢掉了）；
3. 它测的是"凭空生成的高频"，**不是"重复的物体"**，与主张不是同一个量。

---

## 7. 证据现状（每次实验后更新这张表）

| 支撑哪一句 | 证据 | 状态 |
|---|---|---|
| §1.2 因子一 | 邻域/画布比 +0.20/+1.00/+5.40（DemoFusion +0.40/+2.20/+4.60） | ✅ 两种机制同向 |
| §1.2 因子二 | 空视野比例 vs \|excess\|，受限 ρ：ScaleDiff +0.845 / DemoFusion **+0.686** | ✅ 样本外确认 |
| §1.2 general | **另一种效率机制（DemoFusion，patch）上复现** | ✅ **缺口已补** |
| §3 有效 | lone MAE 5.20±0.92 → 1.67±0.31（−68%），n=3 seed | ✅ |
| §3 无害（语义） | CLIP +0.02% | ✅ |
| §3 无害（细节） | 高频 −1.1% | ✅ |
| §3 代价 | +3.9% 时间 / +1.7% 显存 | ✅ |
| A/B 可归因 | 基图逐字节 30/30 相同；empty 五条逐字节相同 | ✅ |
| §3.1① | 注意力质量 5.52%，v0 无效 | ✅ |
| §3.2 消融 | v2 更差（lone 2.00 → 3.00） | ✅ |
| §5 表 | FID/KID/IS/FIDp/KIDp/ISp × 1000 LAION | ❌ **零，卡数据下载** |
| §0 同一任务 | **FLUX** | ❌ 一次没跑 |
| §5 对照 | DemoFusion / AccDiffusion | ❌ |
| 完整度加分 | 8192²（ScaleDiff 的表止步 4096²） | ❌ OOM，未试 offload |
| §3 尾部 | 三个 seed 定位到 23/26/27 的 prompt 泄漏；**门全部关掉（P2 5/5）** | ✅ 闭合 |
| §3.15 门 | 平台 (5/16, 7/16]；τ=3/8 临时 | ⚠️ **待新数据验证** |

### 7.1 三个硬阻塞

1. **单 seed。** 而且 v0/v1/v2 三个变体**在同一批数据上评估后挑最好**
   —— 有拟合测试集的风险。补 seed 之后才算有验证集。
2. ~~**网络。**~~ ⚠️ **这条阻塞不存在，2026-08-10 实测撤销。**
   服务器上 **huggingface.co 直连可用**（真取到字节，不只是 HEAD 200），
   github.com 直连可用，清华 pip 源可用。**Mac 中转路线删除。**
   一个真 bug：`os.environ.setdefault("HF_HUB_OFFLINE","0")` 覆盖不掉 `env.sh`
   的 `=1`，`huggingface_hub` 根本没发请求就报 offline。已改硬覆盖。

   **端点：两个都间歇可用，不要选。** 三轮实测互相矛盾（mirror 超时/可用、
   直连可用/SSL EOF 各出现过），我据此先后写过"mirror 不通"和它的反面，
   两次都是拿一个样本下全称结论。现固化为 `hfnet.pick_endpoint()`：
   轮流重试两个端点，谁先成谁上。**长任务必须容忍中途掉线。**

   **评测资产已全部确认可取**（`eval_assets.py`）：
   caption 源 `laion/relaion2B-en-research-safe`（257 片）、
   `laion2B-en-aesthetic`、`laion-coco`；真图参考集 `sayakpaul/coco-30-val-2014`、
   `nlphuji/mscoco_2014_5k_test`。Inception 走 pytorch-fid 官方 release。
   `fetch_eval_prompts.py` 用 HTTP Range 只读 parquet 的一个 row group，
   不下整片就能取到 1000 条 caption。
3. **算力。** 复现整张 Table 2 不可能（DemoFusion 一行就是 279 GPU 小时）。
   可行做法：**自己只跑 ScaleDiff + 我们两行**（1000 条 × 2 arm × 2 分辨率
   ≈ 90 GPU 小时 ≈ 4 天），用复现出的 ScaleDiff 行校准管线，**其余 baseline
   引用他们发表的数字**。新那一列只在我们 20–30 条 prompt 上跑，不需要 1000 条。

   **算力可以砍掉约四成，而且是白得的（2026-08-11）**：我们那一行里，
   门关闭的 prompt 走的是与 ScaleDiff 逐字节相同的代码路径、同 seed，
   输出**必然逐字节相同**。所以我们那一行只需要重跑**触发子群**，
   其余直接复用基线的输出文件。若触发率约 20%，总量从 1000×2 = 2000 张
   降到 1000 + 200 = 1200 张，≈ 90 -> 55 GPU 小时。
   **前提要先验证，不能假定**：预注册 —— 随机取 20 条门关闭的 prompt
   真的过一遍我们的管线，与基线输出做 md5 比对；**20/20 相同才允许复用；
   只要有一条不同，说明确定性不成立，我们那一行必须 1000 条全跑。**
   （这条验证本身也是论文里"门关闭时零代价"这个主张的直接证据。）

   **eval-1000 的挑法必须现在定死**：从 clean.json 的 eval split 里
   **按原始下标顺序取前 1000**。原始顺序来自 LAION 分片的存储顺序，
   与我们的问题无关，因此这是一次与结论无关的截断。
   **不得按触发与否挑** —— FID 那张表必须落在未经挑选的样本上；
   触发分层只用于重复指标那张表（它不需要真图）。

### 7.1.5 评测用的 LAION 变体：选哪个、为什么、以及不可比在哪（2026-08-10）

**ScaleDiff §4.1 只写了 "LAION-5B [38]"** —— 没说子集（2B-en / aesthetic /
high-resolution？）、没说快照、没说过滤条件。而**原始 LAION-5B 仓库
2023-12 已下架**。所以"用和 ScaleDiff 一样的数据"在字面意义上做不到，
这不是我们的取舍，是数据本身没了。

**但"LAION-5B"是伞名，要查的是这条线实际用哪个子集。** 证据分三层：

| 来源 | 原文 | 强度 |
|---|---|---|
| **PixelRush**（同线，training-free 高分辨率） | *"1000 prompts randomly sampled from the **LAION/LAION2B aesthetic** dataset"*，且开篇称 *follow the experimental settings of prior methods* | ✅ 明确 |
| ScaleDiff §4.1 | *"1,000 image-text pairs from the LAION-5B dataset [38]"* | ⚠️ 伞名 |
| DemoFusion | **未能直接核实**（PDF 取不下来，检索无原句） | ❌ 待补 |

包含关系：`laion2B-en-aesthetic ⊂ laion2B-en ⊂ LAION-5B`。
**所以 ScaleDiff 写伞名、实际用 aesthetic 子集并不矛盾**，而这条线上唯一
明确写出子集的那篇写的是 aesthetic。

> **首选 `laion/laion2B-en-aesthetic`。** relaion 系列降为退路
> （`relaion-safe ⊂ relaion-research ⊂ 原始 LAION-5B`，`-safe` 多一层
> `p_unsafe > 0.45` 过滤），且都是 gated access，要填机构信息 + 审核。

（两次更正：第一版把 `-safe` 排首位是没查变体关系；第二版把 relaion 排首位
是只顺着"下架→重发"找替代品，**没去查这条线实际用哪个子集**。）

**aesthetic 过滤带来的一条附带影响，要写进 §1.2.1a**：美学子集偏"好看"的
构图，触发区占比在它上面测出来的数，是**美学过滤语料**的占比，不是一般
文生图使用场景的占比。这不影响与这条线的可比性（大家都用它），但
prevalence 那句话必须限定在这个语料上。

**写进论文的措辞**（现在定死，不许事后放宽）：

> We sample prompts from re-LAION-5B (English research subset), the
> official successor to LAION-5B, since the original release was
> withdrawn in December 2023. ScaleDiff specifies only "LAION-5B", so
> an exact reproduction of their sample is not possible; we therefore
> reproduce the ScaleDiff row ourselves and use its agreement with the
> published value to calibrate the pipeline.

判据不变（§7.2）：**复现的 ScaleDiff 行落在发表值附近 -> 管线通过校准，
其余 baseline 引用发表数字；落得远 -> 三行全自己跑，只报 A/B 相对变化。**

### 7.1.6 参考图的质量门槛：299，且不许为凑数量而降（2026-08-10）

FID 把两边都重采样到 **299×299**。对生成图（4096² -> 299²）是降采样，
无损；对真图，短边小于 299 就意味着**上采样**，那是插值出来的高频，
比原生 299px 的照片糊。Inception 对锐度/纹理敏感，混进上采样过的真图
会让**参考分布系统性偏糊** —— 这个偏差与我们的方法无关，纯粹是数据
处理引入的，却会同时改变基线行和我们行的绝对值。

> **原则：参考图短边 >= 299，不做上采样。**

**记录一次被否决的放松（否决是对的）：** 实测 200 条探路存活 47%，其中
41 条（20.5%）是"图下下来了但短边 200–250"。我一度想把阈值从 256 降到
128，理由是"反正缩到 299²，236px 和 300px 没区别"。**那句话对生成图成立、
对真图不成立，而且是朝着数据变多的方向找的说辞。** 正确的原则指向
**更严**（299），不是更松。

**数量不足的解法是多取候选，不是降标准。** 候选几乎免费：第一个
row group 就有 405,224 行（首轮只读了 196,608），一共 128 片。
只有在候选池本身耗尽时，才退到 §7.2 的备用口径。

配套：`fetch_eval_prompts` 用 LAION 元数据里的 `WIDTH`/`HEIGHT` 在**选
prompt 阶段**就按 299 筛掉，那些图根本不用去下 —— 于是下载失败里只剩
真正的死链（探路数据里约 27%）。

**全量实测（2026-08-11，4919 候选，17.4 分钟）：存活 3146 = 64.0%，
相对需要的 1200 张余量 2.6x。** 值得记下来的是提高标准反而少丢数据：

| | 探路轮（阈值 256，无预筛） | 全量轮（阈值 299，元数据预筛） |
|---|---|---|
| 存活率 | 47% | **64.0%** |
| `too_small` 占比 | 20.5% | **0.3%**（15/4919） |

标准从 256 抬到 299，尺寸损失却降到六十分之一 —— 因为**淘汰点从"下完再
量"挪到了"下之前就知道"**。剩下的失败已经是纯粹的链接腐烂：
HTTPError 20.2% / URLError 9.4% / OSError 2.5% / UnidentifiedImage 2.2% /
Timeout 1.0% / 其余 0.4%。

### 7.1.7 存活 ≠ 可用：不诚实的链接腐烂（2026-08-11）

64% 存活不等于 64% 可用。腐烂有两种形态：

- **诚实的**：404 / 超时 / 连接断 —— 已经落在失败那一栏，进不了参考集；
- **不诚实的**：HTTP 200 返回一张"图片已下架"的占位图或追踪像素 ——
  **落在 ok 那一栏。**

证据就在这轮的失败分布里：`too_small_1`、`too_small_10`、`too_small_80`
各有几条。这些 URL 的**元数据里写着正常尺寸**（否则 299 预筛早筛掉了），
下下来却是 1px、10px、80px。所以站点确实会在 200 里塞占位物；既然存在
"下下来是 1px 占位图"的，就必然存在"下下来是 600×400 占位图"的 ——
**那种尺寸合法，尺寸检查抓不到，直接混进参考集**。

`scalediff_probe/real_audit.py` 的判据（写在看结果之前）：

1. **重复**：逐字节 md5 相同、或 32×32 灰度指纹相同的组，
   size >= 2 只留一张（理由与占位图无关 —— 重复样本本身就会把参考分布
   往点质量上压，对 FID 是错的）；size >= 5 整组丢弃（同一张图出现在
   几千条不同 caption 下，只可能是占位图）。
2. **磁盘上重新量短边**，< 299 丢弃。
3. **近乎纯色**（灰度 std < 5）丢弃。

**不做别的过滤。** 每多一条前人没有的过滤，绝对 FID 就更不可比一分；
域名分布、长宽比只报告不进判据。另一条可用但**故意不用**的诊断是真图与
自身 caption 的 CLIP 分数（低分几乎必是占位图）—— 不用是因为它会把参考集
往"图文对齐好"的方向偏，而 CLIP 正是我们要报的指标之一。

**顺带修掉的一个后门**：`fetch_real_images` 原来命中 `p.exists()` 就直接
返回 `cached`，于是早期 256 阈值下下来的图（短边 256–298）绕过了尺寸检查
留在集合里 —— "阈值不能将就"要防的那件事从缓存分支溜了进来。现在缓存也
重新解码量尺寸，不合格就删掉重下。同理，候选表重取过（加了 299 预筛）
使下标整体位移，因此 mtime 明显早于本轮的文件其 idx 对应的已是另一条
caption；FID 比分布不比配对，所以不致命，但这些文件一并删掉重下。

**第二遍会自己长出来（2026-08-11）**：同一份候选表再跑一次，存活
**3146 (64.0%) -> 3421 (69.5%)**，多出 275 张。增量的来源可以逐项对上账：
URLError −126、OSError −67、HTTPError −35、TimeoutError −30、
UnidentifiedImage −8、RemoteDisconnected −8、IncompleteRead −1，
**合计恰好 275**。也就是说约 **5.6% 的候选是瞬时失败**，重跑即回来；
连 HTTPError 都掉了 35，说明其中有一部分本来就是 429/503 限流而非 404。
两点结论：(a) 存活率是"这一次网络状况"的函数，日志之间数字不同不是异常；
(b) 重试对图像内容无偏，因此可以放心多跑一遍，但 2.9x 余量已经够，不必再跑。

**体检结果（3146 张，4 秒）—— 占位图的担心没有兑现，但另外两件事查实了：**

| 检查项 | 结果 | 读法 |
|---|---|---|
| 解码失败 | 0 | 存活的都是真图片文件 |
| 指纹组 size >= 5 | **0 组** | **参考集里没有占位图**，担心证伪 |
| 重复 | 74 组 / 148 张，**全是 size=2** | LAION 常态：同一张图挂在两个域名下（`images.unsplash.com` 与某游艇站、`i.pinimg.com` 与 `i0.wp.com`），不是链接腐烂 |
| md5 组 vs 指纹组 | **74/148 完全一致** | 每一对都逐字节相同；32×32 指纹在 3146 张真实照片上**零误报**，仪器本身可信 |
| 近乎纯色 | 0 张 | 没有空白占位 |
| 长宽比 | p99=2.12，最极端 3.4:1 | 没有横幅广告，按预注册只报告不过滤 |

两件查实的事：

1. **短边 < 299 的有 11 张，最小 267 —— 全部落在 [267, 299) ⊂ [256, 299)，
   正是旧阈值 256 放行的那扇窗。** 机制由数值区间确认，不是由 mtime 确认：
   我写的 mtime 判据（"比最新文件早 1 小时以上"）报了 0，因为探路轮就在
   正式轮前十几分钟跑，总跨度不足 1 小时 —— **固定门限在这里是错的仪器**，
   已改成找 mtime 序列里最大的空档。真正抓到它的是短边分位表。
2. **那 94 个 `cached` 是上一轮候选表的遗留，有定量指纹**：探路轮
   `--limit 200` 的存活率是 47%，`0.47 × 200 = 94`，与 `cached=94` 严丝合缝；
   若探路轮跑的是加了 299 预筛的新表，存活率应是 64%，缓存数应约 128。
   所以这 94 张的 `idx` 对应的是**旧表的 caption**。FID 比分布不比配对，
   任何指标都不受影响，但代价只有一分钟：删掉 `idx < 200` 重下，歧义清零。

留下 3061 张（丢 85 = 2.7%：dup_keep_one 74 + short_side 11），
eval 2531（2.5x）/ tune 530（2.6x），两个 split 都够。
另外 p10 = 301：**约一成样本就压在 299 的边上**，说明这条线卡得很紧 ——
当初若真把阈值降到 128，混进来的绝不是"边缘几张"。

**复查（3426 张）：尺寸那条彻底关上。** `p0 = 299`、短边 < 299 **0 张** ——
缓存分支重新量尺寸的修复生效。`size >= 5` 的组依然 0 个（三轮都是 0）。

**重复率那条判断错了，这里改正。** 我一度写下"4.67% 对 4.70%，样本量
变了比例不动，所以是 LAION 的重复率"。清掉那 83 张旧表文件之后：

| | 集合 | 重复组 / 张 | 占比 |
|---|---|---|---|
| 第一轮 | 3146 | 74 / 148 | 4.70% |
| 第二轮 | 3426 | 80 / 160 | 4.67% |
| **清掉旧表文件后** | 3426 | **2 / 4** | **0.12%** |

**那 80 组里有 78 组是旧表文件自己造出来的。** 机制：旧表与新表取自同一批
LAION 行，同一张图在两代表里各占一个位置，于是"旧下标下的那张"与
"新下标下的同一张"逐字节相同，凑成一对。数目也对得上 —— 旧表遗留 83 张，
其中 80 张在当代集合里找得到孪生（第一轮是 94 张里找到 74 对，集合更小、
孪生还没下全，所以对数更少）。

连带订正一处读图错误：我当时说那些组是"同一张图挂在两个域名下的网络转载"，
证据是打印出的 `images.unsplash.com` 与某游艇站。**那两个域名里有一个是假的**
—— 审计脚本按 `idx` 去当前表里查 URL，而旧表文件的 `idx` 指向的是另一行，
查出来的域名根本不是它的来源。所以那张表里的域名列，对旧表文件而言全是噪声。

**我犯的是同一类错误的第二次**：拿"两次测量数值接近"当稳定性证据，而没问
两次是不是共享同一个污染源。这里两次共享的正是那批旧表文件；比例看着不动，
是"孪生找到得更多"和"分母变大"两个反向效应恰好抵消。

订正后的事实：真正的 LAION 重复只有 2 对（`price4limo.com` 的租车图、
`m.atcdn.co.uk` 的车辆图，**都是同站两条不同 caption 指向同一张图**），
**0.12%**。这对我们是好消息 —— 去重规则实际上近乎空操作，
参考集与前人协议的偏离又小一分。

留 3424：eval 2864（2.9x）/ tune 560（2.8x）。`table.json` 指纹一致，
mtime 空档虽然仍报出 2930 张（两趟之间的停顿），已被指纹判据正确压掉 ——
降级那一步的设计意图当场兑现。

**一次砍错位置的记录（值得留，因为错在假设而非手误）：** 我让删 `idx 0–199`
去清那批旧表缓存，删掉的却全是当代文件。原因是 `fetch_real_images` 里
`--limit` 作用在 `--split` **之后**，而 split 的切法是"前 k 条为 tune、
其余为 eval"；探路轮跑的是 `--split eval --limit 200`，它的文件因此落在
**eval 段的开头（idx ≈ 819–1018）**，不在 0–199。对账也印证了：探路轮
留下 94 张，其中 11 张短边不足 299 已被缓存校验删掉重下，**94 − 11 = 83**,
正是复查里报出的那 83 张。

**据此加的防线（治机制，不治症状）**：`fetch_real_images` 在输出目录里写
`table.json`，记候选表文件的 sha256；下次运行发现指纹不符就**直接拒绝**，
要么 `rm -rf` 整个目录重下，要么显式 `--allow-mixed` 并在论文里交代。
同时把 `real_audit` 的 mtime 空档判据降级为提示 —— **它分不清"换了候选表"
和"同一张表跑了两趟、中间隔了二十分钟"，两者在 mtime 上长得一模一样**；
权威判据是指纹，不是时间。

**数据来源句（论文里必须这么写）**：我们的 image–text pair 取自
`laion2B-en-aesthetic`，并限制为**截至 2026-08 仍可解析、且短边 ≥ 299 的
URL**。这是一次基于宿主站点存活性的选择，不是基于图像内容的选择；它对
两个 arm 完全对称（同一份参考集），但确实使绝对 FID 与发表值不可比 ——
这也正是基线行必须由我们自己复现的原因（§7.2）。

### 7.2 评测数据到底卡在哪（先测再说）

`scalediff_probe/net_probe.sh` 把第三条腿要的每一样东西单独试一遍：
Inception（FID/KID/IS 的骨干）、CLIP 塔、LAION caption 分片、pip 源。
判读规则：

- Inception 有两个来源（github release / HF 镜像），任一通就够；
- CLIP 塔在 HF 上，和已经拉成功的 SDXL 走同一条通道；
- caption 只要 parquet 的一个分片，不是全量数据集；
- **真图参考集是唯一可能真需要中转的一项**，但也有两条不用中转的出路：
  HF 上打包好的图片集（COCO val 等，同一条通道），或者 FIDp/KIDp
  本来就只比"我们两个 arm 自己生成的图"，不需要外部真图。

**只有这些都不成立时才谈 Mac。** 在此之前不再把中转写进计划。

---

## 8.9 仪器换刀：计数器从检测器换成 VLM（2026-08-13，用户否决后定案）

**用户否决了检测器方案，否决是对的**：触发集接触表上 GroundingDINO
肉眼可见地失败（剪影/水墨/插画/商品图 —— 风格化内容的分布外失效）。
headline metric 底下压着一眼不准的仪器，校准数字救不回第一印象 ——
顶会审的第一道是观感可信。

**但量化不能丢**：弃度量 = 回到贴图比柯基，AccDiffusion 2024 年就做完了。
"超过 AccDiffusion"只能靠他们没有的量化成立。

**换刀方案**：计数器 GroundingDINO -> **Qwen2.5-VL-7B**（单卡 4090 装得下、
本地、零标注，VLM 裁判是 2026 通行做法，风格化图像是 VLM 强项）。
**Δdelta 的设计一个字不改**（配对/同尺度/基图参照/真值检定），
换的只是"数数"这一步；GroundingDINO 降为附录交叉验证副尺。
门（方法本体）不受影响 —— 门控图来自 cross-attention，本就不用检测器。

**三级降落伞（全部预注册在 vlm_count.py）**：
1. `--probe`：合成 3 圆图数对 + 贪心解码三问同答 + 主体抽取自检；
2. `--calibrate`：GenEval/CoCoCount 真值检定，**MAE <= 1.0 且 |bias| < 0.5
   才上岗**；
3. **判死**：VLM 也检定不过 -> 弃计数，改成对偏好判决
   （VLM 只答"哪张图有主体被重复"，不数数）。

主体词来源：基准有官方 subject 字段；Parti/showcase prompt 规整，
VLM 自抽一次落盘复用；LAION alt-text 太脏，不走 VLM 计数。

### 8.9a 检定结果落地（2026-08-13）：检测器**正式**没过自己的预注册考试

count_calibrate 全量跑完（GenEval 80 张 + CoCoCount 200 张，ms=0.50）：

| 基准 | grounding | MAE | bias | 预注册门槛 |
|---|---|---|---|---|
| GenEval | caption | 1.90 | −1.90 | MAE≤1.0 且 \|bias\|<0.5 —— **没过** |
| GenEval | subject | 1.27 | −1.10 | 没过 |
| CoCoCount | caption | 4.28 | −4.26 | **触发"两种都差（MAE>2）"判死支** |
| CoCoCount | subject | 3.23 | −3.00 | 同上 |

**分层揭示机制 —— 不是噪声，是饱和**：subject 模式按声明数分层，
bias ≈ −(card−2) 几乎精确成立（card=2 时 MAE 0.58–0.76；card=3 约 1.0–1.3；
card=4 约 1.9–2.1；card=5 −3.27；card=7 −4.85；card=10 −7.55）。
GroundingDINO 在这套图上数到 **2–3 个就封顶**，超出部分系统性漏计。
NMS/二次筛把重叠同类实例合并吃掉 —— 这正是"数重复出来的副本"
最需要的能力。**这台仪器在原理上就不适合数重复。**

三个落地结论：
1. **预注册的"计数器不合格，表 B 换仪器再跑"支正式触发** ——
   §8.9 的换刀从"用户观感否决"升级为"真值检定否决"，用户的第一眼
   判断被数字完整追认。这段写进论文附录反而是加分项：
   "我们检定并淘汰了检测器计数"是审稿人想看的严谨性。
2. **GroundingDINO 连附录交叉验证副尺都当不了**（计数任务上）。
   它保留的唯一岗位：evf 选样（选样已被人工标签接管，只剩历史作用）
   与"有/无主体"的粗判。已算的 LAION delta 表结论（各层皆平）
   方向上仍成立 —— 漏计只会低估 delta，而结论本来就是"平"；
   但数值不再引用，若 LAION 平表要进论文需 VLM 重数。
3. **subject > caption 的次级预测应验**（1.27 vs 1.90 / 3.23 vs 4.28）：
   全句 grounding 确实伤，但不是主因 —— 主因是饱和。

VLM 的考试（同两个基准、同门槛）成为唯一在场仪器的上岗考。

### 8.9b VLM 检定结果 + **考卷作废**：真值不是真值（2026-08-13）

`--probe` 满分：3 圆图三次全答 3，主体抽取 "a small house on a mountain
top" -> "house"。贪心解码可复现性成立。

`--calibrate`（GenEval 80 + CoCoCount 200）：

| 基准 | MAE | bias | 严格命中 | ±1 内 |
|---|---|---|---|---|
| GenEval | 1.32 | **+0.85** | 33.8% | 70.0% |
| CoCoCount | 1.95 | **+1.26** | 31.5% | 67.5% |

CoCoCount 分层：card=2/3/4/5 -> MAE 0.62/1.00/0.94/0.88，
bias +0.62/+0.35/+0.45/+0.45；card=7 -> 2.58/+1.42；card=10 -> 5.76/+4.30。

**按字面：没过门槛（MAE≤1.0 且 |bias|<0.5），该判死。但先看两件事。**

**① bias 换了号。** 检测器 bias ≈ −(card−2)（饱和、少数），VLM bias
**为正**（多数）。同一批 card=10 的图，检测器读 2.8、VLM 读 14.3 ——
**同一批图上两台仪器差 5 倍**，至少一台错得离谱。接触表的眼睛证据
已经说明检测器把重叠同类实例并掉了，所以分歧的责任在检测器一侧。

**② 真值根本不是真值 —— 这是我设计考卷时的硬伤。**
`card` 是**prompt 里声明的数**，不是**生成图里实际有的数**。
而 GenEval 这个基准存在的理由恰恰是"T2I 模型数不对"：
SDXL 在 GenEval counting 子任务上的公开准确率只有 0.4 上下。
于是我们量到的"误差" = **计数器误差 + 生成器没听懂数量指令**，
两者不可分。VLM 严格命中 33.8%，与"一台完美计数器在
只有约 40% 图数量正确的样本上作答"完全一致。
card=10 让 SDXL 画一堆领带，画出 14 条是它的常态，VLM 读 14.3
可能**是对的**，被判成 +4.30 的"偏差"。

> **裁定：这张考卷作废，不是 VLM 判死。** 作废理由是设计缺陷
> （测不出目标量），不是结果不合意 —— 两者的区别必须写进附录：
> 失败与作废都如实记录。检测器的判死**不受影响**：它的死因是
> 仪器侧的饱和（读不过 3），有眼睛与跨仪器分歧双重佐证。

**重考设计（预注册，跑之前写死）**：

1. **真值改由作者逐图核对**（分层 60 张：CoCoCount card=2/3/4/5/7/10
   各 10 张），得到 `n_actual`；同时产出一个附带数字：
   SDXL 的数量服从率（= n_actual == card 的比例），它本身就是
   "为什么不能拿声明数当真值"的证据。**这是作者自查，不是标注员，
   不违反零标注约束**，且必须在论文里写明"由作者核对"。
2. **门槛不变**：VLM 对 `n_actual` 的 MAE ≤ 1.0 且 |bias| < 0.5 -> 上岗。
3. **加一场对 Δdelta 更贴切的考试（零标注、零人眼）**：
   **空对照** —— 同一张基图与它的"重采样往返版"（1024 -> 4096 双三次
   -> 1024）配对计数，内容逐像素同源、物体数必然相同，
   **Δ 必须为 0**。这直接量的是 delta 在实际配置下的噪声地板，
   比单图绝对精度更贴近我们的用法。预注册：|Δ| 的均值 ≤ 0.3
   且 95% 分位 ≤ 1。
4. **运行域限定（预注册）**：我们的样本主体数集中在 1–7，
   观测到的重复量级是 +1；**card ≥ 7 的塌方不在运行域内**，
   报告时明确限定 "calibrated for scenes with ≤ 6 instances"。
5. **判死不变**：对 `n_actual` 仍不过 -> 弃计数，改成对偏好判决
   （VLM 只答"哪张图有主体被重复"）。

### 8.9c 重考通过：VLM 正式上岗主尺（2026-08-14）

作者核对真值 60 张（CoCoCount card 2-5 各 15，重复图与 uncertain
如实标注），VLM 检定：**MAE 0.20 / bias +0.03 / 严格命中 80% /
±1 内 100%**，分层 card=2/3/4/5 = 0.00/0.27/0.33/0.20 —— 无饱和
模式，压倒性通过预注册门槛（MAE<=1.0 且 |bias|<0.5）。
20% 的 ±1 分歧几乎全落在作者自标 uncertain 的图（嵌套碗/交叠领带/
遮挡牛群）—— 场景歧义，非仪器误差。
**仪器链三腿闭合**：空对照满分（配对零噪声）+ 绝对精度（本节）+
眼睛核账协议（曾抓住 263 假阳性）。运行域 <=6 实例照旧限定。
附赠：**SDXL 数量服从率 33%**（声明数=实际数比例）——
旧考卷作废的带数字证据，写进附录一句话。
事故记录：同一真值文件被套到 GenEval（idx 撞车）出过一块
MAE=2.02 的假结果 —— 已修（真值文件声明 _bench，不匹配跳过），
GenEval 那块作废不引用。

## 9. 顶会作战图（2026-08-13 重排：方法优先，度量降为支撑 —— 用户的标准，
且数据站在这一边：触发率四语料 ~2%，评测缺口当头条撑不住；
方法故事恰好瞄着 ScaleDiff 真正输掉的 patch 列）

**一句话论文（方法优先版）**：*一张免费的门控图、两个空间化旋钮：
在单次全图前向内同时修掉窗口注意力家族的两个失效（重复、过平滑），
门关时逐字节零代价 —— 标准表上胜过 ScaleDiff（patch 三列），
重复修复上胜过 AccDiffusion（他们的主场正面比 + 1/N 的结构代价）。*

**两个"胜过"各自的达成路径与所需证据**：

| 目标 | 路径 | 证据形式 |
|---|---|---|
| **标准表胜 ScaleDiff** | v2（背景放松结构引导 -> 真细节）打 FIDp/KIDp/ISp —— ScaleDiff 输 2/3 的列 | 标准指标本身，无需新尺 |
| **修重复胜 AccDiffusion** | E4 他们主场（DemoFusion）正面比 + 触发集上并排图（他们残留、我们干净） | 图 + **一个量化读数**（VLM-Δdelta，检定后） |

**度量的新地位：实验协议里的仪器，不是贡献支柱。**
"七篇点名、零篇测量"降为 motivation 里的一句话；律降为 method 一节的
机制分析（门为什么这么设计）；语料学发现（四语料 2%、seed 方差、
图表错位）压缩进触发集构建的 justification 段。
**但"修重复比 AccDiffusion 好"这个主张在顶会无法零数字成立** ——
两家的柯基都干净时图分不出高下 —— 所以 VLM-Δdelta 保留，
身份是"支撑该主张的检定过的读数"，不再是 headline。

**原"一句话论文"（度量优先版）存档于此，作为 fallback**：若 v2 判死
（P1 不过）且 patch 列打不动，方法柱塌，届时评测故事重新上位。

### 9.4 v2 死后的作战图（2026-08-13，"换不换方向"之问的回答：不换）

**v2 之死改变的唯一一件事：patch 三列暂失武器。** §9.3 的"赢"
从来是帕累托（标准表平 + Rep 列碾压 + patch 列争取）——门关字节等同
使 FID 严格赢在数学上不可能，而这个不可能正是零代价卖点。
争取项悬空，帕累托主张未损。**对照在位者**：AccDiffusion 的 ECCV
牌面（定性诊断/patch 级方法/标准表/贴图）每一项我们都有更强版本
（定量律/单前向 1/N 代价/检定仪器/双管线），外加图表错位机制与
"结构引导净正贡献"这条新负结果。方向不缺证据，缺执行。
换方向 = 律/协议/参考集/仪器/harness/双管线代码全部清零。

### 9.5 DemoFusion 移植侦察（2026-08-13，读完 pipeline_demofusion_sdxl.py）

**前向结构**（1446 行，单文件）：Phase 1 = 标准 1024 全图去噪
（latent 128，与 ScaleDiff 基础阶段同构 -> **门控图录制零改动可用**，
BlendCrossAttn 的 phase-1 录制逻辑原样成立）。Phase k = 每步三合一：
① **MultiDiffusion patch 支**：get_views 出 128×128 latent 窗
（stride 64 + random jitter），每窗**喂完整 prompt**（L1157
prompt_embeds_input = cat([prompt_embeds]*vb_size)）——
**重复的病灶就在这行**：每个窗都被告知"画一个 XX"；
② **Dilated Sampling 支**：按 scale 跨步抽全局子网格 + 高斯滤波，
同样喂完整 prompt；③ c1/c2/c3 余弦调度混合 + 与初始噪声版本插值。

**门的挂法**：窗坐标 (h_start..w_end) 已知 -> 把门控图裁到窗内，
BlendCrossAttn 逐位置混合逻辑**原样适用**，只差"当前批的窗坐标"
这个上下文：views 按 vb_size 批进 UNet（repeat_interleave(2) 后
批元素 i 的窗 = batch_view[i//2]），在 unet 调用前
gate.set_views(batch_view, jitter) 即可。dilated 支的子网格 =
map[h::s, w::s] 同理可裁（是否也上门 -> 消融决定）。

**实施方式**：拷贝管线文件为 pipeline_demofusion_gated.py 显式加钩
（monkeypatch 1400 行 __call__ 不现实）；改动点三处：
phase-1 结束 finalize / patch 支 set_views / dilated 支 set_views。
**门关 = 不装 processor = 逐字节原版**（同一文件跑基线臂）。

**E4 协议草案**：同 prompt 同 seed 四臂 —— DemoFusion 原版 /
DemoFusion+门 / AccDiffusion v1 / v2；语料 = Parti 34 触发集 +
LAION-1k 抽样（分层）；读数 = VLM-Δdelta（检定过）+ 并排图 +
时间/显存；DemoFusion 声明 3090-24GB 可跑 4096（lowvram），
4090 无障碍。他们管线出成片重复（AccDiffusion 全文为证）——
门在那里的每一分修复都是正面战果。

### 9.5b 调研：同问题不同 backbone 如何中顶会 + **一个漏掉的近邻**（2026-08-14）

**⚠️ 文献缺口：FAM Diffusion（CVPR 2025，Samsung，arXiv 2411.18552）
我们的文献库零记录。** 定位与我们高度重叠：training-free、可插任何
latent diffusion、**明确避开 patch/渐进式以求低延迟**（原文
*"avoids redundant inference tricks for improved consistency such as
patch-based or progressive generation, leading to negligible latency
overheads"* —— 正是我们"单次前向 vs N 次 patch 前向"那条轴）；
两模块 = Frequency Modulation（傅里叶保全局结构）+ **Attention
Modulation**（局部纹理一致性，自称 *"a problem largely ignored in
prior works"*）。
**已核定（2026-08-14，用户推 PDF，全文读毕）**：

1. **AM = self-attention 矩阵混合，不碰文本条件**（Eq. 7:
   `M̄ᵐ = λ·U(Mⁿ,s) + (1−λ)·Mᵐ`，只在 up_block_0，目标是局部纹理
   一致性 —— "狐狸毛长到衬衫领子上"）。**我们的位点（cross-attention
   文本条件逐位置混合）安全。**
2. **FM = 频域版结构引导，与 ScaleDiff 同族**：
   Eq. 5 `fₜ = zₜ + κ(t) ⊛ (z̃ₜ − zₜ)` vs ScaleDiff
   `x_pred + scale·(lowpass(x_ref) − lowpass(x_pred))` —— 区别只在
   κ(t) 是频率选择性时变核、ScaleDiff 是固定低通+标量时间调度。
   **他们写 "FM module ... solves the issue of object duplication"，
   证据只有定性图，全文零重复指标**（评测 = LAION 1K captions +
   10K 真图，FID/KID/FIDc/KIDc/CLIP + latency）。
   -> **我们有该族的实测反例**（ScaleDiff 带结构引导仍 +0.61、
   331 上 1->4）：一篇 CVPR 2025 的未验证主张，我们有仪器可检验。
3. **两个旋钮在空间上都均匀**：κ(t) 是卷积核（平移不变）、λ 是全局
   标量。**母题"均匀条件 × 异质内容"正落在最新在位者头上。**
   谱系补完：ScaleCrafter 均匀膨胀 / DemoFusion 均匀 skip residual /
   **AccDiffusion 逐 patch prompt（唯一空间自适应，代价 N 次前向）**/
   ScaleDiff 均匀结构引导 / **FAM 均匀频率核+均匀 λ** /
   **我们：单次前向内逐位置文本条件**。
4. **无公开代码**（GitHub 遍搜 happy-hsy / SamsungLabs / Samsung 均无）
   -> 只能作引用行进表，无法复现；但也意味着无人能验证其重复主张。

**被 FAM 拿走、必须放弃的三个卖点**：① "更快"（SDXL+FAM 2×2 仅
1 min vs DemoFusion 2.5 / AccDiffusion 2.6；3×3 为 2.5 vs 8.6/10）；
② "首个避开 patch 的单次前向"（其核心叙事）；③ "跨家族通用"
（已装在 SDXL 直推 + **HiDiffusion 窗口注意力**上）——
**柱三撤销是对的，否则正面撞车。**

**坏消息（诚实记账）**：FAM 赢的正是 **patch 三列**（FIDc/KIDc/CLIP），
而 FID 不稳（3×3 时 69.25 输给 DemoFusion 68.82）。patch 列反映
细节/纹理质量 = Campaign A 的目标 = v2b 负责而 v2b 不出货的那块。
-> **"标准表打赢 patch 三列"这条路前面挡着一个带强数字的 CVPR 2025。**
战略结论：**重心彻底压在"重复 + 测量"，patch 列降为护栏**（落在
噪声地板内即可），不再当胜负手 —— 与"v1 出货、v2b 退实验室"一致，
现在有了外部理由。

**创新模式（四种，无一涉及跨框架移植）**：
| 模式 | 内容 | 实例 |
|---|---|---|
| A 换干预位点（主流） | 同病换下手处，论证该位点能修别人修不了的 | ScaleCrafter 卷积/FouriScale 频域/DemoFusion skip residual/AccDiffusion patch 级 prompt/Pixelsmith patch 级结构条件/FAM 傅里叶+注意力矩阵/HiWave 小波/ScaleDiff 窗口注意力 |
| B 点名被忽略的子失效 | 先发明问题再发明方法 | AccDiffusion「物体重复」；FAM「局部纹理一致性」 |
| C 换 regime | 推进到范式会崩的新状态 | PixelRush few-step，换 10–35× |
| D 加实用轴 | 延迟/显存/单卡/分辨率上限 | FAM 零延迟开销；PixelRush 速度 |

**可比性从哪来**：同基座（SDXL）+ 同评测协议（LAION prompts、
FID/KID/IS + patch 三列、CLIP）。**不是靠移植机制。**

**证据标准比预期宽松**：HiWave 拿 SIGGRAPH Asia 2025 靠定性对比 +
偏好研究（80% 胜率）；AccDiffusion 进 ECCV，重复问题**零量化指标**。
-> **这条线至今没有一把重复的定量尺，我们那把检定过的（MAE 0.20）
是全场唯一，属超配。**

**对我们的推论**：① 移植彻底作废（无人如此，写了不加分）；
② 位点仍空 —— AccDiffusion=patch 级文本条件（需 N 前向）、
FAM=注意力矩阵+频域（不碰文本条件）、Pixelsmith=patch 级结构条件，
**「单次全图前向内逐位置混合文本条件 + 免费门控图」无人占据**（待
FAM 原文确认）；③ 我们占 B+D：点名的不是"重复"（已被占两次）而是
**"重复从未被测量、以及它何时发生"**（律 + 触发率 <3% + seed 主导
+ 图表错位），实用轴 = 1/N 代价 + 门关零开销。

### 9.5a 移植降级为附录：**这条线没人这么做**（2026-08-14，用户指出，核实属实）

AccDiffusion v2 源码原话（accdiffusion_plus.py L1016）：
*"We build AccDiffusion v2 based on Demofusion pipeline"* ——
**他们的机制从未离开 DemoFusion**。这条线的公共分母是 **SDXL**，
不是框架：ScaleCrafter / DemoFusion / AccDiffusion / ScaleDiff /
PixelRush 各自原生跑，同一张表里比。**"把机制移植到对手框架"
是我给我们自己加的、没人要求的考题**，代价是工程时间 + GPU +
一个混淆的首战结果（331）。战略误判，认账。

**修正后的 E4（按领域惯例）**：同 prompt 同 seed，各方法**原生**跑 ——
DemoFusion / AccDiffusion v1 / v2 / ScaleDiff（我们的基线行）/
Ours = ScaleDiff+门。我们的贡献 = 与 ScaleDiff 行的差。
**对我们更有利**：我们有检定过的尺（MAE 0.20），可以给表里
**每一个方法**算 Rep 列 —— 这条线上没人量化过重复（AccDiffusion 全靠
贴图）。"提出度量、评所有人、自己赢下该列"比"搬到别人家赢"更合惯例
也更省。移植代码保留为附录通用性脚注（门关字节等同在第二条管线上
亦成立），**不再投 GPU**。

### 9.5d PixelRush 的身份存疑 + "换 regime"的正确理由（2026-08-14，用户质询后订正）

**我自己的两个错误，先记账**：
① 我说 few-step 那一格是空的 —— **错，PixelRush 就在里面**（§5.46 早已
记过：one-step、4K 20 秒、单卡 8K）。速度这条轴我们追不上也不该追。
② 我说"少步下重复大概率更严重" —— **纯属猜测，且方向可能相反**：
PixelRush 原文写自己从保结构 latent 起步，*"inherently mitigates
object repetition"*。

**但那句宣称零度量。** 全线清单：ScaleCrafter "mitigates" / HiWave
"reduce repetition" / FAM "solves the issue of object duplication" /
AccDiffusion 整篇以重复为卖点 / PixelRush "inherently mitigates" ——
**五家宣称，零个定量指标**。在无人测量的领域里，"我解决了"与"没提"
的信息量同样接近零。

-> **换 regime 的正确理由不是"那里空"，而是"那里的新霸主把重复当作
顺带解决掉的东西写进了论文，从来没人验过，而全场唯一检定过的尺在
我们手上"。** 这就是 §5.46 的形状一，打的是当前前沿而非两年前的目标。
regime 选 one-step 还是 few-step **不由我们定，跟着靶子走**。

**⚠️ 记录内部矛盾（必须核实后才能据以决策）**：
§5.46 写"PixelRush（CVPR）"，但 `survey_pixelsmith_citers.md`
记的是"2026 arXiv | 未确认"。二者不能同时为真。
- CVPR + 代码可跑 -> 打其未验证宣称是顶会级贡献；
- 仅 arXiv 预印本 -> 分量大减（未经同行评议，"它错了"不值钱）；
- 代码跑不起来 -> **硬闸**。我们的地基筛是"开源码 + 会议接收"，
  不能对自己放水。

**预注册两道闸**（过不了就不转）：
- 闸一（零 GPU）：核实 venue + 开源码可运行性；
- 闸二（1–2 h）：用我们的尺量它。delta ≈ 0 -> 其宣称为真，该方向
  当场判死；delta 明显 > 0 -> 最新 SOTA 有一个从未被测量且不成立的
  核心宣称。

**附带风险**：我们的门逐 cross-attention 前向生效，50 步管线有几十次
挂载点，one-step 管线可能只有个位数次。机制不靠跨步累积，理论上一次
也成立，但"够不够把副本压下去"未知，须实测。

### 9.5e PixelRush 原文核定：闸一判死 few-step 转向，但**主线被加固**（2026-08-14）

**身份争议已解决**：PDF 带 CVF Open Access 水印 ——
*"This CVPR paper is the Open Access version ... identical to the
accepted version"*，正文页码 35946。**是 CVPR 正式接收**，
`survey_pixelsmith_citers.md` 记的"2026 arXiv | 未确认"是错的，
§5.46 的"CVPR"是对的。作者 Qualcomm AI Research。

**闸一判决：转向 few-step —— 死。** 三条各自足够：
1. **全文零 github、无代码发布**（用户确认无开源）。我们自己的地基筛
   是"开源码 + 会议接收"，不能对自己放水。无代码 = 无法复现 =
   **无法用我们的尺去量它** = 攻其未验证宣称这条路不存在。
2. 它跑 **A100-40GB**；我们是 4090-24GB。即便有码也未必复现得了。
3. 它已 CVPR 接收并占住该 regime。**带着"无法与之比较"进场是坏位置** ——
   审稿人一句"PixelRush 已在 CVPR 做过 few-step training-free 高分辨率"
   就够了。故**也不独立进场**（不用 SDXL-Lightning 另起炉灶）。
判死干净，代价 20 分钟，不是三个月。

**但读完原文，主线反而被加固 —— 谱系现在是完整的**：

| 方法 | 对重复的处方 | 空间上 | 重复指标 |
|---|---|---|---|
| ScaleCrafter | 扩张卷积 | 均匀 | 零 |
| DemoFusion | dilated sampling / skip residual | 均匀 | 零 |
| FouriScale / FreeScale | 频域对齐 | 均匀 | 零 |
| HiWave | DDIM 反演噪声作结构先验 | 均匀 | 零 |
| FAM（CVPR 2025） | 频率调制 κ(t) + 全局 λ | 均匀 | 零 |
| ScaleDiff | 结构引导 | 均匀 | 零 |
| **PixelRush（CVPR）** | **DDIM 反演 latent 作结构先验** | **均匀** | **零** |
| AccDiffusion（ECCV） | 逐 patch prompt —— **唯一空间自适应** | 逐 patch | 零 |

> **七家开出同一张处方："给它一个更好的结构先验"，且全部空间均匀。
> 八家宣称缓解重复，零家测量过。**

**而且 PixelRush 自己就是 patch-based**（原文：*"builds upon the
established patch-based inference paradigm"*，用 SDXL-Turbo 做放大）——
即它明知 *"patch ... frequently exhibit object repetition"*，仍只用
结构先验去治，没有碰文本条件。

**我们有该处方族的实测反例**：ScaleDiff **带**结构引导，触发集上仍
+0.61，331 上 1->4。这是对整族前提的**可测量的反证**，不是修辞。

**证据标准的另一面（对我们不利，须正视）**：这条线的定性证据靠**人工
偏好研究** —— PixelRush 盲测 30 人（82.83/84.09/86.49%）、HiWave 81.2%。
**我们无标注人力，这条轴对我们关闭。** 反过来说，一个检定过的自动
计数器（MAE 0.20、可复现、有 null control）比一次 30 人的偏好投票
更可复现 —— 这是我们唯一能出、且站得住的证据形态。

**顺带一条协议不稳定的新证据**：PixelRush 的 baseline 是
SDXL-DI / FouriScale / DemoFusion / FreeScale ——
**不含 AccDiffusion、不含 ScaleDiff、不含 HiWave**。
与 §9.5c 的两张相反排序合并成同一条论证：这条线的标准表互不可比。

**结论：不转。回到主线，论点收紧为"处方族的可测量反证 + 唯一空间
自适应的低成本实例化"。AccDiffusion 正面测量仍是唯一的去留闸门。**

### 9.5f **AccDiffusion 自己写明"现有定量指标测不出重复"**（2026-08-14，原文核定）

读 AccDiffusion.pdf 原文（不是转述），三条硬事实：

**① 他们没有做人工评测。** 全文
`user study` / `human` / `participant` / `preference` / `volunteer`
**各出现 0 次**。（对照：PixelRush 30 人盲测、HiWave 81.2% 偏好。）

**② 框是手画的。** Fig.6 图注原话：
> *"**We draw a red box** upon the generated images to highlight the
> repeated objects. Best viewed zoomed in."*

**③ 他们在正文里宣布了测量缺口**（Fig.6 正下方）：
> *"Considering the fact that **existing quantitative metrics are unable
> to accurately reflect the extent of object repetition**, we choose to
> provide visualizations to demonstrate the effectiveness of our core
> modules in preventing repeated generation."*

**这一句是本项目最重要的外部授权，必须进论文正文（引用 [AccDiffusion]）。**
它一次做三件事：
- 证实"重复无法被现有指标反映"是**领域公认**，不是我们的自说自话；
- 把度量缺口变成一张**公开的邀请函** —— SOTA 自己宣布这个位置空着；
- 因此我们的尺子**不必是卖点**，它是"补上 SOTA 自己承认的缺口"，
  放附录/辅助表即可，一句引文就立住，不心虚。

**对证据策略的直接推论（本轮定案）**：
> 这条线接受的证据格式 = **带框的定性图 + 标准表**。
> 我们**照办**（box_figure.py），不再把度量当贡献。
> 用户连续两轮把方向从"度量优先"拉回"方法优先"，两次都对，已记账。

**版面上有意与他们不同**：他们是「一个 prompt × 四种长宽比 × 四个方法」；
我们用「**一行一个 prompt × 各方法一列**」—— 多几个 prompt 比多几个
长宽比有说服力，尤其我们要打的正是"他们在更难的 prompt 上没解决"。

**可直接打的靶子**：他们 Fig.6 的 (d) AccDiffusion 那一块**一个红框都没有**，
图面含义是"我们零重复"，而那是在**单个 prompt**（Astronaut on Mars）上
展示的。我们的实测：AccDiffusion 在硬子集上 **Rep⁺ = 0.667**，
**734 是 +3**（帕特农被镜像复制 + 水面凭空一艘发光大船）。
-> **用他们的格式、他们的画框规则，在他们没展示的 prompt 上，
把红框画到 AccDiffusion 的输出里。** 这是论文第一张图的形状。

**我们比他们多的一条诚实规则**（box_figure.py 已实现）：
标注文件里该臂**键不存在** = "NOT ANNOTATED YET"（红字）；
该臂是**空表** = "no repetition found (author-checked)"。
两者绝不能都渲染成留白 —— 留白会被读成"漏画了"。
作者自行标注这件事必须在论文里明写（与他们同一做法）。

### 9.5c 两篇顶会给出**完全相反的排序**（2026-08-14，用户举证）

同样两个方法、同样 4096²、同样 LAION-1k 式协议，两张已发表的表：

| 出处 | AccDiffusion | DemoFusion | 谁赢 |
|---|---|---|---|
| **AccDiffusion 自己的 Table 1** | FID **63.89** | 65.97 | Acc 赢 |
| **FAM Diffusion Table 1** | FID 73.97 | **65.89** | Acc 输 10 分 |

DemoFusion 两处几乎一致（65.97 / 65.89 —— 说明协议大体可比），
**AccDiffusion 差了 10.08 分**。不是抄错数量级的错，是一整个方法
在别人手里复现不出自己报的数。

**这不是要去指控谁**，而是三条对我们直接有用的推论：

1. **FID 在这条线上不具备判定力。** 用它当胜负手，等于把结论押在
   "谁跑的实现、谁选的 10K 真图子集、谁的 CLIP/Inception 版本"上。
   这为 §9.3"不承诺 FID 赢、标准七列只当护栏"提供了**外部依据** ——
   原先那是我们的自我保护，现在是文献里的既成事实。
2. **它正面论证了 Δdelta 的设计。** Δdelta 是**配对**的
   （同 prompt 同 seed，4096↓1024 减 base）、**以基图为参照**、
   **尺度漂移相消**、且**在使用前被检定**（MAE 0.20 / bias +0.03 /
   100% 落在 ±1）。上面那张表里出问题的每一处 —— 跨实现、跨真图集、
   绝对量纲 —— Δdelta 结构上都不经过。
3. **写法**：放进"为什么需要一把新尺"那一节，作为动机的**第二条腿**
   （第一条腿是"重复从未被量化过"）。措辞只陈述两张已发表的表加
   一句"两者不能同时成立"，**不去裁决谁对**，也不需要我们复现任何
   一方 —— 我们没有算力做那件事，也不必做。

**要守的纪律**：正因为我们拿这个当论据，我们自己那把尺的检定材料
（gt_filled.json 60 条、null control、per-stratum 分解）必须全部
进附录。批评别人不可复现的人，自己得可复现。

**E4 首战（2026-08-14，331 @4096，先锋战第一发）**：
工程全绿 —— 冒烟 1024+2048 双级 md5 与原版一致（门关字节等同在
DemoFusion 上实测钉死；期间钉掉 Python random 的窗口抖动不可复现
问题、修掉上游 lowvram 空指针）；4096 单臂 17.5 分钟 / 18GB，
不开 lowvram 装得下。
眼睛判决：**base 火炬平台长出整尊金色女神克隆（金冠金袍举臂俱全），
v1 同位置是一架黄色直升机（prompt 合法物体）**；base 天空另有
幽灵女神轮廓 + 蚊群尺寸失调小直升机，v1 全部收敛。
主体计数口径：base 女神 ≈4（主像+岛像+金克隆+幽灵），v1 = 2。
诚实注记两条：① 岛上小女神两臂共有 —— 生于 phase-1，门的职责是
不让放大器新铸主体，不涂改基础阶段（作用边界干净的证据）；
② 两臂皇冠均有直升机-雕像熔接 —— patch 家族的局部畸变病
（AccDiffusion v2 的 ControlNet 战区），不在我们射程，如实标注。
账本：单样本两臂 35 分钟 -> 34 全量 ≈ 20h，先锋批 6 样本 ≈ 3.5h；
AccDiffusion v2 臂 1599s/张另计（~15h）。
**该批止于此（见 §9.5a）**：移植降级为附录，GPU 转投原生对比。

**331 挖出的真缺陷（与移植无关，必须带回自家管线）**：门把主体从
背景条件摘掉后，**替代文本残留的可数名词成为新的重复受害者** ——
331 的背景条件字面就是 "helicopters"，满天小直升机有我们一份责任。
**ScaleDiff 上一样存在**：263 背景拿到 "the Washington Monument"、
583 "the net of a tennis court"、1527 "a door"、994 "a poker table"、
146 "surounding a kettle"（另有 1059/715/157/553 等纯场景，干净）。
**我们此前只数主体、从没数过这些残留名词，+0.61 -> +0.10 的成绩单
可能虚高。** 诊断集为何没暴露：那 30 条是"单主体 + 风景"，摘掉主体
剩下的正好是场景 —— 又一处脚手架与真实语料的系统性差异。
自检工具 resid_count.py（图现成，几分钟 VLM）：
坐实 -> **v1.1 = 替代文本摘掉所有可数名词、只留场景词**（自然推广，
可写成一节）+ 成绩单附该列如实披露；不成立 -> 成绩单照旧。

**自检结果（2026-08-14）：转嫁假说被推翻，成绩单不打折。**
22 条可比（6 条替代文本纯场景跳过）：差值均值 **−0.50**，
增加的仅 1 条（146 kettle 1->2），减少 4 条（998 pyramid 4->3、
734 airplane 3->2、1187 cow 2->1、1257 wall 10->1），持平 17 条。
方向甚至相反 —— 门降低的是整幅画的复制冲动，不只主体。
**关键：331 的 helicopter 在 ScaleDiff 上 10->10 纹丝不动** ——
DemoFusion 那边的蚊群直升机是那条管线自身的行为，不是我们的替代
文本造成的；我上一轮的自责撤回。+0.61 -> +0.10 如实成立。
待查一项：1257 "wall" 10->1 跨度过大（长城近不可数、超出 <=6 运行域），
放大看一眼定是仪器噪声还是门压掉了背景结构。

**执行清单（每步带判据）**：
1. 仪器收官：--armdelta / --null / --gt-sheet -> --regt（本周，便宜）。
2. **v1 真考**：34 条触发集跑 v1 臂 —— 263 女神消失？583 网纹清掉？
   25 条干净样本字节不动？过 -> 论文第一张方法图；
   **不过 -> 那才是真正讨论方向的时刻**（机制上判断会过：门作用于
   文本条件，与结构引导那条死路无关）。~45 min GPU。
3. **v2b 唯一一发**：空间化 LFM 噪声注入（背景多给原料）。依据：
   封棺证明瓶颈在原料侧；PixelRush 已证均匀噪声注入治过平滑，
   我们做空间自适应版，仍是同一张门控图。过 -> Campaign A 满血；
   不过 -> 过平滑章以两条负结果收笔，论文收缩为 v1 单旋钮。
   **对 §0.0 措辞的依赖（2026-08-13 记）**：v2b 若成，"一句话论文"
   不用改 —— LFM 均匀撒噪声原料本身就是"均匀条件 × 异质内容"的
   又一实例，只是旋钮从引导强度换到噪声原料，母题更纯；
   v2b 若死，§0.0 的"两个失效"必须诚实收缩为"一个失效 + 机制分析"
   （负结果二条：放松引导无效 / 全关更糟 = 结构引导净正贡献）。
   另：**对手基准始终是 AccDiffusion v2**（标准表 64.64/1599s 那行、
   motivation 引文、对偶性分析、E5 ControlNet 消融全部对着 v2）；
   "开源+顶会"是选地基的门槛（ScaleDiff），不是选对手的门槛，
   对手打最强在位版本，v1(ECCV24) 并列一行。
4. **DemoFusion@4096 决战**：律的高段 + E4 正面比 —— "新 SOTA"
   的来源，与 v2 生死无关。
5. E3 标准表（护栏）+ E5 反向移植代价 + 余力 E6 FLUX。


### 9.1 三根柱子与证据状态

| 柱子 | 主张 | 证据 | 状态 |
|---|---|---|---|
| **事实** | 展示语料（主体+场景手写 prompt）与评测语料（LAION 裸主体 caption）错开，故表格从不显示该失效 | LAION 侧：触发率 2% + 直接测量 delta 平（C−A=+0.27）；触发侧：Parti 34 条眼睛判为 +1 级（§5.49e）。**律的高段必须来自 DemoFusion@4096（R 轴用 2048/4096）—— 诊断集的 +5.40 按 §6.9 不得进正文，8192² 按 §5.49e 封条不复活** | **半闭合** |
| **尺子** | Δdelta 无需真值可测重复；朴素测法被尺度漂移污染（实测假阳性 +2.33） | 漂移消除设计 + 双检测器一致 + D 层干净；**真值验证（CoCoCount/GenEval 计数）待跑** | 大半 |
| **方法** | AccDiffusion 原则的 patch-free 实例化：单前向、双嵌入、连续门控、门关时同管线逐字节不动、+3.9% | 机制消融已有（v0 无效、KV×2 太贵）；**主表与正面对比待跑** | 待主战 |

### 9.2 六个实验到三个口号

| # | 实验 | 支撑的口号 | 算力 |
|---|---|---|---|
| E1 | Δdelta 对 CoCoCount/GenEval 真值计数校准 | "被验证过的尺子" | 数小时（只计数） |
| E2 | ScaleDiff+DemoFusion 基线在 CoCoCount 上的 delta（触发侧闭环 + 律的正域检验） | 事实柱第二半 | ~400 张 ≈ 10 h |
| E3 | **主表**：我们 vs ScaleDiff，CoCoCount 分层 Δdelta + LAION-1000 标准表（护栏，配换 seed 噪声地板） | "比 ScaleDiff 强"（帕累托+扩维） | 最大头 |
| E4 | 我们的方法装上 DemoFusion，与 AccDiffusion 同批 prompt 正面比（Δdelta、FIDp/KIDp、时间） | "比 AccDiffusion 强" | DemoFusion 慢，~60 条分层 |
| E5 | 在 ScaleDiff 上实测移植 patch-content-aware prompt 的代价 | "结构上不可用"成为实验 | 小 |
| E6 | FLUX 第二骨干（ScaleDiff 对齐） | 同任务定位 | 后置 |

### 9.3 "赢"的定义（诚实版）

- **vs ScaleDiff**：标准七列落在实测噪声地板内 + Rep 列量级差的赢
  + patch 三列争取真赢（畸形复制体消失应体现在原生裁块上）。
  **不承诺 FID 赢** —— 同骨干上没有人能有意义地赢 FID。
- **vs AccDiffusion**：他们主场（DemoFusion + 他们式 prompt）上
  Δdelta 相当或更好，代价 +3.9% vs 每 patch 独立编码+前向；
  且其方案在窗口注意力家族结构不可用（E5 的表）。
- **判死条件**：E2 在 CoCoCount 上也平 -> 前提死，转 few-step/PixelRush
  分支；E4 输 -> 降级为成本/通用性主张；patch 列不动 -> 撤该主张只留 Rep。

## 8. 实验优先级（按"支撑哪一句"排，不按方便程度）

```
0. trigger.py（空视野比例 vs excess）         ~5 min  ✅ 已做
1. 补 seed 1234 / 2025，两个 arm            2.6 h   ✅ 已做
2. DemoFusion 上量同一条律                  ~6 h    ✅ 已做，§1.2.2 缺口已补
3. net_probe.sh（服务器逐项测网络）           ~2 min  解 §7.2，决定第 5 条怎么做
4. Table 2 两行                            ~90 h   支撑 §5，决定录用
5. FLUX 跑通 ScaleDiff + 我们的方法          ?      支撑 §0"我们在同一个任务里"
6. DemoFusion vs AccDiffusion（尺子的外部验证） ~6 h  已发表共识在这一对上
7. 8192²（试 offload）                      ?      完整度加分
```

**第 3 条只花两分钟，但它决定第 4 条（90 GPU 小时那件事）是直接开跑
还是要先解决数据。先测，不要再基于一次失败做长期计划。**

## 10. 转向：从"3% 的失效"转到"每张图都有的属性"（2026-08-15，方法线判死后重定向）

### 10.1 为什么必须转（上一节留下的硬规矩）

§3.14e 判死了干预家族（Rep⁺ 在 0.23 处撞墙）。但更致命的是**触发率**：
物体重复在四个语料上的触发率 <3%。标准表是 1000 张图的均值 ——
**3% 的样本就算修到完美，均值也只动 3%×效应，淹在噪声里。**

已发表的反证（FAM Table 1，4×4）：AccDiffusion 把重复修好了，
FID **73.97** 输 DemoFusion 的 65.89，FIDc **54.80** 输 48.44。
**修重复 → 标准表变差。** 这不是我们的推测，是印刷出来的。

> **规矩（写死）：要动标准表，必须改「每张图都有」的东西。**
> 任何候选方向先过这一关，过不了不再讨论。

### 10.2 新方向的证据：ScaleDiff 自己的两张表

**Table 2（4096² SDXL）—— 他们唯一没赢的一列是 KIDp：**

| 方法 | FID↓ | KID↓ | IS↑ | FIDp↓ | KIDp↓ | ISp↑ | CLIP↑ | Time↓ |
|---|---|---|---|---|---|---|---|---|
| DiffuseHigh | 63.91 | .0034 | 18.99 | 42.30 | **.0079** | 19.54 | 32.68 | 325 |
| FreeScale | 64.33 | .0036 | 19.18 | 39.56 | **.0079** | 18.91 | 32.56 | 517 |
| AccDiffusion v2 | 64.64 | .0037 | 18.56 | 40.92 | .0083 | 18.42 | 32.34 | 1599 |
| DemoFusion | 65.06 | .0041 | 19.13 | 41.29 | **.0079** | 19.59 | 32.61 | 1005 |
| **ScaleDiff** | **61.87** | **.0025** | **19.56** | **38.89** | .0080 ❌ | **20.41** | **33.04** | **113** |

**Table 3（他们自己的消融，同分辨率同骨干，只换注意力）：**

| Attention | FID↓ | KID↓ | IS↑ | FIDp↓ | KIDp↓ | ISp↑ | Time↓ |
|---|---|---|---|---|---|---|---|
| **MultiDiffusion** | **61.71** | **.0021** | **19.71** | **38.08** | **.0069** | **20.85** | 239 |
| NPA（他们的） | 61.87 | .0025 | 19.56 | 38.89 | .0080 | 20.41 | **113** |

原文承认：*"MultiDiffusion generates high-quality images and achieves the
best scores, but suffers from substantial computational overhead."*
（SDXL 2.1×，FLUX 1148s vs 407s = 2.8×）

**把两张表对起来 —— 缺口比他们的领先还大：**

| 列 | NPA 比 MD 差 | ScaleDiff 对第二名的领先 | 比值 |
|---|---|---|---|
| FIDp | 0.81 | 0.67（vs FreeScale 39.56） | **1.2×，自己丢的比赢的多** |
| KIDp | 0.0011 | −0.0001（**输**给三家） | **11× 的差距就在自己消融行里** |
| ISp | 0.44 | 0.82（vs DemoFusion 19.59） | 54% |

**结论：ScaleDiff 的速度是拿局部保真度换的，换了多少他们自己印出来了。**
把 MD 级的 patch 质量在 NPA 级的开销下拿回来，就是标准表七列全赢。

### 10.3 第三个事实：patch 三列随分辨率单调崩塌，全局列察觉不到

2048² → 4096²（ScaleDiff 自己的数）：全局 FID **62.98 → 61.87（变好）**，
而 ISp **25.70 → 20.41（−21%）**、KIDp **.0067 → .0080（+19%）**。
DemoFusion（26.42→19.59）、AccDiffusion（25.66→18.42）同样崩。

**根因**：FID/KID/IS 要把图缩到 299px，局部损失被缩没了；损失全部
落在 patch 三列，且越放大越严重。这是整条线共有、随任务难度增长的失效。

另记：2048² 上 ScaleDiff 的 patch 三列**根本不是 SOTA** ——
FIDp 38.03 输 DemoFusion 35.98，KIDp .0067 输 .0050。两个分辨率都有活。

### 10.4 这条轴逐条过 §10.1 的规矩

| 检验 | 物体重复（旧） | 局部保真 / patch 三列（新） |
|---|---|---|
| 每张图都有？ | ❌ 触发率 <3% | ✅ 每张图每个 patch |
| 能动 1000 张均值？ | ❌ 淹在噪声里 | ✅ 直接就是均值 |
| 尺子现成？ | ❌ 要自造 RLoc/DLoc | ✅ FIDp/KIDp/ISp 已在每篇表里 |
| 目标有数字？ | ❌ 只能说"更少" | ✅ MD−NPA 缺口已印刷 |
| 在我们的骨干上？ | ✅ | ✅ 同一份 Table 3 |

### 10.5 重复那条线不作废，变成定性半边

ScaleDiff 原文：*"Since NPA processes images through patches, **it can
introduce repetitive patterns**. To mitigate this ... we apply Structure
Guidance."* —— **patch 注意力近似同时造成重复和 patch 指标损失，
是同一个病因的两个症状。**

于是已有资产全部转成证据：那条律（重复 ∝ R²×(1−主体覆盖)，ρ +0.845 /
+0.686）、Rep⁺/Dmg⁻ 仪器、31 条诊断集、认证过的 VLM（MAE 0.20）、
box_figure.py 红框图、以及 s=0.7 那个点（Rep⁺ −56%、Dmg⁻ 0.000、
+3.7% 开销）——都变成"我们修注意力顺带把重复也修了"。
**这正是 FAM / ScaleDiff 的版面形状：标准表 + 红框定性图。**

### 10.6 算力账：对手臂跑不起，必须引用

单卡 4090，1000 prompt：

| 臂 | 单张 | 1000 张 |
|---|---|---|
| ScaleDiff / 我们 | 113s | **31 h** |
| MultiDiffusion-attn | 239s | 66 h |
| DemoFusion | 1005s | 279 h（11.6 天）|
| AccDiffusion v2 | 1599s | 444 h（18.5 天）|

**引用他们发表数字的前提**：我们复现的 ScaleDiff 行要对得上他们的行。
所以复现实验不是可选项，是引用资格的门票。

### 10.7 预注册（写在看到任何数字之前）—— **三臂，不是两臂**（见 §10.9）

**P0（≈30 h）：n=200，只算 KIDp + ISp，三臂。**
选这两个指标：KID 是无偏估计、小样本可用；IS 不需要参考真图。
FID 在 n=200 上偏得没法看，**不算**。

| 臂 | 说明 | 单张 | 200 张 |
|---|---|---|---|
| A. NPA | ScaleDiff 的评测配置（**未开** shifting） | 113s | 6.3 h |
| B. NPA + Query Window Random Shifting | 他们写了但没用的开关（§10.9） | ~113s | 6.3 h |
| C. MultiDiffusion-attn | 他们消融里全赢的那一行 | 239s | 13.3 h |

**判据一（缺口是否存在）** —— ⚠️ **本条已被 §10.12 作废并重写，
以 §10.12 的三结局版为准。** 原文留档：*"C 对 A 的 KIDp 优势 ≥0.0005
且 ISp 优势 ≥0.2；否则方向当场判死。"*
两处错：(a) 0.0005 这个阈值低于实测噪声地板，跑完也判不出来；
(b) 把"看不见效应"写成了"方向判死"，混淆无效应与欠功效。

**判据二（缺口住在哪，决定方法长什么样）**：令 g = C−A 的缺口，b = B−A。
- **b ≥ 0.6 g** → 缺口主要是**边界伪影**，他们那个没开的开关就吃掉大半。
  这时"打开开关"只是强基线不是贡献，**天花板低**，要重估是否值得做；
- **b ≤ 0.3 g** → 缺口主要是**上下文不足**（每个 query 只看到 2× 窗口，
  而 MultiDiffusion 是重叠平均、覆盖更广），这是真机制，可攻，
  **这才是我们想要的那一格**；
- 中间 → 两者都有，按比例分别处理，先攻大的那一半。

**P1（≈31 h，P0 过了才做）：n=1000 全协议复现 ScaleDiff 4096² SDXL 行**，
对标 (61.87, .0025, 19.56, 38.89, .0080, 20.41, 33.04)。
- 各列在容差内 → 拿到引用整张表的资格；
- 对不上 → 自己跑全部对手，或退回 2048²（那里 patch 缺口更大）。

**评测协议（照抄 ScaleDiff §4.1，不许自选）**：LAION 随机 1000 组图文对，
每 prompt 一张；FID/KID/IS 对真图算；FIDp/KIDp/ISp 按 DemoFusion 的裁块
方式算；CLIP Score 测对齐。**参考集用我们已审计的那 3424 张**（§10.8）。

### 10.8 参考图不是阻塞 —— 那件事 2026-08-11 就做完了（本节订正我自己）

我在本节初稿里把"1000 张真实参考图"写成硬前置。**查了 §7.1.5–§7.1.7，
它早已解决**，写错会让人白跑一趟，这里改正：

- 来源 `laion2B-en-aesthetic`，元数据阶段按短边 ≥299 预筛；
- 全量 4919 候选，两轮存活 3421（69.5%）；
- `real_audit.py` 体检过：占位图 0 组、纯色 0 张、解码失败 0、
  真重复仅 2 对（0.12%）、短边 <299 已归零；
- 留 **3424 张：eval 2864（2.9×）/ tune 560（2.8×）**，`table.json` 指纹守门。

**真正缺的不是数据，是打分代码**：仓库里 `grep FID` 只命中
`fetch_eval_prompts.py`（那是选 prompt 用的元数据字段），
**FID / KID / IS / FIDp / KIDp / ISp / CLIP 一行都没有**。
这才是 P0 之前要写的东西。

另：`help_code/DemoFusion` 里**没有评测代码**（只有 pipeline 和 demo），
而 ScaleDiff 的 FIDp 是 "following [8]"。所以 patch 裁法只能按 DemoFusion
**论文正文**定，且必须在我们论文里写死裁法参数 —— 这是绝对值不可比的
第二个来源（第一个是参考集，§7.1.7 末）。**因此基线行必须自己复现**，
这条与 §7.2 的判据一致。

### 10.9 **ScaleDiff 写了一个修边界伪影的开关，然后没用它**（2026-08-15，读附录 B.1）

附录 B.1 原文（Query Window Random Shifting）：

> *"While Neighborhood Patch Attention utilizes overlapping key/value
> patches to ensure a smooth transition at patch boundaries, **minor
> boundary artifacts can sometimes appear in the generated output.**
> Query Window Random Shifting is an optional technique designed to
> further alleviate such artifacts ... Since offsets are independently
> resampled at each layer, explicit patch boundaries are avoided, which
> reduces border artifacts **with minimal computational overhead**.
> **Note that this technique was not used during the evaluation in this
> paper.**"*

即他们自己：① 承认 NPA 有边界伪影；② 给出一个几乎免费的补救；
③ **评测里没开**；④ 另一边（Table 3）又印出 MultiDiffusion 在七列上全赢。

**核实过的两件事**：
- 开源代码 `help_code/ScaleDiff/SDXL/attn_scalediff_sdxl.py`（205 行）里
  `grep shift` **零命中** —— 这个开关只存在于论文文字，没有实现。
- 该文件的 `get_kv_view` 用 `clamp(r-p4, 0, h-p1)` 把 K/V 窗口套在
  query patch 外围，query 由 `patchify_q` 按 `p2 = window//2` **不重叠**切分。
  要加 shifting，就是给 query 网格加随机 top/left 偏移并同步重算 `kv_views`，
  偏移量有限可缓存，**开销确实接近零**。

**对我们的意义（两面）**：
- 机会：这是一个已被作者承认、已被作者放弃、且从未被任何人量过的缺口，
  而它正落在我们选中的 patch 三列上。
- 风险：**如果打开开关就补上大半缺口，那"打开开关"不是贡献。**
  所以 P0 必须是三臂（§10.7 判据二）—— 先量清楚缺口住在边界还是上下文，
  **再**决定方法长什么样。这是 §3.14e 之后立的规矩：不在没有数字的地方
  提方向。

**论文里的诚实处理（现在写死）**：Query Window Random Shifting 是他们的，
必须作为**强基线**报出并注明出处，不得算进我们的消融收益。

### 10.10 问题陈述定稿：我们到底研究什么（2026-08-15，用户追问"到底转向什么问题"）

#### 一句话

> **窗口/分块注意力是 >1K 生成能跑起来的唯一原因，但现有的每一种实现
> 都是用「丢掉跨窗口上下文」换来的这份便宜，而代价全部记在局部保真度上
> —— FID 要缩到 299px 看不见它，只有 patch 三列看得见，而报了这三列的
> 论文自己没有讨论过它。**

可执行形式：**能不能在 NPA 的开销下拿到 MultiDiffusion 的质量？**
这条帕累托前沿的两端由 ScaleDiff 自己的 Table 3 给出（§10.2），不是我们编的。

#### PixelRush 为什么能中 —— 它的问题也不新（原文核定）

它的贡献结构是三步，**没有提新问题**：

1. **换 regime**：把 training-free 高分辨率从 multi-step 搬到 few-step
   （partial inversion + 蒸馏模型），10–35× 加速。任务一模一样。
2. **承认这个 regime 生出两个新失效**（原文）：
   - *"standard patch blending methods, which work in multi-step processes,
     **fail in a low-step context and produce severe boundary artifacts**"*
   - *"to counteract the **oversmoothing** typically observed in few-step
     generation, we introduce a noise injection technique"*
3. **各修一个并量出来**：FID 52.87 → 50.13，人工偏好 82.8%。

> **中稿逻辑 = 旧任务 + 新 regime + 「该 regime 特有的失效」+ 修好并量。
> 三条 contribution 里两条是子问题的解药。**

对我们：好消息是"问题不新"从来不是这条线的死因；坏消息是它靠 regime
转移（§9.5e 已判死：无代码 + A100）和人工偏好（我们无标注人力）。

#### 一个不该忽略的巧合：两篇点名同一对失效

| | PixelRush（few-step patch） | ScaleDiff（multi-step NPA 窗口） |
|---|---|---|
| 边界伪影 | *"severe boundary artifacts"* → feathering blending | 附录 B.1 *"minor boundary artifacts can sometimes appear"* → 写了 Query Window Random Shifting，**评测未开、代码未实现**（§10.9） |
| 过平滑 | *"oversmoothing ... in few-step generation"* → noise injection | §3.3 *"often results in outputs that lack fine texture details and appear **overly smoothed**"* → LFM |

**两支队伍、两种 regime、两种分解方式，命名出同一对失效** —— 这不是某个
方法的 bug，是**分块/窗口范式本身的税**。而这个税是顶会认的问题类型：
PixelRush 拿它当两条 contribution 中了 CVPR。

#### 三层论点（分清哪层已证、哪层是假设 —— 不许混着写）

**第一层（已证，靠已发表的表）**：patch 三列是这条线唯一还有空档的地方。
ScaleDiff 4096² 七列赢六列，唯独 KIDp 0.0080 输给三家的 0.0079；
而它自己消融里 MultiDiffusion 是 0.0069（缺口是它输掉那 0.0001 的 11 倍）。
且 2048²→4096² 全线 ISp 掉 20%+，而全局 FID 反而变好。

**第二层（已证，靠原文）**：这份损失是**分解范式的税**，不是实现缺陷 ——
七家开出同一张处方（更好的结构先验，全部空间均匀，§9.5e），
两家在不同 regime 下点名同一对失效（上表）。

**第三层（假设，目前零个直接实验，必须标明）**：
**重复与局部保真损失是同一个病的两个症状。** 分解切断长程上下文，模型用
"局部看着合理"的东西填空 —— 区域空旷时填出**副本**（重复），
区域有纹理时填出**糊或胡编**（patch 列变差）。

现有支持只有三条**间接**证据：
(a) 我们那条律 重复 ∝ R²×(1−主体覆盖)，ρ +0.845 / +0.686 ——
    R² 量的正是分解被拉伸了多少，(1−覆盖) 量的正是文本条件有多不管这块；
(b) ScaleDiff 原文 *"Since NPA processes images through patches,
    it can introduce repetitive patterns"*；
(c) 上面那张"两 regime 同一对失效"的表。

**可证伪判据**：若真是一个病，一个只补上下文的机制应**同时**动 patch 三列
和 Rep⁺。**动一个不动另一个 → 第三层作废**，退回第一二层
（仍是一篇"把帕累托前沿往上推"的论文，弱一档，但不死）。

#### 风险（写在前面，不许事后才提）

- 这是**"把权衡做得更好"**的论文，不是"提出新问题"的论文。这类中 CVPR
  是常态（ScaleDiff 本身就是一篇），但要求收益干净、机制不显然。
- **数字小**：FIDp 缺口 0.81，单看不叫大胜。**叫大胜的是"七列全扫且保住
  113s"**，加上重复那一半作定性证据（红框图，§9.5f 的外部授权）。
- **最实的风险仍是 §10.9**：若打开他们那个没用的开关就补上大半缺口，
  天花板就低 —— 故 P0 必须三臂，判据二写在跑之前。
- PixelRush 报 FID 52.87→50.13，ScaleDiff 报 62.98/61.87 —— 又一次印证
  §9.5c：**这条线的标准表互不可比**，基线行必须自己复现（§10.7 P1）。

### 10.11 标准表打分器落地 + patch 裁法的**推导**（2026-08-15，`std_table.py`）

打分代码写好了：FID / KID / IS + FIDp / KIDp / ISp + CLIP，
`scalediff_probe/std_table.py`。三件事必须写进论文的 Evaluation 一节。

#### ① patch 裁法：三家原文一致，真图那侧是**推**出来的

| 来源 | 原话 |
|---|---|
| AccDiffusion / v2 | *"we crop **10 local patches at 1x resolution** (native resolution) from each generated high-resolution image and subsequently resize them, yielding FIDc and ISc"* |
| FAM（CVPR25） | *"we extract **10 random crops** from each image before calculating FID and KID"* |
| PixelRush（CVPR） | *"FIDc, a variant of FID computed on local crops **without resizing**"* |
| ScaleDiff | 只写 *"following [8]"*，而 DemoFusion 仓库**没有评测代码** |

生成侧三家一致：**每图 10 个原生分辨率（SDXL=1024²）随机裁块**。
**真图侧三家都没写**，但 AccDiffusion Table 1 的 1× 行把它定死了：

    1024x1024 (1x)   SDXL-DI   FIDr 58.49   FIDc 58.08

1× 时"10 个 1024² 裁块"退化为同一张整图重复十次；两数几乎相等，
**只可能在参考侧完全相同的情况下成立**。
-> **参考侧 = 真图整图，FID 与 FIDp 共用一套特征，差别全在生成侧。**
这也解释了 FIDp(38.89) 反而低于 FID(61.87)：4096² 缩到 299² 不像任何真实
照片，而 1024² 裁块缩到 299² 就很像。

#### ② 特征塔只认一个

`torch-fidelity` 的 InceptionV3（`weights-inception-2015-12-05`，TF 移植版）
—— pytorch-fid / torch-fidelity / torchmetrics 共用，也是全线发表数字的来源。
换 torchvision 自带的 `inception_v3` 数值整体平移，**与发表值不可比**；
代码里那条路要显式 `--allow-torchvision` 才走，且一路打警告。
本机已验通：uint8 NCHW 输入、塔内部 resize 到 299²，返回 2048-d + 1008-d。
**外面不做任何缩放** —— 两步重采样会偏离发表路径。

#### ③ 两个数值陷阱，都已实测

**FID 的小样本偏差。** 合成高斯自检（d=64，真值 16.0）：

    n=400 -> 20.85     n=2000 -> 17.17     n=20000 -> 16.08

d=2048 时更严重。**这是 P0（n=200）不算 FID、只算 KID（无偏）与 IS
（不需参考集）的实测依据**，不是省事。代码在 n<1000 时会主动打警告。

**KID 依赖 subset_size。** n=200 与 n=1000 的 KID 不是同一个数。
故 P0 的 KIDp **只用 A/B 差值**，绝对值到 P1 才谈；`subset_size` 写进输出 json。

#### ④ 这把尺的量程：bootstrap 噪声地板

沿用 §8.9 的纪律 —— 先报量程再报读数。按**图**（不是按裁块）有放回重抽
B 次重算指标，取标准差。按图重抽是关键：同一张图的 10 个裁块彼此相关，
按裁块抽会把地板压低，读数就会显得比实际显著。

> **判据只在差值 > 2× bootstrap 标准差时才算数。** 写死，不许看到数字再商量。

KID/IS 默认 B=100；FID 每次要一个 2048² 的 sqrtm（实测很慢），默认不做，
需要时 `--boot-fid`。

#### ⑤ 已验通的路径（合成图端到端，CPU）

`clean.json`（键是 `alive_idx`）解析 / `--real-split tune|eval` 过滤 /
随机裁块 / 变尺寸按像素预算攒批 / 特征落盘缓存（键含文件指纹+裁块参数）/
bootstrap / `--patch-only` / `--boot-fid`。
指标本身对过三处：`FID(X,X)=0`、FID 收敛到闭式 `‖μ‖²`、
`IS(确定且均匀,k=8)=8.0` 与 `IS(无信息)=1.0`。

**纪律**：`tune` split 开发用，`eval` split **只在最后出表时用一次**。

### 10.12 尺子体检的三个结果 + **判据一重写**（2026-08-15，n=120 tune 集实测）

在 `laion_hi`（tune split 的 120 张 ScaleDiff 4096²，全部到位无 OOM）上
空跑打分器，目的只是体检，不是实验。三个结果，两个是我的错。

#### ① 尺子准：KIDp 0.0078 vs ScaleDiff 发表的 0.0080

差 2.5%。不同 LAION 子集、不同真图参考集、不同卡、n=120 而非 1000。
这是管线复现了他们协议的强信号。

**连带订正一处我自己的错话**：我原先写"n=200 与 n=1000 的 KID 不是一个数"。
KID 是**无偏 U 统计量**，期望对任意 m≥2 都等于总体 MMD²，
`subset_size` 只影响方差不影响期望。所以绝对值的不可比性只剩
"数据池不同"这一条（§7.1.7 末），与 subset 大小无关 —— 上面那个 2.5% 的
对照因此比我原先说的更有分量。

#### ② 我的实现缺陷：IS 切 split 前没打乱（已修）

实测 ISp = 11.04，发表值 20.41，差 1.85 倍 —— 不是噪声。
机制：特征数组按图顺序排（第 0 张的 10 个裁块、第 1 张的 10 个……），
1200 行切 10 份，**每份只含 12 张不同的图**，分母 p(y) 由 12 个场景估出。

合成验证（120 张图各占一类、每图 10 裁块）：

    不打乱 -> IS = 12.00   （恰好等于每份的图数，机制确认）
    打乱   -> IS = 67.32

torch-fidelity 官方 ISC 的 `samples_shuffle` 默认就是 True，我漏了。
已改为固定种子的确定性打乱（种子固定 -> bootstrap 各次共模，不污染地板）。

**修后复测：ISp 11.04 -> 15.09**，发表值 20.41，仍差 26%。
这个残差**有理由，不是第二个 bug**：

> **IS 与 KID 相反，它不是 n 无关量。** KID 是无偏 U 统计量（见 ①），
> 而 IS 的分母 p(y) 由每一份 split 内的样本估出，份内样本越少估得越窄，
> IS 被系统性压低。合成验证已经量到这个性质：120 个真实不同的类、
> 每份 120 个样本，真值 120 却只读出 **67**。
>
> · 发表值：1000 prompt × 10 裁块 = 10000 样本切 10 份 -> **每份 1000**
> · 我们 n=120：1200 样本切 10 份 -> **每份 120**

**因此定死一条：ISp 只在同一次运行内做臂间比较，绝不与发表值对表。**
唯一的例外是 P1（n=1000，每份正好 1000，与发表口径一致）。
`--is-sweep` 把这个依赖直接扫出来存证。

**这条也让 ISp 出闸门的决定更稳**：修后 σ(ISp) 从 0.3377 升到 0.5707
（IS 变大，绝对 σ 随之变大），要看见发表的 0.44 缺口需要 **n>1615**。
ISp 留作佐证列，闸门只看 KIDp —— 这一条在看到数字之前就定了，不改。

#### ③ 判据一按现在的样子跑不出结论 —— 重写

实测 σ(KIDp) = ±0.0006 @ n=120，σ(ISp) = ±0.3377 @ n=120。
按 √n 缩放并保守取 σ_diff ≈ √2 σ：

| | 发表缺口 | n=200 的 2σ_diff | 结论 |
|---|---|---|---|
| KIDp | 0.0011 | 0.0013 | **看不见**；要 n≈286 |
| ISp | 0.44 | 0.68 | **看不见**；要 n≈565 |

而我原来的阈值 0.0005 更是远在噪声里。两个错：
**(a) 阈值低于分辨力**；**(b) 把"看不见"写成"判死"**，混淆无效应与欠功效。

**重写后的判据（写死，以此为准）**：

> 令 g = **配对**差值，σ_d = **配对** bootstrap 标准差。
> - `g` 方向对且 `|g| > 2σ_d` → **✅ 缺口为真**，进 P1；
> - `|g| ≤ 2σ_d` → **… 不确定。这不是判死。** 同时报出所需
>   `n = 当前 n × (2σ_d/|g|)²`，再决定加不加样本；
> - `g` 反向且 `|g| > 2σ_d` → **❌ 该机制比基准更差**，方向判死。

判据自校准，n 不再靠猜 —— n 只决定"落进不确定区的概率"。
**不做 optional stopping**（边跑边看、达标就停会抬高假阳性）。

**为什么必须配对**：三个臂跑同一批 prompt、同一批种子。各臂单独 bootstrap
再假设独立，等于把"抽到哪些 prompt"这层共享方差算了两遍，地板虚高 ——
代价要么是白烧几十 GPU 小时凑样本，要么是把真差异判成噪声。
`paired_boot()` 用同一组图下标同时喂两个臂，这层自然抵消。

**P0 的 n 定为 300**（比原计划的 200 多约 9 GPU 小时），
理由是让"落进不确定区"的概率降下来，而不是为了满足某个阈值。
ISp 在 n=300 上大概率仍不确定 —— 因此 **ISp 降为佐证列，不进闸门**，
闸门只看 KIDp。这一条也写在跑之前。

#### 顺带确认的两件事

- **打分成本可忽略**：约 1 s/张（4096² 解码 0.54s + 裁块 0.45s）。
  P0 三臂 300 张 = 15 分钟，P1 一臂 1000 张 = 17 分钟。瓶颈 100% 在生成侧。
- **正式跑一律显式 `--res 4096`**：`--res` 缺省时 `arm_files` 取每 idx 的
  最大分辨率，某张若在 4096 上 OOM 只剩 2048 就会被静默混进同一个臂。

### 10.13 P0 三臂实现落地（2026-08-15，`attn_variants.py` + `p0_run.py`）

#### 三个臂

| 臂 | 是什么 | 论文时长 |
|---|---|---|
| A `npa` | ScaleDiff 评测配置（附录 B.1 那个开关**没开**，与他们一致） | 113s |
| B `shift` | NPA + Query Window Random Shifting（他们写了、没用、代码里没实现） | ~113s |
| C `md` | MultiDiffusion 注意力（他们消融里七列全赢、只输时间那行） | 239s |

**基线文件一个字不动**（BASELINE_PATCHES.md 的纪律）。
管线在 `__call__` 内部调模块级的 `register_attention_control`，换臂就是
换这个函数指针。

#### shifting 的实现按附录 B.1 的字面

padding 总量 = `p2 = ws//2`，上侧 `top`、下侧 `p2-top`，左右同理；
query 块从 nh×nw 变成 (nh+1)×(nw+1)；第 (i,j) 块在原始坐标的起点
`i*p2 − top`（可为负），K/V 窗口起点 `clamp(起点 − p4, 0, H − p1)`，
与基线同一条 clamp。偏移**每层独立重采样**。
基线用 Python 双循环建索引，shifting 每层每步都要重建，故**向量化**重写。

#### 自检六条，全过（`--selftest`，零 GPU）

1. 向量化 kv_view **逐元素等于**基线的 Python 双循环（3 组尺寸）
2. `shift(0,0)` 的左上 nh×nw 块 == 无偏移版
3. pad→unpad 恒等（4 组偏移，`atol=0`）
4. MD 窗口数 = (2s−1)²，与他们 Table 1 的 FLOPs 行对上
5. MD 重叠平均把常数场原样还原（`max|err| = 0`）
6. **`shift(0,0)` 的输出与基线 NPA 逐字节相同** ← 最要紧的一条

第 6 条覆盖的不只是索引，还有重写的整个前向：少写一次 norm、写错一次
transpose，B/C 两个臂就会被悄悄污染而从数字上看不出来。
这与 v1 时期"门关时同管线逐字节不动"是同一条纪律。

#### 两个刻意的设计

**① 按 prompt 交错，不逐臂跑。** 三十几小时挂在共享服务器上，中途死是
常态。逐臂跑，死在第 20 小时就只有一个完整的臂和两个空目录 —— 一无所有。
交错跑，死在任何时刻都有前 K 条的**三臂齐全**数据，配对比较当场可做。

**② prompt 用 tune split 的随机 300 条，不用 `laion_hi` 那 120 条。**
那 120 条是按空视野比例分层抽的，为"物体重复"这个**罕见**失效设计；
现在量的是**每张图都有**的局部保真度，要无偏随机样本。
名单抽一次写盘（`plan.json`），三臂与每次续跑一律读盘。
`eval` split 一次不碰。

#### 复现性

- 全局 RNG 每张图重钉（放大阶段的 `randn_like` 没传 generator）；
- shifting 的偏移用 `seed*1000003 + idx` 生成确定性 generator，
  同一 idx 的偏移序列在重跑之间不变。

### 10.14 P0 开跑 + 开跑后翻出的四条（2026-08-15）

自检六条在服务器上全过（含第 6 条逐字节）。名单 300 条已写盘。
第一张实测 **75s / 11.9GB**（论文 A6000 上 NPA 是 113s）—— 我们的卡更快，
显存也宽裕。开跑后又翻了一遍还没考虑到的，四条：

#### ① 磁盘：约 41 GB（**开跑前没算，是疏漏**）

900 次生成 × 每次三张（4096²≈35MB + 2048²≈9MB + 1024²≈2MB ≈ 46MB）。
余量不足就要加"只留 4096"的开关（打分只用 4096，可省约 24%）。

#### ② 三臂真装上了没有 —— 自检覆盖不到的那一段

`--selftest` 验的是索引与前向的数学，**没验真管线里的注册路径**。
三重验证（前三个任务落地后即可做，零成本）：

| 检查 | 应当 | 若不然 |
|---|---|---|
| `[md]` 单张时长 | ≈150–170s（NPA 的 2.1×） | 也是 ~75s -> MD 没装上，立刻停 |
| 三臂的 **1024** 图 md5 | **完全相同** | 不同 -> 全局 RNG 没钉住，配对基础塌了 |
| 三臂的 **4096** 图 md5 | **互不相同** | npa==shift -> shift 臂在空转 |

1024 那条为什么成立：管线在基础阶段 `attnController.disable()`（源码
第 504 行），三臂在 1024 上走同一条标准注意力，**必须**逐字节一致。
这是白捡的决定性自检。

#### ③ 裁块位置的对齐原本是"碰巧"的（已修）

`extract` 里裁块 RNG 原按**文件列表顺序**消费。无 OOM 时三臂图数、顺序
相同，裁块位置碰巧一致（正是我们要的共同随机数）；**可是某臂一旦 OOM
跳一张，它之后所有图的裁块位置就与另两臂错开** —— 不产生偏差，却恰好
在最需要配对的时候悄悄削弱配对，且看不出来。
改为**按 idx 定种子**（`cseed*1000003 + idx`，从文件名取 idx），
与文件顺序无关。缓存键加 `v2` 使旧缓存自动失效。

#### ④ 两条已知的、可接受的不完美（记下来，别以后当新发现）

- **bootstrap 里的 KID 有轻微上偏**：按图有放回重抽会带来重复行，
  重复样本让 kxx 的非对角项混入 k(x,x)。两臂同样处理、取差值时大部分
  抵消，故对**地板估计**可接受；**点估计走全量、无重复**，不受影响。
- **MD 臂的 `index_add_` 在 CUDA 上走原子加，不是逐位可复现**。
  对 C 臂（我们复现的对照，不是我们的方法）可以接受；若将来 md 要当
  主结果，须换确定性 scatter。

#### 顺带修正一处预期

`ISp 不进闸门`的理由是**功效**，不是不可比 —— P0 三臂都是 n=300、
每份 300，内部完全可比。而那个"要 n>1615"是**未配对**的保守估计，
配对后 ISp 很可能够用。**预注册不改**（看到数字前定的不改），
配对表照常给出 ISp 的判，赢了算白捡。

另加一道防线：各臂样本数不等时（某臂 OOM）**逐臂表的 ISp 不可横比**，
现在会主动打警告；配对表天然免疫（只取共同 idx）。

### 10.15 **订正：`md` 臂不是 MultiDiffusion**（2026-08-15，开跑 11 张后由时长抓出）

实测中位数：`npa 75s / shift 78s / md 85s`，峰值显存 11.9 / 12.0 / 12.0 GB。
**md 只贵 13%，而论文是 239/113 = 2.11×。**

#### 错在哪

我漏读了他们 Table 1（FLOPs）：

| Method | Linear | Conv | Cross-Attn | Self-Attn |
|---|---|---|---|---|
| Base | s²hwd² | s²hwk²d² | s²hwld | s⁴h²w²d |
| **MultiDiffusion** | **(2s−1)²hwd²** | **(2s−1)²hwk²d²** | **(2s−1)²hwld** | (2s−1)²h²w²d |
| NPA | s²hwd² | s²hwk²d² | s²hwld | s²h²w²d |

**MultiDiffusion 的 Linear / Conv / Cross-Attn 三列全按 (2s−1)² 缩放** ——
它把**整个 UNet 在每个重叠 patch 上各跑一遍**再平均。原文讲 NPA 卖点时
写得很清楚：*"eliminates redundant computations caused by overlapping
patches, thereby keeping the computational cost of non-self-attention
layers unchanged"*。我只换了注意力处理器，卷积/线性仍是一次全图前向。

#### 那这个臂到底是什么

「MultiDiffusion 的**注意力模式** + NPA 的其余部分」。还有一处性质要记牢
—— 它给每个 query 的上下文**比 NPA 更少**：

| 臂 | query 块 | 每 query 可见 KV | 重叠平均 |
|---|---|---|---|
| A `npa` | 1024 token，不重叠 | 4096（**4× 上下文**） | 无 |
| B `shift` | 同上，网格逐层随机平移 | 4096（4×） | 跨层 |
| C 现`md` | 4096 token，步长半窗 | 4096（**1× 上下文**） | 层内 |

所以它既不是 MultiDiffusion，也不是"上下文更多"那一端；
**它测的是「重叠平均」单独值多少钱**（+13%）。
目录名沿用 `md`（改名会断续跑），报表一律标 `ovl-attn`。

#### 判据重写（旧的作废，不给同一批数字事后编新解释）

原先 `g = C−A` 定义为"缺口总量"、锚在他们发表的 0.0011 上，
`b/g ≥0.6 住边界 / ≤0.3 住上下文` —— **锚没了，整套作废。**

> **C(ovl-attn) vs A**：配对 ΔKIDp < −2σ_d
> → 局部保真缺口真实存在，且**重叠平均**这条便宜路子（+13%）就能吃到；
>   方向成立，直接进方法设计，**不必补真 MultiDiffusion 臂**。
> → 否则：歧义（要么没缺口，要么非得靠"整个 UNet 逐 patch 重算"才吃得到）。
>   **这时才补真 MD 臂**（约 13 h）区分这两种可能。
>
> **B(shift) vs A**：独立读，他们那个没启用的开关本身有没有效。
> 这一条不受本次错误影响。

#### 不停跑的理由

A、B 两臂完全正确、无论如何都要，占 2/3 机时；C 虽名不副实，却是个
**本身有意义的廉价变体**，甚至可能直接就是候选方法。
另：**主表（P1）本来就不需要 MultiDiffusion** —— 它是消融表 Table 3 的
一行，不是 Table 2 的对手；主表对手引用其发表值。

#### 一条失败的检查，记下来

我曾提议用**峰值显存**判断 md 有没有装上 —— **没用**。峰值由 UNet 别处的
激活决定（三臂 11.9~12.0GB 几乎一样），注意力的瞬时张量淹在里面。
**抓到问题的是单张时长。** 下次验"某个算子是否真的换了"，
优先看时长或直接打印张量形状，不要看峰值显存。

### 10.16 P0 中途读数（n≈40）+ 加第四臂 `ctx`（2026-08-15）

#### 中途表（三臂，n=40/40/41）

| 臂 | KIDp | 配对 ΔKIDp | 配对 2σ_d | 判 |
|---|---|---|---|---|
| npa | 0.0084 | — | — | 基准 |
| shift | 0.0085 | **+0.0001** | 0.0006 | …不定（方向还是反的） |
| ovl-attn | 0.0080 | **−0.0002** | 0.0007 | …不定 |

#### ① 配对 bootstrap 的收益是实的

| | 2σ |
|---|---|
| 各臂单独（未配对，按 √2 推） | 0.0045 |
| **配对实测** | **0.0006–0.0007** |

**地板收窄约 7 倍。** 外推 n=300：2σ_d ≈ 0.00026 —— 对发表的 0.0011 缺口
有 4 倍余量，**P0 的样本量设计成立**。

#### ② 尺子第三次对上

KIDp 0.0080–0.0085 vs 发表 0.0080，这次用的是 **tune 的随机样本**
（前两次是分层 120 条，读 0.0078）。换抽样方式仍落在同一处。
ISp 10.5 也与 §10.12 的 split 大小依赖一致（n=40 -> 每份 40）。

#### ③ 41/41/40 不是 OOM

任务顺序 `(idx,npa)→(idx,shift)→(idx,md)`，md 永远比另两臂少 0 或 1 张。
交错设计的正常表现。

#### ④ 效应很小，而且 shift 方向是反的

ovl-attn 点估计 −0.0002，95% CI [−0.0009, +0.0005]，
**发表的 −0.0011 落在 CI 之外（2.6σ）**。n=40 单次抽样只算苗头，
但第一个信号是：**重叠平均这条便宜路子大概率吃不到 MultiDiffusion 的缺口。**

**不要读那个 "n≈563"**：`n_required ∝ 1/g²`，而 g 在 n=40 上主要是噪声
（真值 −0.0006 -> 要 65；−0.0002 -> 要 563；0 -> 要 ∞）。
我上一轮把它称作"最有用的输出"，说过头了。

#### ⑤ **最要紧的一条：三个臂没有一个在测我们真正的论点**

论点是"每个 query 看不见足够的上下文"。而：

| 臂 | 改了什么 | 测到上下文了吗 |
|---|---|---|
| shift | 窗口位置逐层随机 | ❌ 只测**边界伪影** |
| ovl-attn | query 重叠 + 层内平均 | ❌ **混淆** —— 上下文反而更少（1× vs 4×） |

`ovl-attn` 的零结果无法区分"上下文不重要"与"上下文变少的害处抵消了重叠
平均的好处"。**这是设计上的漏，不是数据的问题。**

#### 第四臂 `ctx`：唯一的单变量上下文探针

**保持 NPA 的一切不变，只把 K/V 窗口放大。** `ctx<N>` 的 N 以半窗 p2 为
单位（p2 = ws//2 = query 块边长）：

| 配置 | K/V 边长(ws=64) | 每 query 可见 | 面积 | 估计时长 |
|---|---|---|---|---|
| npa / ctx2 | 64 | 4096 | 1.00× | 75s |
| ctx3 | 96 | 9216 | 2.25× | ~81s（显存退档用） |
| **ctx4（默认）** | **128** | **16384** | **4.00×** | **~95s** |

便宜的原因：自注意力只占总时长约 6.5%（由 ovl-attn 的 1.13× 反推）。

**判据（写在 ctx 跑出任何数字之前）**：
> 配对 ΔKIDp < −2σ_d → **上下文缺失是真病因**，方法沿这条设计；
> n≥300 仍在噪声内 → **上下文假设被正面否掉**，退回"分解的代价小到测不出"，
>   §10 整条方向按判死处理，转 B 计划（评测方向）；
> 配对 ΔKIDp > +2σ_d → 上下文更多反而更差，同样判死，但这本身是个值得
>   报告的反直觉结果。

自检加到九条，新增：
⑦ `ctx(kv=ws)` 索引 == 基线（3 组尺寸）；
⑧ **`ctx2` 输出与基线 NPA 逐字节相同**；
⑨ 各档 K/V token 数确实随 N 变（防 N 被静默忽略）。

**显存提醒**：ctx4 在 4096² 最大层要 gather 64×16384 个 K/V token，
估计比 npa 多约 4GB（峰值 11.9 → ~16GB）。第一条 `[ctx]` 日志的 peak_gb
若逼近 20GB，退到 `--ctx-units 3`。

### 10.17 `p0_rep.py`：把"两个失效是一个病"接成可判的实验（2026-08-15）

§10.10 第三层是这篇论文能不能从"更好的模块"升到"我们搞清楚了分块范式的
税"的分界线，但它至今**零个直接实验**。p0 的图正在生成，所以这条**不额外
花生成算力**，只花计数时间。

**可证伪判据（写死）**：一个只改窗口几何的机制，是否**同时**动 KIDp 和 Rep⁺。

> 同时动 → 假设升级为证据，正好补上"效应量小"这个短板：headline 不再是
> "KIDp 改善千分之二"，而是"我们指出分块范式的税，并用一个机制同时消掉
> 它的两个症状"。
> 只动一个 → **第三层作废**，退回一二层，重复那半边降为附录。
> Dmg⁻ 变差超 2σ → 该机制修重复的同时破坏主体，与 §3.14e 的强度旋钮同型，不要。

#### 为什么不直接用 `vlm_count.py --armdelta`（三处对不上）

1. 它要一个目录一份含 `arm` 的 manifest；p0 是**四目录四份**。
2. 它用**按目录**的主体缓存 —— 正是 base_audit 踩过的漂移
   （157 在一处 'person'(6)、另一处 'team'(2)）。`do_delta` 后来改成全局，
   **`do_armdelta` 没跟上**。新脚本一律走全局缓存。
3. 它对每个臂都数一遍基图。而 p0 的 1024 基图**四臂逐字节相同**
   （md5 已验，管线基础阶段 `disable()` 注意力），基图只需数一次 —— 省 3/4。

#### **先过的那一关：LAION caption 抽不抽得出可数主体**

`vlm_count.py` 文件头明写：*"LAION alt-text 太脏，那批不走 VLM 计数。"*
而 p0 的 prompt 正是 LAION caption。**所以这条线未必走得通，必须先验。**

三档，前一档不过就不进下一档：

| 档 | 成本 | 门槛（写在跑之前） |
|---|---|---|
| `--peek` | **零 GPU** | 可疑 ≤20% 干净；20–40% 边缘；**>40% 当场判死** |
| `--subjects` | ~2 min GPU | **可用主体 ≥60%**，否则判死 |
| （默认计数） | 视 n 而定 | 出 Rep⁺/Dmg⁻ + 对 npa 的配对差 |

启发式标记的脏模式（只标不滤，判断权留人眼，与 `real_audit` 同一纪律）：
太短 / 像素串 `1920x1080` / 商品码 `SKU-AB12/XZ-9` / URL 文件名 /
图库套话 `click to enlarge`、`stock photo` / 全大写。

**判死不是坏结果**：那意味着 §10.10 第三层保持"未验证的假设"，论文只报
patch 三列 —— 弱一档，但省下几天，且不会拿一把用在脏语料上的尺子去撑
一个统一论证。

**VLM 占 ~16GB，与 SDXL 生成抢不了显存** —— 后两档必须等 p0 跑完。

### 10.19 接缝探针 v1 在真实图上失效 -> v2（2026-08-15，同日）

#### v1 的读数与为什么它是伪影

| 臂 | C_pred | C_max | 峰值命中率 |
|---|---|---|---|
| npa | 0.0589 | **0.3853** | 8% |
| shift | 0.0719 | 0.4096 | 4% |
| ovl-attn | 0.0627 | 0.3801 | 0% |
| ctx | 0.0562 | 0.3882 | 4% |

合成阴性对照上 C_max = 0.016。真实图上四臂全是 **0.38~0.41**，二十几倍，
**而峰值位置随机**（命中率 0~8%，纯偶然 0.4%）。

病因：**真实照片非平稳** —— 画面中间有主体，中间那几列梯度系统性高；
折叠只平均 8 个位置，压不住慢变趋势。于是 C_max 巨大但落在内容决定的
随机相位上，`C_pred` 的分母（中位数）也失去意义。

> **这是我的对照设计缺陷**：只做了"合成图 ± 接缝"，
> **漏了"非平稳 + 无接缝"这一档** —— 而恰恰是它会暴露该失效模式。
> **教训写死：阴性对照必须覆盖真实数据的主要干扰源，不能只用干净合成图。**

中途还试过频域谐波梳，也不行：窗口里只有 8 个周期，频率分辨率太差，
谱线被低频内容淹掉，连 sd=8 的注入接缝都测不出。已弃，记档免得再走。

#### v2 与它的四档对照

1. 折叠前用移动中位数（窗宽 3×period）**去趋势**；
2. 统计量换成稳健 z 分数 `(剖面 − median)/(1.4826·MAD)`；
3. 加**多周期扫描** 384/448/480/**512**/544/576/640 —— 不依赖任何假设的
   对照：非平稳性对所有周期一视同仁，真接缝只应在 512 上冒出来。
   这一条还能证伪推导本身（512 若算错，别的周期会亮）。

| 档 | z_pred | z_max | phase | 命中 |
|---|---|---|---|---|
| ① 平稳，无接缝 | −0.61 | 2.61 | 163 | ✗ |
| ② 平稳，接缝 sd=8 | 5.32 | 5.32 | **511** | ✓ |
| ③ **非平稳，无接缝** | **−0.99** | 2.92 | 400 | ✗ |
| ④ 非平稳，接缝 sd=8 | **17.36** | 17.36 | **511** | ✓ |

多周期扫描（④ 行）：384→3.31、448→3.07、480→0.51、**512→17.62**、
544→−0.75、576→0.18、640→2.32。**只有 512 亮，且高出 5 倍。**

#### 判读（写死）

> **先看 npa 的"512 突出度"**（512 的 z − 其余周期的中位数）：
> ≤1.0 → 512 不特殊 = NPA 上**没有可测的 query 网格接缝**。
>   "边界伪影"当场否掉，§10.9（他们那个没启用的开关）降级为无关紧要，
>   `shift` 臂失去意义。
> \>1.0 → 接缝存在且位置与推导一致，再看三条预测：
>   `shift` 应消失 / `ovl-attn` 应减弱 / `ctx` 应仍在（只放大 K/V）。
>   全中 → 分解的代价被拆成两个**可分离**机制，各有对症干预。
>   不成模式 → 我对机制的理解错了，**重新想，不许事后编解释**。

### 10.20 **接缝假设判死**：NPA 输出上没有可测的 query 网格接缝（2026-08-15）

`seam_probe.py` v2（四档对照全过），12 张四臂共同 idx：

| 臂 | 384 | 448 | 480 | **512** | 544 | 576 | 640 | 峰值命中 |
|---|---|---|---|---|---|---|---|---|
| npa | 0.08 | −0.44 | −0.25 | **0.28** | **0.49** | −0.28 | −0.28 | 0% |
| shift | 0.27 | −0.38 | −0.13 | 0.42 | 0.55 | −0.23 | −0.21 | 4% |
| ovl-attn | 0.30 | −0.39 | −0.19 | 0.29 | 0.52 | −0.13 | −0.36 | 0% |
| ctx | 0.30 | −0.32 | −0.10 | 0.22 | 0.57 | −0.07 | −0.22 | 0% |

**544 比 512 还高。** 所有周期挤在 −0.44~+0.57 同一区间，四臂无差别，
峰值命中率 0~4%（偶然率 1.0%）。突出度 +0.31~+0.60，全部 ≤1.0。

量程对照：阳性控制里真接缝 **z = 17.36**，弱接缝档也有 5.32。实测 0.28。
**不是信号弱，是没有。**

> **预注册判决执行：npa 突出度 +0.55 ≤ 1.0
> -> NPA 输出上不存在可测的 query 网格接缝。**

#### 一个必须写明的限定（但它不救这个假设）

接缝产生在**潜空间**特征图边界；VAE 解码器的卷积跨越该边界，会把锐利的
潜空间不连续抹成几十像素的渐变，而探针找的是单列梯度尖峰。
所以结论的准确措辞是 **"输出图上没有可测的接缝"**，不是"潜空间没有接缝"。

**但这不救假设**：若 VAE 已把它抹到既测不出也看不出，它就不构成画质问题
—— 我们要修的失效必须存在于输出图上。
**且不再加宽窗口重测** —— 预注册写了 ≤1.0 判死，现在改统计量找信号
就是事后重解释，上一条方法线正是这么死的（§3.14e）。

#### 连带死掉的

- **§10.9 整条线**（他们附录 B.1 那个"写了没启用、代码里也没实现"的
  Query Window Random Shifting）**失去动机** —— 它要修的东西不存在。
  那本来是本方向里我认为最有戏的发现。
- **`shift` 臂失去主要目的**，只剩"实现了他们没启用的开关、测不出差别"
  这点残值，降为附录一句话。

#### 三条独立线索现在指向同一处

| 证据 | 读数 |
|---|---|
| 肉眼（4 张随机 + 裁块） | 看不出 npa 与 ctx 的差别 |
| 接缝探针（12 张，先预测后验证） | 无可测接缝，四臂无差别 |
| KIDp 配对（n=40） | Δ = −0.0002，不显著 |

三条**互相独立**的测量都指向：**窗口怎么切，对输出图的影响非常小。**
尚未判死（闸门是 n=300 的 KIDp），但先验明显下移。

#### 替代假设（可测，且对我们不利）

若窗口切法影响这么小，ScaleDiff 发表的 MD−NPA 缺口（KIDp 0.0011）从哪来？

> **很可能不是来自注意力模式，而是来自真 MultiDiffusion 的冗余计算本身。**
> 他们 Table 1 里 MultiDiffusion 的 Linear/Conv/Cross-Attn 三列全按
> (2s−1)² 缩放 —— 整个 UNet 在每个重叠 patch 上各跑一遍**再平均**。
> 那是一次**集成平均**，而集成本来就会改善 FID/KID 类指标。

若属实，结论干净但不利：**那个缺口买不便宜**，它的来源正是我们想省掉的
那 2.11× 计算。此假设可测（真 MD 臂，约 13 h），而预注册里已写明
"ovl-attn 不显著时才补它" —— 现在看多半会走到那一步。

#### 决定：不改当前的跑

KIDp 闸门是预注册的，n=300 要跑完。**看到不利的中间结果就改计划，
比多花 15 小时更糟。**

### 10.21 选项 2：调度诊断（`sched_probe.py`，2026-08-15）

#### 为什么换到这里看

P0 判死注意力窗口几何后，从我们自己的测量能反推出原因：
**自注意力只占 SDXL UNet 总计算的约 6.5%**（ovl-attn 把注意力 FLOPs 提到
3.06× 而总时长只涨 13%）。93.5% 的网络与窗口划分无关，卷积把影响稀释掉。
**我们把力气全用在了 6.5% 的那部分。**

把他们自己两张消融表换算成 KIDp，杠杆排序：

| 旋钮 | ΔFIDp | **ΔKIDp** | 出处 |
|---|---|---|---|
| **LFM 开/关** | −2.06 | **−0.0007** | Table 3 |
| **τ 300→700** | −1.73 | **−0.0013** | Table 4 |
| 注意力 NPA→MD | −0.81 | −0.0011 | Table 3 |
| SG 开/关 | −0.05 | −0.0001 | Table 3 |
| 我们实测的注意力几何 | ~0 | −0.0002 | P0 |

**SG 对 patch 三列几乎无作用** —— 它治的是重复/全局结构。真正的旋钮是
**LFM 与 τ**。

#### 读代码发现的、比"扫超参"更值钱的事

`pipeline_scalediff_sdxl.py` 里 `refine()` 被调用两次，**共用一个
`scale_factor`（频率分割点）**：

```
latents_LFM = refine(latents_LU, latents_RU, 1,     scale_factor=sf)  # LFM
pred_x0     = refine(latents_LFM, pred_x0, scale=s, scale_factor=sf)  # SG
```

> **两件不同的事共用一个分割点，纯粹是实现上的便利，且从未被消融过。**
> 他们消融了 τ（Table 4）、注意力（Table 3）、LFM/SG 的**开关**，
> **唯独没消融过这个分割点该设多少**，更没试过把两处**解耦**。

两个调用点靠**传参方式**可靠区分（LFM 传位置参数 `1`，SG 传关键字
`scale=`），所以解耦不需要改基线文件，包一层即可。
**已验证**：两点分离、各用各的 sf、且 sf 相同时与原函数**逐字节一致**。

#### 设计（6 个配置 × 60 张 ≈ 7.5 h；基线复用 `p0/npa`）

| 名字 | sf_lfm | sf_sg | restart_ratio |
|---|---|---|---|
| base（复用 p0/npa） | 0.125 | 0.125 | 0.4 |
| lfm_lo / hi / xhi | 0.0625 / 0.25 / 0.5 | 0.125 | 0.4 |
| sg_lo / hi | 0.125 | 0.0625 / 0.5 | 0.4 |
| tau_hi | 0.125 | 0.125 | **0.7** |

`restart_ratio ↔ τ`：`restart_step = int(steps*(1−r))`，50 步 r=0.4 ->
第 30 步 -> τ≈400 ✓ 与他们一致；τ=700 ↔ **r=0.7**。
Table 4 里 τ=700 的 patch 三列最好（FIDp 37.65 / KIDp .0064），
**而他们选了对 patch 更差的 τ=400 因为全局 FID 最好** —— 又一次拿局部换全局。

**免费加成**：管线一次返回 1024/2048/4096，**每张图同时给两个分辨率的
读数**，不花额外算力就能回答"最优分割点会不会随放大倍数移动"。

#### 预注册判据（写在跑之前）

n=60，配对 σ_d ≈ 0.00029（由 P0 实测外推）。

**多重比较修正（初稿漏了，补在看数字之前）**：6 个配置同时对 base 比，
每个用 2σ 判则"至少一个假阳性"的概率 = 1 − 0.95⁶ ≈ **26%** ——
六个旋钮全无效时也有四分之一概率看到假的赢家。Bonferroni 把族错误率
控在 5% -> 单检验用 **2.6σ_d ≈ 0.00074**。

**已知风险**：`tau_hi` 只走 r=0.4→0.7，而 Table 4 的 −0.0013 是 τ=300→700
**全程**；我们约半程，预期 −0.0006~−0.0009，**正好压在门槛上**，且那是
2048² 的数。tau_hi 出"不定"**不代表 τ 无用**，补 r=0.85 一点即可（~1.25 h）。

> **①** 某配置 4096² 上 ΔKIDp < 0 且 |ΔKIDp| > **2.6σ_d**？
>   否 → **所有旋钮都无杠杆，SDXL 上没有 headroom，方向判死**；
>        那时转 DiT 才是有依据的转，不是碰运气。
>   是 → 进 ②
> **②** 2048² 与 4096² 的最优配置是否相同？
>   相同 → 只是"超参没调最优"，**不是论文**，判死。
>   不同 → **自适应频率分割**成立，方法候选，且零额外开销。
> **③** sg_lo/sg_hi 与 base 无差别 → 坐实"SG 与 patch 三列无关"，
>   解耦的价值全在 LFM 一侧。

**判死也是有效结果** —— 它把"SDXL 上还有没有戏"彻底关掉。

### 10.22 旁记：Riemannian 那一类工作是什么（2026-08-15，用户问 OrthoFuse）

**OrthoFuse**（CVPR 2026，Skoltech / Oseledets 组，代码开源
`ControlGenAI/OrthoFuse`）：推导 Group-and-Shuffle 正交矩阵构成的流形结构，
给出测地线的高效近似，实现**首个 training-free 的乘性正交适配器合并**，
外加一个"谱恢复变换"。

**Riemannian 的意思**：参数住在弯曲的流形上而非欧氏空间，因此"平均"不能
走直线。两个旋转矩阵的算术平均已经不是旋转矩阵 —— 它掉出了约束集；
正确做法是沿**测地线**走。常见流形：正交群（适配器）、固定秩（LoRA 天然
就是）、SPD（协方差/风格统计量）、球面（归一化嵌入、扩散 latent）。

**为什么图像领域最近集中出现**：
1. **适配器合并成了刚需** —— 一堆 LoRA 要 training-free 地合并，而朴素权重
   平均会把两个都搞坏，因为掉出了约束集；
2. 它能把"我们换了个平均方式"升级成"我们推导了**正确的**平均方式"，
   有推导、有闭式、有定理形状 —— **在经验增益中等时这是有效的差异化**。

**对本项目**：直接用没有 —— 分辨率外推里没有天然的受约束参数流形，硬套
就是装饰。**值得记的是它的贡献形状**：*大家在做操作 X，X 因结构性质 Y
而是错的，这是正确的 X。*

**一条标为猜测、不作为方向的观察**：扩散 latent 每个时刻近似住在半径由
噪声水平决定的球面上，而 MultiDiffusion/DemoFusion 对重叠 patch 预测做
**算术平均**会缩小范数、降低方差 —— 那本身就是平滑。PixelRush 的 noise
injection 与 ScaleDiff 的 LFM 都在补这个。是否有人改成保范数的球面聚合，
**未查证**；且我们自己的 ovl-attn 数据显示平均效应很小，故不追。

### 10.23 **调度诊断也判死**：SDXL 上六个旋钮零个改善（2026-08-15）

n=60，配对差（负 = 更好），判据 ① 要求 ΔKIDp < 0 **且** |Δ| > 2.6σ_d ≈ 0.00074：

| 配置 | 4096² ΔKIDp | 2σ_d | 2048² ΔKIDp |
|---|---|---|---|
| lfm_lo (sf 0.0625) | −0.0000 | 0.0006 | +0.0000 |
| sg_lo (sf 0.0625) | −0.0001 | 0.0005 | +0.0001 |
| sg_hi (sf 0.5) | +0.0001 | 0.0006 | +0.0001 |
| lfm_hi (sf 0.25) | +0.0005 | 0.0008 | +0.0002 |
| **lfm_xhi (sf 0.5)** | **+0.0015** | 0.0014 | +0.0002 ❌反 |
| **tau_hi (r 0.7)** | **+0.0025** | 0.0025 | +0.0005 |

**唯二为负的是 −0.0000 与 −0.0001，离门槛差一个数量级。2048² 上六个全为正。**

> **判据 ① 失败 -> SDXL 这个 backbone 上没有 headroom，方向判死。**

#### 三条留下的读数

1. **他们的超参在局部最优上。** LFM 分割点扫了 8 倍量程、SG 分割点 8 倍、
   τ 近乎翻倍 —— **没有一个方向能改善**。ScaleDiff 的配置不是随手设的。
2. **判据③确认：SG 的分割点与 patch 三列无关**（±0.0001，8 倍量程全在噪声里），
   坐实其 Table 3 的暗示。**SG 治的是重复与全局结构，不是局部保真。**
3. **LFM 分割点是"平台后上升"**：往上单调变差（0.125→0.25→0.5 给出
   +0.0005→+0.0015），且 **4096 比 2048 差三倍**；往下持平。0.125 正在平台边缘。

#### 一个设计错误，记账

我说"管线一次返回三档，2048 的读数免费" —— **不严谨**：两级放大的 2048 是
**中间产物**，与他们 Table 4 那种一级放大的 2048 **最终输出**不是同一个东西，
**不能与其发表值对表**。（判据②是我们自己两分辨率的内部比较，仍成立，
但①先失败故未激活。）

这也解释了 `tau_hi` 为何与其 Table 4 反向（他们说 τ=700 的 patch 列更好，
我们测出更差）：**两级管线注两次 τ=700 的噪声，与一级注一次不是一回事。**

#### §10 整条弧线的总账

| # | 探测的旋钮 | 结果 |
|---|---|---|
| 1 | 注意力窗口几何（shift / ovl-attn） | 无效应 |
| 2 | 每 query 的上下文（ctx） | **反向变差** |
| 3 | 窗口接缝 | **不存在** |
| 4 | LFM 频率分割点 | 默认已最优 |
| 5 | SG 频率分割点 | 与 patch 列无关 |
| 6 | 噪声时刻 τ | 默认优于 τ=700 |

**六个旋钮，零个改善。** 在这个 backbone 上，ScaleDiff 的配置在我们够得着的
每一根轴上都处于局部最优；它自己 MultiDiffusion 消融行暗示的 headroom
**不能靠任何廉价的重新参数化拿到**。

**这句话是一篇论文的一个消融段落，不是一篇论文。**
按预注册：转 DiT 现在是**有依据的转**。

---

### 10.24 关键词扫描：「training-free / text-to-image / high resolution」这条轴上谁占了什么（2026-08-16）

用户要求：不换方向，先把「过平滑 / 细节」这个子槽的占位情况查清楚。
两个已有 curated list（`littlewhitesea/training-free-methods`、
`GuoLanqing/Awesome-High-Resolution-Diffusion`）都停在 2024、且 venue 标注过期，
下表是逐篇核过 venue 与 code 后的结果。**过基础过滤器（有代码 + 有会议）的才进表。**

#### A. 通过基础过滤器的（venue ✅ + code ✅）

| 论文 | venue | backbone | 核心机制 | 是否踩「过平滑/细节」 |
|---|---|---|---|---|
| MultiDiffusion | ICML'23 | SD | patch 融合 | ✗ |
| ScaleCrafter | ICLR'24 | SD/SDXL | 膨胀卷积改感受野 | ✗（重复/结构） |
| DemoFusion | CVPR'24 | SDXL | 渐进上采 + skip residual + 膨胀采样 | ✗（结构） |
| FreeU | CVPR'24 | SD | **skip / backbone 特征谱重加权** | 半个（1024²，非 HR） |
| FouriScale | ECCV'24 | SDXL | 频域滤卷积核 | ✗（结构） |
| HiDiffusion | ECCV'24 | SD/SDXL | RAU-Net + MSW-MSA | ✗（效率/结构） |
| AccDiffusion | ECCV'24 | SDXL | patch-content-aware prompt | ✗（重复） |
| **DiffuseHigh** | **AAAI'25** | SDXL | **DWT 结构引导（LL 带来自低分图）** | 半个 |
| **ResMaster** | **AAAI'25** | SDXL | LR 结构引导 + **细粒度引导（频域 + patch prompt）** | **✅ 明写 over-smoothed** |
| **FreeScale** | **ICCV'25** | SDXL | 自级联上采 + **region-aware detail control** + **scale fusion** | **✅** |
| **HiFlow** | **NeurIPS'25** | FLUX | flow-aligned guidance（低频对齐 + **高频方向**） | **✅（flow backbone）** |
| **HiWave** | **SIGGRAPH Asia'25** | SDXL | patch DDIM inversion + **wavelet detail enhancer** | **✅ 正中** |
| ScaleDiff | NeurIPS'25 | SDXL/FLUX | NPA + LFM + SG | 半个（我们的基座） |
| FAM | CVPR'25 | SDXL | frequency-aware | 半个 |

#### B. 不过滤器的（无代码 和/或 无 venue，只能当背景读）

PixelRush（arXiv 2602，无码）、AP-LDM（有码，OpenReview 未见接收）、
PBC/position-encoding（arXiv 2503.09830，comments 写「Submitted to ICML 2025」，无码）、
**InfoScale**（arXiv 2509.01421，无码：渐进频率补偿 + 自适应信息聚合 + 噪声适配）、
**RectifiedHR**（arXiv 2503.02537，v4 到 2026-04，无 venue：**平均隐能量分析 + energy decay 导致模糊 + noise refresh + CFG 调**）、
MegaFusion、HiPrompt、HiCache、FreCaS、CutDiffusion、ElasticDiffusion。

#### 结论一：这是全场**最拥挤**的一个子槽

「把高频加回去」的每一种可实现方式都已经有人认领，而且都是 2025 的会议论文：

| 加高频的方式 | 谁占了 |
|---|---|
| 小波域高频注入 | HiWave（SIGGRAPH Asia'25） |
| DWT 低频保结构 | DiffuseHigh（AAAI'25）、ScaleDiff LFM |
| 频域滤波 | FouriScale（ECCV'24） |
| 低分参考图的细粒度引导 | ResMaster（AAAI'25） |
| 区域自适应的细节控制 | FreeScale（ICCV'25） |
| 全局/局部注意力的频带融合 | FreeScale（ICCV'25） |
| flow 模型的高频方向引导 | HiFlow（NeurIPS'25） |
| 能量衰减 + CFG | RectifiedHR（arXiv） |
| 上采样丢失的频率补偿 | InfoScale（arXiv） |

#### 结论二：FreeScale 的 scale fusion **反过来解释了我们 P0 的 `ctx` 臂**

FreeScale 的 self-attention 层：**用高斯模糊把 global attention 的高频**和
**local attention 的低频**拼起来。也就是说 —— 全局注意力**只有高频那一半可用**，
低频那一半是有害的。

这与我们 §10.16 / §10.18 的实测**完全一致**：`ctx` 臂（整块地扩大 K/V 上下文）
ΔKIDp **+0.0006**（变差）、ΔISp **−0.67**（变差）；ScaleDiff 自己 Table 3 的
Base 行（全局注意力 = 上下文最大）在三列 patch 指标上全是最差。

我们当时把 `ctx` 判为「反向」，判对了；现在知道**为什么**反向，
也知道**正确的用法已经被 ICCV'25 拿走了**。

#### 结论三：五个认领者没有一个做了「病因定位」——但这不构成我们的机会

ResMaster / FreeScale / HiWave / HiFlow / DiffuseHigh 的共同形状是：
**断言 HR 输出缺高频 → 从外部（小波 / 傅里叶 / 低分图 / 全局注意力）把高频搬进来。**
没有一篇指出高频**在哪一步被销毁**。最接近的是 RectifiedHR 的 average latent
energy 分析，而它至今无 venue。

诱人的推论是「那我们去定位病因」。**但这条推论要在 §10 的总账面前作废**：
我们已经在这个 backbone 上试了六个旋钮，零个改善（§10.23）。
其中注意力那三个只覆盖 SDXL UNet 的 **6.5% 计算量**，调度那三个不改网络。
如果病因在剩下的 93.5%（卷积栈 / VAE 解码），那么：

* 它与我们跑过的所有阴性结果**一致**；
* 但**能改它的训练无关旋钮，正是 ScaleCrafter（ICLR'24）和 FouriScale（ECCV'24）
  已经占掉的那两个**（膨胀卷积、频域滤核）；
* 剩下唯一没人碰的是 **VAE 解码路径**（tiled decode 的重叠混合本身是一个低通）
  和 **FreeU 式谱重加权的分辨率自适应版本**。

这是**一个消融段落的体量**，不是一篇 CVPR。

#### 判决

**SDXL + 过平滑/细节 = 关闭。** 两个独立理由，各自单独就足够：

1. **机制空间已被瓜分**：四篇 2025 会议论文（AAAI / ICCV / NeurIPS / SIGGRAPH Asia）
   带开源代码，全部明写这个问题。要成为新 SOTA，标准表上要同时压过
   FreeScale、ScaleDiff、AccDiffusion 三家。
2. **我们自己的读数说这个 backbone 上没有空间**：六个旋钮零个改善，
   最好的一个负值 −0.0001 对阈值 0.00074。

另记一条**证据可行性**约束：HiWave 的主要证据是 user study（>80% 偏好）。
我们没有标注池（硬约束），这条证据形式对我们关闭。
也就是说即使找到一个真的改善，我们只能靠标准表 + patch 三列去证明它 ——
而标准表恰恰是 §10.1 里那个「3% 患病率推不动」的表。

**方向仍然指向 §10.23 结尾那句：转 DiT 现在是有依据的转。**
若坚持留在过平滑轴上，唯一还没被认领的落点是 **flow/DiT 上的病因定位**
（HiFlow 只做了引导，没做定位；DyPE / UltraImage / Scale-DiT 走的是位置外推，
不是频率），代价是 FLUX 在 24GB 上必须 fp8，需要先做可行性验证。

---

## §11 新方向候选：训练无关的属性绑定（2026-08-16）

### 11.1 为什么换到这条轴

§10 判死的是「SDXL + 过平滑/细节」。换轴的依据不是"换个新鲜的"，
而是两条筛子的重新应用：

**筛子一（§10.1 的患病率规矩）。** 重复伪影 3% 患病率推不动 1000 图平均，
这是 AccDiffusion 输 FID 的原因。属性错绑不同 —— 只要 prompt 里有两个带
修饰的名词，失败就是常态。

**筛子二（新加的，算力）。** 这条我们两个月里一直没算进账：
4096² 每张 ~100 s / 17 GB，一个臂 n=60 要 7 h，而噪声底其实要 n≈300。
1024² 每张 ~5.5 s。**实验吞吐差 20 倍。** 这决定了预注册判死的成本从
"一周"降到"一下午"，我们能打十枪而不是一枪。§10 三个探针每一个都拖成
周级、最后都停在"差值小于噪声"，根子在这里。

### 11.2 问题陈述

组合性线与引导线的能力是**错位**的：

| 线 | 问题 | 手段 | 缺口 |
|---|---|---|---|
| 组合性（A&E / SynGen / CONFORM / INITNO / ToMe） | 对 | **测试期梯度优化**，贵、不稳、逐图 | — |
| 引导（PAG / SEG / CFG++ / NAG / TPG / HeadHunter） | 泛画质 | 单次额外前向，无梯度 | **没有一篇把目标定在绑定上** |

> **研究问题**：能否构造一个"语义定向的负分支" —— 它相对于原模型的唯一
> 差别是属性-名词绑定被破坏 —— 使 CFG 式外推自动产生一个按构造指向正确
> 绑定的引导方向，从而在**不做任何测试期优化**的前提下修复属性错绑？

技术内核在**扰动的选择性**：外推推的是差向量 `x_neg + w(x_pos − x_neg)`。
负分支若除绑定外还坏了别的（结构、色彩分布、构图），差向量里就混进无关
成分，外推会在那些维度过冲。PAG 换单位阵是**无差别破坏**，能work是因为
"泛泛变好"不需要精确。我们要的方向精确得多，所以
**"如何构造一个只在单一语义轴上退化、其余轴与原模型尽量一致的分支"
本身就是非平凡问题** —— 这个选择性是可定义、可测量的，判据本身即贡献。

### 11.3 占位调研：不空，缺口真实但窄

**空格在 `optimization-free × guidance-form × binding-targeted`。**
组合性线七篇里六篇要优化（ToMe/NeurIPS'24 也要，两个辅助损失迭代更新
composite token）；引导线没有一篇瞄准绑定。

**但必须记下的风险 —— HeadHunter（NeurIPS'25）吃掉了问题陈述的一半：**

| 子问题 | 状态 |
|---|---|
| Q1 在哪扰动 | **HeadHunter 做了**（头级粒度，不同头管不同视觉概念） |
| Q3 强度怎么给 | **SoftPAG 做了**（向单位阵连续插值的旋钮） |
| Q2 怎样只坏一个轴 | **没人回答** |

HeadHunter 选头靠**贪心搜索 + 目标打分**，不是构造，且轴是
style/structure/texture 不是语义绑定，也没在 T2I-CompBench 上评。
所以新颖性全部重量压在 Q2：给出**有原理的构造**并证明其选择性。
审稿人会问"这不就是 HeadHunter 换个轴"——这个问题必须有硬答案。

**新颖性边际比 §11.2 初稿描述的窄。此处订正我自己。**

### 11.4 与 §10 那条轴横比

| | 过平滑/细节 | 组合性绑定 |
|---|---|---|
| 机制空间 | 一维（加高频），5 篇占满 | 结构化（谁绑给谁） |
| 正面竞争 | 4 篇 2025 会议+开源，全部直接认领 | venue 主线在 2023–24 且全靠优化；2025–26 是 arXiv 尾巴 |
| 已知余量 | 实测六旋钮零改善 | SDXL color .6369 / shape .5408 / texture .5637，SOTA +2–10 pp |
| 每实验 | 7 h/臂 | 2.5 h/臂 |
| 证据形式 | 圈内惯例要 user study（我们没有） | 全自动 |

**余量 2–10 pp 对 ~1 pp 量级的噪声底**，与 4096² 上"最好负值 −0.0001 对
阈值 0.00074"是完全相反的局面。

### 11.5 代码基础

| 侧 | 选择 | 理由 |
|---|---|---|
| 打分 | 官方 `Karine-Huang/T2I-CompBench` | 读数②是 `published_SOTA − our_baseline`，**只有打分器逐字节相同才成立**（`std_table.py` 锁特征塔同理） |
| 生成 | 裸 `diffusers` 0.39.0 | PAG 已是官方 pipeline（`StableDiffusionXLPAGPipeline` / `pag_applied_layers`），方法阶段对照臂零成本；`attn_variants.py` 的 processor 替换手艺直接搬 |
| 不用 | A&E / ToMe 仓库 | SD1.5 时代 + 优化范式，与"不做优化"的主张相反 |

### 11.6 存在性检查（`comp_probe/exist_check.py`）

**只跑基线，不做方法。先量问题再设计方法 —— §10 把这个顺序做反过一次。**

两个读数：**① 患病率**（逐 (prompt,seed) 的 BLIP-VQA 分低于阈值的比例）、
**② 余量**（发表 SOTA − 我们的基线，对上 cluster bootstrap 的 σ）。

预注册判据（写在看数之前）：

* 判死 A：患病率@0.5 < 10%
* 判死 B：余量 < 3σ_boot
* 放行：余量 ≥ 5σ_boot；3σ–5σ 记「边缘」，**不自动放行**

账：300 prompts × 4 seeds = 1200 张 ≈ 1.8 h、1.6 GB。

**两个已固化为 selftest 的静默错误：**

1. **question_id ↔ idx 错位。** 官方按**文件名字典序**编 question_id，
   我们的 idx 是生成顺序，两者不同 —— 直接 reshape 静默错位。
   `--selftest ②` 断言偷懒 reshape 必须 ≠ 真值，否则 ① 是空测试。
2. **bootstrap 必须按 prompt 聚类。** 同 prompt 的 seed 相关（prompt 难易
   是主要方差来源），按图独立重采样**低估 σ**、让余量看起来更显著。
   `--selftest ③` 实测簇内完全相关时 σ_cluster/σ_naive = 1.99×，
   理论值 sqrt(N_SEEDS)=2.00。

**开跑前三个人工阻塞项（`--plan` 会打印）：**

1. `REF` 里的 SDXL 数字是**二手转述**，必须对回论文原文 —— 判据 B 直接
   建立在它上面。
2. `REF[*]['sota']` 还是 `None`，判据 B 现在算不出来。
3. `N_SEEDS=4` 而官方协议是 **10**。两边协议不同 → 读数②**只能当量级
   参考**；若落在边缘带必须补跑到 10 seeds 再判。

**§11 全部内容以存在性检查为前提。判死判据触发即停，不进方法设计。**

### 11.7 第二轮调研：拿到一手表格，两处订正我自己（2026-08-16）

#### 订正一：REF 数字全错，已换成一手来源

上一版 §11 里的 SDXL 基线（color .6369 / shape .5408 / texture .5637）来自
综述页面转述，**是错的**。从 ELDiff（arXiv 2606.20924）Table 2 的 PDF 里
逐行抠出真表：

| Method | Color | Shape | Texture | Spatial | NonSpat | Complex | Latency |
|---|---|---|---|---|---|---|---|
| SDXL | 0.6734 | 0.5064 | 0.6243 | 0.2073 | 0.3166 | 0.3575 | 8.07s |
| SynGen (NeurIPS'23) | **0.7267** | **0.5249** | 0.6476 | 0.2387 | 0.3172 | 0.3944 | 11.62s |
| InitNO (CVPR'24) | 0.6823 | 0.5229 | **0.6547** | 0.2553 | 0.3147 | 0.4077 | 19.42s |
| R2F | 0.7135 | 0.5194 | 0.6625 | 0.2447 | 0.3170 | 0.4031 | 10.14s |
| ELDiff（**要训练**） | 0.7768 | 0.5663 | 0.6941 | 0.2496 | 0.3261 | 0.4252 | 8.07s |

训练无关的余量：**color +0.0533 / shape +0.0185 / texture +0.0304**。
color 最大 → 预注册主子集选 color 是对的。

**这个余量对上 ~1 pp 量级的噪声底是 5×。** 与 §10 那条轴上
「最好负值 −0.0001 对阈值 0.00074」是反过来的局面。读数②的前提成立。

#### 订正二：「比优化法快」这个卖点不成立

我在 §11.2 把组合性线的优化范式说成「贵、不稳、逐图」，用它当动机的一半。
真实延迟推翻了这个说法的一半：

* SynGen 只慢 **44%**（11.62 vs 8.07），而且 color 最好；
* 真正慢的 InitNO **+141%**，color 反而更差（0.6823）。

我们的机制是三次前向 vs 基线两次 ≈ **12.1s**，与 SynGen 同档。
**所以速度不是卖点，必须在分数上真赢 SynGen 的 0.7267。**
这一条已写进 `exist_check.py --plan` 的输出，防止后面自己忘掉。

#### 订正三（往回放松）：HeadHunter 没我上一轮说的那么危险

拿到全文摘要后，三点与 §11.3 的判断不同：

1. **backbone 是 SD3 / FLUX.1，不是 SDXL。** 它是 DiT 侧的工作。
2. **轴是 structure / style / texture quality**，明确的视觉质量与风格，
   摘要里没有 attribute binding / semantic binding，没有任何组合性 benchmark。
3. **最关键**：SoftPAG 的扰动仍然是「向单位阵插值」——
   **依然是钝器，只是更软、更精细地选了位置。**
   HeadHunter 回答的是「在哪个头上用钝器、用多软」，
   不是「怎么造一把只切一个语义轴的刀」。

所以 Q2（构造只坏绑定的分支）与 HeadHunter 是**不同类的对象**：
一个是在钝器集合上做选择，一个是语义构造。**§11.3 说「新颖性边际很窄」
是我过度悲观了，这里往回放松半格。**

但保留一个必须做的对照：「用 HeadHunter 的头选择、把目标换成绑定分」
是审稿人会想到的基线，我们要把它做成消融行，不能回避。

#### 还查了什么（都不构成阻塞）

| 工作 | 状态 | 与我们的关系 |
|---|---|---|
| Contrastive Guidance (arXiv 2402.13490) | 无 venue、无码 | **最接近的先例**：用非空 baseline prompt 替代空串做 CFG。确立了「负分支可以是构造出来的 prompt」这个一般形式，但目标是 disentanglement / rig-like control / 编辑，**不碰绑定**。→ 引用并区分，不是杀手 |
| ColorWave (WACV 2026) | 有 venue | 用了「semantic attribute binding」这个词，但做的是 **IP-Adapter 的 RGB 级精确配色**，不是多物体绑定正确性。术语撞车，问题不同 |
| Detail++ (arXiv 2507) | 无 venue，码未开 | 训练无关但**仍用测试期优化**（centroid alignment loss）+ 多分支。SDXL color 0.7241（CompBench++） |
| ELDiff (arXiv 2606) | 无 venue，**要 finetune** | 不占训练无关的槽，但它 0.7768 会出现在审稿人看到的表里 |
| ABC-6K / ARO / 属性交换硬负样本 | 已有 | 「交换形容词造负样本」在**评测与训练**文献里是老做法。→ 意味着最朴素的 instantiation（交换属性当 negative prompt）**不是新机制**，只是新应用。Q2 必须给出比这更强的构造 |
| TAG (2510.04533) | — | PAG 系在 CompBench 上只碰 spatial / complex 子集，**没碰 attribute binding** |

#### 结论

槽仍然是「不空但有缺口」，而且缺口比 §11.3 判断的**稍宽**（HeadWave 那条放松），
同时动机比 §11.2 判断的**稍窄**（速度卖点作废）。两处订正方向相反，净结论不变：

**可以做，但胜负手全在 Q2 —— 而且现在有了硬指标：训练无关 color 必须
> 0.7267，延迟 ~12s。**

### 11.8 停：这是同一个错误的第七、八、九次（2026-08-16）

`survey_slow_turnover.md` 底部的「作废」小节在 2026-08-06 就诊断过这个循环：

> 三次都是「先选一个位置，再论证它为什么归我们」。
> **一个失效如果已经被写进文献，它就已经被修了。所以「没被修的失效」
> 按定义不在文献里 —— 用检索去找研究问题，是拿错了仪器。**
> 检索只能回答「什么被占了」，回答不了「什么是坏的」。

**本 session 的 §10.24 / §11 / §11.7 是同一个错误的第 7、8、9 次。**
每一次的形状都一样：选轴 → 检索占位 → 发现拥挤 → 换轴。三次都没有
任何一张图被看过。

那份文档给的正确顺序（此处照抄，因为我一直把第 5 步提到第 1 步之前用）：

```
1. 选一个在位者（已录用+开源+跑得动）
2. 跑它
3. 看输出，找到可见的错
4. 给失效命名 —— 到这一步才有东西可查
5. 【此时】查这个具体失效有没有被后续工作修掉
6. 做定位实验找病因
7. 干预点由病因决定
8. 在在位者的坐标系里比
```

一条订正：该文档写「19 个方法我们一个都没跑过」是 08-06 的状态，现已过期 ——
ScaleDiff（§10 全程）、DemoFusion、AccDiffusion、StyleSSP、make-it-count
都跑过了。但 §10 里我们**从生成直接跳到了测量**，第 3 步（看）只在用户
两次要求下做过（§10.16 的 `multi_look`），而且没有从中产出命名的失效。

### 11.9 唯一一类还没做过的检索：结构性不匹配（不是空位）

跟「空位检索」不同类的一个观察：

**195 条 training-free 覆盖图里的方法，几乎全部建立在两个脚手架上 ——
(a) CFG 的双分支外推，(b) 25–50 步的去噪时间轴。**
而少步蒸馏模型（SDXL-Turbo / SDXL-Lightning 1/2/4/8 步、LCM）把两个都拆了：

* **LCM 不支持 SDXL 的常规 CFG**（条件尺度走 embedding）；
  NAG 那篇的存在理由就是「few-step régime 下负引导不可用」。
* HR 那一系（DemoFusion 的 progressive upscaling + skip residual、
  AccDiffusion、ScaleDiff 的 restart noise τ）**全部需要时间轴上有足够多的
  步可用**。4 步里只有约 2 步能调度。

这**不是**「没人发论文的格子」——那是我刚犯过三次的错。
这是一个**可证的机制失配**：脚手架在新 régime 里在结构上不成立。
它的价值不在于「空」，而在于它**便宜到可以直接进第 2、3 步**：

| régime | 单图耗时 | 相对我们 §10 的吞吐 |
|---|---|---|
| 4096² ScaleDiff 50 步 | ~100 s | 1× |
| 1024² SDXL 50 步 | ~5.5 s | 18× |
| **1024² Lightning 4 步** | **~1 s** | **100×** |

而且在位者已经在本地：`help_code/{DemoFusion,AccDiffusion,ScaleDiff}`，
换 backbone 是换 checkpoint + 换 scheduler，小时级不是周级。

**⚠️ 记死：本节「检索没打到」这句话是最弱的证据。**
前四次教训全是「搜一轮觉得空、深查才发现有人」。
这里不主张任何空位；只主张这是**目前进入第 2、3 步最便宜的入口**。

### 11.10 AR/VAR 深查：我上一轮的三条主张全部被推翻（2026-08-16）

用户要求对「自回归视觉生成的推理期方法」做深度占位核查。四个角度扫描，
每一个都打到已占领地。

#### 打到的东西

| 角度 | 工作 | 干什么 |
|---|---|---|
| 引导 / 尺度轴 | **SWG**（Spectrum Weakening Guidance, arXiv 2511.22991） | training-free，**保留部分频谱分量构造弱模型再引导** —— 就是 PAG 移植到 VAR |
| 引导 / 尺度轴 | **Scale-Wise VAR is Secretly Discrete Diffusion**（2509.22636） | 证明 VAR **就是**离散扩散，「enables principled use of diffusion techniques such as CFG, token resampling, and scale distillation」 |
| 引导 | VPG（Visual Prefix Guidance, 2605.30317） | AR 图像与视频的前缀引导 |
| 加速 | **SparVAR**（2602.04361） | training-free，利用 VAR 注意力的 sink / 跨尺度相似 / 局部性 |
| 加速 | LiteVAR | 高效注意力 + 量化 |
| **高分辨率** | **ExtraVAR**（2605.10045） | **training-free 分辨率外推**，Stage-Aware RoPE Remapping + 熵驱动注意力校准 |
| 编辑 | AREdit（2503.23897） | training-free 文本引导编辑，缓存源 token 索引与概率分布 |
| 编辑 | **BitResEdit**（2606.13558） | training-free 编辑 **Infinity 的 bitwise residual** |

#### 三条主张，逐条推翻

**① 「129 条里只有 2 条 training-free」——计数本身是坏仪器。**
那个 repo 只是不追踪这类工作。三轮检索就打到 ≥8 篇。
**我不该再产出这种一轮计数，它系统性地制造假的空。**

**② 「扩散那套杠杆在 AR 上大部分不存在」——直接被 2509.22636 反驳。**
我列的那张"✗ 没有连续轨迹 / ✗ 没有时间轴 / ✗ 没有可分解的连续场"表，
在「VAR 就是离散扩散」这个结果面前整张作废。SWG 就是 PAG 移植过去的实例。

**③ 「VAR 的尺度轴没人像挖时间轴那样挖过」——ExtraVAR 就在挖，SWG 也在挖。**

#### 最要命的一条：ExtraVAR 的失效清单 = §10 判死的那三个

ExtraVAR 原文列出把现有方法直接用到 VAR 上的三种失效：

> **global repetition**（整体布局重复）、**local repetition**（中等结构变小变多）、
> **detail degradation**（细纹理模糊）

**这就是 §10 那条轴上的三个失效，原样搬到 AR。**
也就是说即使进了 AR，我们落地的地方仍然是那三个我们已经证明推不动的失效。

#### 判决

**AR / VAR 这条：死。** 不是"拥挤"，是"已占 + 且占的是我们已知推不动的同一组失效"。

#### 元教训（第十次，写下来）

这次我在提出时就预言了会这样（「前四次教训全是搜一轮觉得空、深查才发现有人」），
然后**照样提了，而且给了一张编造出的机理表当支撑**。
那张表不是从代码或论文读出来的，是我从"离散 vs 连续"这个直觉推的。
**推出来的机理表读起来像证据，实际上是猜测。以后不写这种表。**

十次方向调研，十次判死。这个仪器在我手上的失败率是 100%。

### 11.11 概念擦除深扫：死于换手速度，不是死于占位（2026-08-16）

#### 约束匹配：这一格其实是过的

| 约束 | 状态 |
|---|---|
| 单卡 4090 | ✅ SD1.5/SDXL 的 cross-attention 微调、闭式编辑、推理期干预都跑得动 |
| 无标注池 | ✅ ESR（分类器）+ FID + CLIP，全自动 |
| 标准表 | ✅ I2P(4703 条) / UnlearnCanvas / MMA-Diffusion / Ring-A-Bell |
| venue | ✅ ICCV'23 起，CVPR 2026 还有专门 workshop (MUV) |

**前十次都死在约束或占位上，这一格两样都过。**

#### 但它是一场军备竞赛，而且节奏比 HR 快

综述 2502.14896 把方法分三类：**微调**（ESD/AdvUnlearn/Receler/SALUN…13 篇）、
**闭式权重编辑**（UCE/MACE/RECE/EMCID…7 篇）、
**推理期干预**（SLD/SAFREE/Content Suppression/ORES/GuardT2I，5 篇）。

只数 2026 年（arXiv 2601–2607）这一个窗口：

| 防守侧 | 攻击侧 |
|---|---|
| Mass Erasure + Hierarchy (2601) | ReLAPSe (2602, RL 搜对抗 prompt) |
| Prototype-Guided (2603) | REFORGE (2603, 多模态攻击) |
| Erasing Thousands of Concepts (2604) | Stress Testing w/ LLM Agents (2607) |
| Orthogonal Concept Erasure (2605) | ZIUM (2507) |
| ForceForget (2606) | UnlearnDiffAtk（标准攻击） |
| Uni-AdaVD (2607) / TILDE (2607) | |
| ScaleErasure (2606, AR) / Obliviate (2606, AR) | |
| Video Unlearning (**ICLR 2026**) | |

**七个月，攻防两侧加起来 15+ 篇。约 2 篇/月。**

#### 最自然的入口已经被占：机理诊断

**《When Are Concepts Erased From Diffusion Models?》**（2505.17013）已经给出诊断：
> 大多数方法是 **guidance-based avoidance 而非 destruction-based removal** ——
> 概念没被删掉，只是被绕开了，用对的输入就能捞回来。

外加《The illusion of unlearning》。**"擦除是假的"这句开场白已经是别人的结论**，
和 §survey_unified_multimodal 里 GapEval 那次一模一样。

#### 判决：死，但死因是新的

前十次的死因是**关于那个格的**（被占 / 硬件 / 评测形式 / 与 §10 同一组失效）。
这一次的死因是**关于我们的**：

> **一场 2 篇/月的军备竞赛，换手速度超过我们的一个研究周期。**
> 我们三个月的窗口里，防守方法从设计到跑完标准表要占掉大半；
> 等写出来，攻击侧已经出了三到四篇新的，其中任何一篇都可能把我们的防守打穿 ——
> 而 reviewer 会拿最新的攻击来问。

`survey_slow_turnover.md` 那条"边际实验成本决定换手速度"的分析在这里应验：
概念擦除的边际实验极便宜（一个概念、几百张图、分类器打分），
**所以它对所有人都便宜，大组能并行跑十个想法。这正是我们必输的象限。**

附带一条：安全类论文的评审惯例常要人工核查有害内容，我们没有标注池。

#### 十一次的总账

| # | 方向 | 死因 |
|---|---|---|
| 1–6 | （见 08-06 各 survey） | 占位 |
| 7 | 过平滑/细节 | 占位 + **实测零余量** |
| 8 | 组合性/属性绑定 | **未死** —— 唯一撑过第二轮 |
| 9 | 少步 × HR 组合 | 用户否决（未跑） |
| 10 | AR / VAR 尺度轴 | 占位 + 失效清单与 §10 重合 |
| 11 | 概念擦除 | **换手速度 > 我们的周期** |

**十一次调研，一个幸存者。** 而它幸存的唯一原因是：
它是唯一一个我能从一手表格里读出**量化余量**的（+0.0533 对 ~1 pp 噪声底）。

**这才是信号：能不能活，取决于有没有一个可读的余量数字，而不是取决于格子空不空。**
