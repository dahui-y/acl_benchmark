# 下一篇 ACL Benchmark 论文 —— 选题方案

本文档基于对 `OSCBench: Benchmarking Object State Change in Text-to-Video Generation`
(ACL 2026, pp. 30867–30884) 的完整分析，给出可执行的后续选题方案。

---

## 0. OSCBench 的可复用配方

| 步骤 | OSCBench 的做法 | 为什么有效 |
|---|---|---|
| 1. 选能力维度 | prompt 显式指定的物体状态变化 (OSC) | 本质是"语言→世界"的语义问题，不是画质问题，因此归属 NLP 而非纯 CV |
| 2. 数据来源 | 在 HowToChange (源自 HowTo100M) 上做人机协同抽象：20 动作元素 → 9 动作类；134 物体元素 → 8 大类 / 28 子类 | 不从零标注，成本低、覆盖可控、能论证无长尾偏置 |
| 3. 难度阶梯 | regular (108) / novel (20) / compositional (12)，每场景 8 组合 = 1120 prompts | 把"记忆 vs 泛化 vs 组合"分离，analysis 章节自然成型 |
| 4. 双轨评测 | 人工 user study (140 prompt × 6 模型 × 3 标注者, 1–5 Likert) + MLLM CoT 自动评测 (criteria grounding → evidence extraction → score decision) | 人工作为上界，MLLM 作为可扩展代理 |
| 5. 可信度论证 | human–MLLM 的 Kendall τ / Spearman ρ，并以人–人一致性为参照上界 | 直接回应"自动评测可信吗"这一必然的审稿意见 |

**核心叙事**：语义/场景对齐分很高（subject 0.86–0.94, scene 0.74–0.89），
但状态变化准确性只有 0.38–0.79 → *高层语义对齐 ≠ 忠实实现动作后果*。

### OSCBench 的软肋（= 我们的机会）

1. **自动评测在最核心维度上并不可靠**：consistency 维度 GPT-5.2 与人的 τ 仅 0.317，
   而人–人一致性为 0.501；realism 上 CoT 版本 (τ=0.276) 甚至不如 GPT-5-mini (0.303)。
2. **单句、短 prompt**：平均 9.2 词，模板固定为 `<subject><action><object><scene>`，
   最多两个动作复合，不涉及跨句共指、省略、隐含论元。
3. **单一领域**：仅烹饪（论文 Limitations 自陈）。
4. **仅英文**：完全没有触及结果义在不同语言中的词汇化差异。
5. **评分是整体 Likert**，无法定位"在第几步/第几帧崩坏"。

---

## Idea 1（首推）ProcBench：多步程序性指令下的实体状态追踪

### Gap
真实指令是多句、跨步骤的，并带有 NLP 的经典难点：跨句**共指与零形式**、
**隐含前提**、**顺序敏感性**。OSCBench 的 compositional 场景只做到"peeling and slicing pear"
这种单句双动作，远未触及程序性话语。

### 与 NLP 任务谱系的锚定（务必写进 Intro 第二段）
这是把文本领域的 **entity state tracking**（ProPara、OpenPI、bAbI、TRACIE）
迁移到视频生成评测：生成模型必须在多步骤话语中维护实体状态，
且需要正确解析指代与省略才能确定"对哪个实体施加哪个变化"。

### 数据构造
- **来源**：RecipeNLG / Tasty / HowTo100M ASR 字幕 → 抽取 3–6 步的程序片段。
- **标注单元**：每一步附一条**状态断言** `(entity, attribute, before → after)`，
  整条 prompt 对应一条**状态轨迹** ground truth。
- **四条受控扰动轴**（每条都对应一个可单独成表的实验）：

  | 轴 | 操作 | 考察什么 | 预期发现 |
  |---|---|---|---|
  | A. 顺序置换 | 打乱步骤顺序（"先炒后切" vs "先切后炒"） | 模型是遵循指令，还是回退到菜谱先验 | 对顺序基本不敏感 |
  | B. 共指压力 | 显式名词 → 代词 → 零形式（中文可做零形式） | 跨句指代解析是否影响状态施加对象 | 代词化后显著掉分 |
  | C. 隐含步骤 | 删掉一个必要中间步 | 模型是否自行补全（能力）还是不忠实（幻觉） | 静默补全，对忠实性是负面 |
  | D. 不可逆性 | 要求"把切好的洋葱恢复原状" | 是否编码了状态变化的方向性 | 几乎完全不遵守，直接倒放式生成 |

- **规模**：300–500 条 prompt × 5–6 个 T2V 模型。

### 评测协议
不打单一总分，而是**逐步骤断言核对**：
- `step-wise state accuracy`：每步断言是否被满足（二元/三元判断，标注者一致性远高于 Likert）
- `first-failure step`：轨迹在第几步崩坏 → **崩溃步分布图，本文最好看的一张图**
- `trajectory monotonicity`：状态是否出现回退（对应论文里 Wan-2.2"切了一半的苹果变回整个"）
- 保留 OSCBench 的 realism / aesthetics 作为对照维度，便于与其结果对齐比较。

### 人工成本
只需对 ~150 条 prompt 做逐步断言标注；因为是断言判定而非 5 级主观打分，
标注速度更快、一致性更高（预期 human–human κ 明显优于 OSCBench 的 τ≈0.50）。

---

## Idea 2（最有语言学差异化）跨语言状态变化：结果义的类型学

### 核心问题
结果义（resultative meaning）在不同语言中的词汇化方式截然不同
（Talmy 的 verb-framed vs satellite-framed 类型学）：
- 中文：动结式补语 —— 切**开** / 切**碎** / 煮**熟**
- 英文：融入单个动词 —— slice / mince / cook
- 西班牙语、日语、韩语：又各不相同

**当结果义由补语/卫星成分而非动词本身承载时，T2V 模型是否还能实现正确的状态变化？**
即：模型学到的是"动词 token → 视觉模式"的检索映射，还是真的在做组合语义？

### 杀手锏实验：中文最小对立对
"切西瓜" vs "切开西瓜" vs "切碎西瓜" —— **动词完全相同，仅结果补语不同，
目标状态完全不同**。英文构造不出这么干净的对照。
若三者生成结果无差异，即证明模型忽略结果义成分、仅对动词做检索。

### 必要的对照设计（隔离混淆变量）
- 英文原 prompt vs 人工翻译 vs 机器回译 → 区分"多语言能力弱"与"结果义未组合"
- code-switching prompt（英文句式 + 中文结果补语）
- 覆盖 4–6 种语言，至少含一种低资源语言

### 风险
需要各语言母语标注者。审稿风险是被读成"翻译鲁棒性研究"——
必须在 Intro 就用类型学框架定调，而非用"多语言支持"定调。

---

## Idea 3（作为第二贡献打包进 Idea 1 或 2）可验证的状态变化自动指标

### 动机
OSCBench 自陈：MLLM 在 consistency 上 τ=0.317，远低于人–人的 0.501。
直接让 MLLM 打分（无论加不加 CoT）在最核心的维度上就是不够用。

### 方法思路
不让 MLLM 打分数，而是让它输出**可验证的结构**：
1. 逐帧（或逐窗口）抽取物体的**状态谓词序列** `state(object, t)`；
2. 用一个**符号化轨迹检查器**判定：是否达到目标末态、是否单调不回退、
   是否出现实体的无解释出现/消失；
3. 分数由检查器导出，而非由模型直接生成。

### 卖点
- **可解释**：能精确定位失败帧（Wan-2.2 的状态回退、Veo 末帧多出一块苹果这类，
  直接打分抓不到，轨迹检查器能抓到）
- **相关性更高**：与人工评分的 τ/ρ 应显著优于 direct-scoring 与 CoT-scoring
- **抬升论文定位**：从"纯资源型"变成"资源 + 方法"，ACL 评分通常高一档

---

## Idea 4（作为 analysis 小节，不单独成篇）生成–理解的不对称

把生成失败的视频反过来喂给 MLLM 做判别：模型**看**得懂状态变化吗？
若 MLLM 能准确指出"苹果没被切开"而生成模型做不到，
说明瓶颈在生成端而非视觉表示端。同时让 benchmark 复用为 video-understanding 测试集。
单独撑不起 ACL 长文，但作为 discussion 一节非常有价值。

---

## Idea 5（不推荐单独做）换域扩展

即 OSCBench Limitations 自陈的方向（工具使用、家务组装、户外活动、化学实验）。
审稿人大概率会给 "incremental, same recipe with new data"。
除非同时引入一个新的能力轴（例如 Idea 1 的多步 + 不可逆性），否则不建议。

---

## 推荐组合

> **主体 Idea 1（ProcBench）+ 第二贡献 Idea 3（可验证指标）+ 一节 Idea 4（生成–理解不对称）**

理由：
- Idea 1 的 NLP 归属最硬（实体状态追踪 + 共指 + 隐含论元），审稿人不会质疑投稿场地；
- 实验成本可控（300–500 prompt，人工只标 150 条断言）；
- 预期结论足够尖锐（顺序不敏感 / 代词化掉分 / 不可逆约束失效）；
- Idea 3 正好补上 OSCBench 被诟病的自动评测不可靠问题。

若追求更强差异化与更低撞车风险，则选 **Idea 2**，前提是能落实多语言母语标注者。

---

## 必须提前防守的审稿意见

| 审稿意见 | 应对 |
|---|---|
| "为什么是 ACL 不是 CVPR？" | Intro 第二段即锚定到 entity state tracking / 共指 / 类型学的 NLP 任务谱系 |
| "MLLM 评测不可信" | 必须报告人–人一致性作为上界参照（沿用 OSCBench 做法）；Idea 3 直接正面回应 |
| "prompt 由 LLM 生成是否有偏？" | 保留 human-in-the-loop 的验证记录，附录给出候选拒绝率与拒绝原因分布 |
| "只是 OSCBench 的增量" | 强调新增的是**能力轴**（多步话语 / 类型学）而非新数据域；用扰动轴的受控对比证明 |
| "模型选择过时" | 送审前补测当时最新的开源 + 闭源模型各一 |

## 工程复用

仓库已有的以下资产可直接复用/改造：
- `action_object_taxonomy/*.json` —— 动作与物体分类体系，可作为新 benchmark 的实体词表
- `mllm_eval.py` —— MLLM CoT 评测框架（`EVALUATION_CRITERIA` 换成逐步骤断言即可）
- `extract_frames.py` —— 均匀采样 20 帧
- `result_analyze.py` —— Kendall τ / Spearman ρ 相关性分析
