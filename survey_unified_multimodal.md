# CVPR 方向调研：统一多模态模型（理解 + 生成）

调研日期 2026-08-06。触发：用户改投 CVPR，方向定为多模态 / 统一机制。
按"统一多模态模型（理解与生成统一在一个模型里）"这个读法做。

> **结论先说：这是 2026 年多模态最热的一片，动机和 venue fit 全免费，
> 但它同时是我们这套硬件条件下最难进的一片。**
> 最自然的那个 training-free 想法（让模型用自己的理解修自己的生成）
> **四个月前已被占**；连"差距存在且是工程层而非认知层统一"这个**诊断本身**
> 也已经是 CVPR 2026 的论文。

---

## 1. 格局：谁占了什么

| 子问题 | 已占 |
|---|---|
| **差距的测量** | **GapEval**（CVPR 2026，双向基准）、**MME-Unify**（ICLR 2026）、Uni-MMMU、IMUG-Bench |
| **用理解改进生成 · training-free** | **Free Lunch / UniRect-CoT**（arXiv 2604.13540，把去噪当内在视觉推理，用理解做自监督信号纠偏）、**FiRe**（2604.13491） |
| **用理解改进生成 · 训练 / RL** | Understanding-in-Generation（2509.18639）、Turning Internal Gap into Self-Improvement（2507.16663）、Understanding-Driven Intrinsic Rewarding（2603.06043）、UniRL（2505.23380）、UniPath（2605.11400） |
| **生成改进理解（反向）** | Multi-Representation Generation（2601.21406）——**只找到一篇** |
| **AR 图像生成的 RL** | AR-GRPO、STAGE、Interleaved-GRPO（2603.09538） |
| **架构 / 加速** | Breaking Dual Bottlenecks（2605.14709）、Hyper-Bagel、Kelix |
| **综述** | arXiv 2505.02567（v3+，三类架构 + 八条开放挑战） |

**连诊断都被发表了。** GapEval 的原话：各架构上差距持续存在，说明当前模型只做到
**"engineering-level unification rather than truly cognitive-level integration"**；
理解与生成**依赖各自独立的知识表示**，知识"共存而非融合"。

也就是说，"统一模型其实没真的统一"这句开场白，**已经是别人的论文的结论**。

---

## 2. 综述点名的八条开放挑战（可作为选题清单）

1. 高效的 tokenization 策略
2. 跨模态注意力机制
3. 数据构造与对齐
4. **模型评测标准**
5. 细粒度生成控制
6. **对新概念 / 分布外 prompt 的泛化**
7. 计算效率（迭代去噪）
8. **理解与生成性能的权衡**

架构三分法：diffusion-based / autoregressive（再分 pixel、semantic、learnable-query、hybrid 四种编码）/ fused AR+diffusion。

表示瓶颈被单独点名：**pixel 编码器保细节但与文本不对齐，semantic 编码器对齐但丢细粒度控制，
hybrid 引入模态失衡或冗余。**

---

## 3. 可行性：一张 4090 能做什么（实测参数量）

| 模型 | 参数量 | bf16 显存 | 24GB 上 |
|---|---|---|---|
| **BAGEL-7B-MoT** | **14.7B** | **27.4 GB** | ❌ 必须 offload 或量化 |
| Emu3-Gen | 8.5B | 15.8 GB | ⚠️ 推理可以，很紧 |
| Janus-Pro-7B | ~7B | ~14 GB | ✅ 推理 |
| Janus-Pro-1B / Show-o2-1.5B | 1–1.5B | 2–3 GB | ✅ 推理 + **LoRA 微调** |

**推论（这一条决定能做哪类论文）：**

- ✅ **推理期 / training-free 方法**：可行
- ✅ **1–1.5B 小统一模型上的 LoRA**：可行
- ✅ **分析 / 评测**：可行
- ❌ **7B 上的 RL / GRPO**：**不可行**——GRPO 要同时驻留策略模型、参考模型、
  以及采样缓存，24GB 装不下 7B 的这套
- ❌ 任何预训练：不可行

**而第 1 节表里过半的工作是 RL / 训练类的。** 也就是说这一片的主流打法我们打不了，
只能走推理期和分析这两条窄道——**而推理期最自然的那一格已经被 Free Lunch 占了。**

---

## 4. 看起来还薄的几处（**均未核实**）

1. **反向（生成 → 理解）**。整片检索只打到一篇（2601.21406）。
   正向被做烂了，反向没人做——但也要问一句：**是没人做，还是做不出效果。**
2. **失效分析**。所有 self-improvement 方法都报告有效，**没有人问它们什么时候失效**。
   "用理解修生成"在什么条件下反而更差？这是批评式贡献，CVPR 收，但不是主流。
3. **机制解释**。GapEval 测出差距**多大**，没解释理解**怎么**没传过去。
   我们自己那个胡萝卜观察恰好是一个机制样本：**体态被正确理解（两行第一帧不同），
   却被执行成静态场景选择而非时间程序**——这就是"理解到了、没传到生成"的一个具体形态。
4. **视频侧的统一模型**。图像侧挤爆，视频侧稀薄得多。但算力成本回到 208 s/条那个量级。

---

## 5. 必须直说的战略判断

**用一张 4090 进入 2026 年最热的一片，对手是在 7B 上跑 RL 的组，这是最差的对位。**

前三次方向失败的原因分别是测量、动机、新颖性。这一次动机和 venue 是白送的，
**风险全部集中在"新颖性 + 算力"这一格**，而且这两者互相加强：
能做的窄道（推理期、小模型）恰恰是别人最容易顺手覆盖的地方。

不是不能做，但要清楚这是**在别人主场用轻装备打**，需要靠选题极准来弥补。

---

## 6. 下一步该核实什么（按性价比）

1. **Free Lunch / UniRect-CoT 的全文**——它到底占了多大一块？用了哪些基座、哪些基准、
   提升多少？它没占的边界在哪？（这决定推理期这条窄道还剩多少）
2. **"反向"那一篇的效果如何**——如果生成→理解本来就效果有限，那片"空白"是假的
3. **CVPR 2026 这一片实际收了什么**——从 highlights / open access 里数一遍，
   看录取的是训练类还是推理类，规模多大
4. 如果要走"失效分析"：先在 Janus-Pro-1B 上复现一次 self-improvement，
   看它在什么样本上变差——**这是唯一一条我们现在就能动手跑的**
