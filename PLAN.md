# 我们在做什么（每次迷路先看这里）

> **产出物的形状在 `PAPER_SKELETON.md`。**
> 这份是过程和纪律；那份是动机 / 问题 / 方法 / 贡献 / 目标数字 / 证据现状。
> **每个实验都要能指到 PAPER_SKELETON 里的某一句话。指不到就是漂移。**

日期 2026-08-07。

## 一句话

**写一篇和 ScaleDiff 同一种的方法论文：training-free 高分辨率外推，
修 ScaleDiff 自己在 Limitations 里承认、但没修的那条失效。**

我们和 ScaleDiff 的关系 = **StyleSSP 和 StyleID 的关系**。
不是重做它做过的事，是开在它的残余上。这就是谱系的走法。

| | ScaleDiff (NeurIPS 2025) | 我们 |
|---|---|---|
| 任务 | training-free 高分辨率外推 | 同 |
| 基座 | SDXL（+FLUX） | 同 |
| Intro 的失效 | U-Net 偏见 / patch 重叠贵 / RGB 上采样糊 | **4096² 幻觉出 prompt 主体** |
| Method | NPA + LFM + SG | **?（第 6 步）** |
| 评测 | FID/KID/IS + FIDp/KIDp/ISp + CLIP，1000 条 LAION | **同一套** + 幻影计数 |
| Limitations | *"repetitive artifacts may still occur in background regions"* | ← **我们的入口** |

**不做用户研究**（没有标注员），和 ScaleDiff / LSRNA / HiFlow 一样。

---

## 进度（2026-08-08 复盘）

```
[x] 0  选在位者   ScaleDiff (NeurIPS25)。4090 上 4096² = 74.7s / 11.9GB
[x] 1  找到失效   4096² 把 prompt 主体在每个受限视野里各实例化一次
[x] 2  造尺子     计数器 + CLIP + 频谱，三把都过了阳性对照
[x] 3  诊断病因   邻域/画布比 + 全局文本条件，两者都必要
[x] 5  占位核查   ScaleDiff 的残余没人认领；AccDiffusion v2 没有重复的定量指标
[~] 6  设计方法   v1 定稿候选。v0/v2 是阴性结果，进消融表
[ ] 4  普遍性     **n=1 seed。这是现在最薄的一环**              <<< 下一步
[ ] 7  第三条腿   FID/IS/CLIP x 1000 LAION。**零。卡在数据下载**
```

### 三把尺子（都有阳性对照，都过了）

| 尺子 | 量程（阳性对照） | 现状 |
|---|---|---|
| 计数器 MAE | 五条预注册预测过 4.5 条；阈值 0.50–0.80 是平台 | 主指标 |
| CLIP score | 配对 29.96 / 错配 2.38，判对 30/30 | 只能证明语义不变 |
| 频谱高频 | 双三次 **−94.6%** | 细节没塌（干预 −1.1%） |

**CLIP 对质量免疫**：高斯模糊 r=8 实测 **+1.18%**（糊了反而涨）。
±1% 是噪声。不要拿它说"细节没塌"，那是频谱那把尺子的活。
**但别扩大解释成"领域指标看不见细节"** —— AccDiffusion v2 自己把 CLIP 定位成
additional *alignment* metric，他们测细节用 FID_c（裁 10 个原分辨率 patch）。
领域只对**物体级计数**盲，缺口就这一条。

### 方法三个版本

| | 做法 | lone MAE | 全部 MAE | 结论 |
|---|---|---|---|---|
| 基线 | ScaleDiff 原版 | 5.40 | 1.50 | — |
| v0 | 压主体 token 的注意力权重 | — | — | **无效**，5→5。CLIP 文本编码器是因果的，主体语义摊在整句上，压一个 token 没用 |
| **v1** | 两套嵌入同一次前向，按主体图逐位置混合（min-max） | **2.00** | **0.85** | **最好**。+3.9% 时间 / +1.7% 显存 / CLIP +0.02% / 高频 −1.1% |
| v2 | 同上，改 rel = m/mean(m) 归一化 | 3.00 | 1.00 | **更差**。cov 全塌到 ~50%，把有信息的那一维归一化掉了 |

v1 的已知尾部问题（**单 seed，分不清是真回归还是检测器噪声**）：
portrait / structure MAE 0.20 → 0.40；08_sheep 的羊群被删光（cov 11%）。

### 顶会的四条腿

| | 状态 |
|---|---|
| ① 命名前作的残余失效 | 有。ScaleDiff 自己的 Limitations + 代码机制（NPA 无全局注意力） |
| ② 结构上新的干预 | 有。单次前向内逐位置混合两套嵌入；AccDiffusion 的修法需要独立 patch 前向，结构上不可用 |
| ③ **标准指标上赢的表格** | **零**。这条腿才是决定录用的 |
| ④ 尺子（他们想要拿不到的） | 有，且验证过 |

### 硬阻塞

1. **单 seed。** 所有数字建立在 30 条 prompt x 1 seed（主指标只有 20 条可用）。
   而且 v0/v1/v2 三个变体在**同一批数据**上评估后挑最好 —— 有拟合测试集的风险。
2. **网络。** hf-mirror 和 huggingface.co 都不通。LAION prompt / 10000 张真图 /
   InceptionV3 权重全拿不到，第三条腿开不了工。**Mac 中转是关键路径，比 GPU 时间长。**
3. FLUX 一次没跑过（ScaleDiff 的第二个骨干）。

### 决策队列

```
1. 冻结 v1，补 seed 1234 / 2025，两个 arm         2.6 小时   <- 现在做这个
     lone 5.40->2.00 三个 seed 都成立  -> 主结果坐实
     尾部回归三个 seed 都在            -> 真的，加适用性门
     尾部回归只在 seed 77              -> 噪声，v1 直接定稿
2. Mac 中转评测数据（可与 1 并行）
3. 第三条腿 FID
4. FLUX
```

## 为什么"任务是高分辨率生成"而"问题是物体重复"（问过两次，写下来）

**它就是高分辨率生成。物体重复不是另一个问题，是这个任务的特征失效。**

SDXL 只在 1024² 上训练过。不训练就生成 4096²，所有方法只有一招：
**让模型一次看到的东西变少**（DemoFusion/AccDiffusion 切 patch、Pixelsmith 滑
slider、ScaleDiff 用注意力窗、ScaleCrafter 用扩张卷积）。而每一个受限视野在
模型看来都是一整张画布 —— 它被训练成"给我一张画布，我画一个完整场景"。

> **计算可行性和全局一致性是同一枚硬币的两面。
> 为了算得动而切开的每一刀，都在制造一个会独立实例化 prompt 主体的新画布。**

谱系上每一篇的问题都是重复的某个变体（ScaleCrafter/DemoFusion/AccDiffusion
v1+v2/HiWave/ScaleDiff），这不是巧合。

**不能把问题写成"高分辨率图像生成"** —— 那是任务名不是研究问题。而且按现有
指标这个任务已经做完了：ScaleDiff 一张 4090 上 74.7s 出 4096²，FID 58.12 比
1024² 原生的 58.49 还好。没做完的是那张图上有 6 个登山者而 prompt 说一个 ——
**而 FID 看不见**。

**写作决定：任务在前，失效是轴。** 开头写"我们解决物体重复"会被读成抢
AccDiffusion 的题。要写成"这个任务的中心张力是效率↔全局一致性，物体重复是
这笔交易的价钱"，重复就从抢来的题目变成量这个权衡的标尺。

用 GroundingDINO 数物体不会让论文变成检测/计数论文 —— 检测器是仪器不是主题，
就像 FID 用 InceptionV3 的分类特征不会让生成论文变成分类论文。

## 叙事（2026-08-08 改）

**不要把"修法"当贡献。** "把 AccDiffusion 的修法搬到 ScaleDiff 上"是
incremental port，一句话就能被打掉，而且打得对。借机制是这条线的常态
（StyleSSP 借 FlexiEdit、HiWave 借 Sadat、ScaleDiff 借 LSRNA），
但借了机制就必须有别的东西撑着。我们手上只有两样别人没有的：

1. **这条线上没有能看见这个失效的指标。** FID/KID/IS 把 4096² 压到 299²，
   物理上看不见多出来的一个人；FIDp/KIDp 是分布距离，不数物体。
   失效被普遍观察到（ScaleDiff 自己写在 Limitations 里）却从未被测量过。
   **措辞不能过头（2026-08-08 更正）：**"用检测器数生成图里的物体、和目标数量
   比"这套协议在 T2I 计数线里是成熟的（CountGen、Counting Guidance、
   Detection-Driven Object Count Optimization，都用 GroundingDINO/YOLO/DETR
   数数报 MAE）。**我们不是发明了指标，是第一次把它用到高分辨率外推上。**
   正确表述：*协议成熟，但在这个子领域从没被用过，而领域自己说测不了；
   没被用过是有原因的 —— 直接套会漏掉 80%（同一张图缩到 1024²，4096² 上的
   6/8/9 个物体只剩 1/2/1 个），要用得对必须做尺度匹配分块。*
   那个 80% 的数据是我们自己的，也正好解释了为什么一直没人做。
2. **一条定量的律。** AccDiffusion v2 只给了定性机制（"同一句 prompt 施于
   每个局部视野"）。我们量到的是：幻影数由**邻域/画布比**支配，不由分辨率
   支配。**修完尺子后的数（旧的 "0 -> 5" 是修尺子之前的，已作废）：**

   | 分辨率 | KV 邻域占整图 | lone 类平均 excess |
   |---|---|---|
   | 1024² | 1 | +0.20 |
   | 2048² | 1/4 | +1.00 |
   | 4096² | 1/16 | +5.40 |

   **不是二值的门，是单调超线性曲线** —— 比原说法更好。另有邻域 x2 的对照
   （幻影 5->2~3，代价 +37% 时间 / +41% 显存）。
   证据强度：n=5 prompt、1 seed。够说"存在且随分辨率恶化"，
   **不够写任何定量的律 —— 要写律必须补 seed。**
   而这个比值是整个家族共有的轴 —— DemoFusion 的 patch、AccDiffusion 的
   patch、Pixelsmith 的 slider、ScaleDiff 的注意力窗，都是同一个轴上的点。

> **高分辨率外推里，效率与全局一致性是同一个权衡的两端。
> 这是汇率，这是尺子，这是少付一点的方法。**

修法降格成"这条律可操作"的证明。代价：测量要做到整个家族上（三个都开源，
一张 4090 跑得下），而且尺子要被验证得比现在硬得多。

### 顶会的三条腿（照 AccDiffusion v1->v2 抄）

机制新颖不在其中。① 命名前作留下的残余失效 ② 结构上新的干预
③ **领域标准指标上赢的表格**。我们 ①② 有，**③ 是零 —— 而③才是真正让论文
进去的那条腿**。FID/IS/CLIP + 1000 条 LAION，对 ScaleDiff，一次都还没跑过。

第四条腿是他们想要拿不到的：**尺子**。它让我们不是给 Table 1 添一行，
而是添一列，且可回溯给所有前作打分。但这一列只有在尺子可信时才值钱。

### 尺子的验证程序（按性价比排，不需要标注员）

1. **用别人的论文当标签集。** 领域对 DemoFusion -> AccDiffusion v1 ->
   AccDiffusion v2 的重复程度有已发表的定性共识（Fig. 9）。三个都开源。
   我们的计数器若复现出这个排序，尺子就被一个外部、非自证的标签集验证了。
   **同时产出家族级测量 —— 一件事办两个目的。**
2. **合成真值。** 把已知数量/位置/尺度的物体贴进图里，直接得到召回率关于
   物体大小的曲线。零标注、精确真值。这是目前最大的未知量。
3. **尺度不变性自检。** 4096² 降采样到 2048²/1024²，尺度匹配后计数应一致。
   不一致的量就是尺度偏差本身；修完 tile=width/4 后这一项必须归零。
4. **指标不能是原始计数。** 改成对 ±1 噪声钝的量：**重复率 = 4096 计数 >
   基图计数 的图像占比**，配平均超出量，配阈值扫描。
5. **第二个检测器做误差棒。** OWLv2 / YOLO-World，报一致率，不一致处标成
   不确定区间。承认精度边界比假装没有误差好。

三个补丁式过滤器（max_aspect / max_area_frac / 退化框）每个都要给敏感度表，
给不出的删掉。

## 我们现在在跑的这个实验是什么

**v1 是候选方法。这一批 30 条是它的普遍性检验。**

单图（seed 77）证明了机制存在；一张图不是方法。批跑要同时回答两个问题，
第二个比第一个重要：

1. 幻影是不是普遍减少了？（`lone` 类 delta 至少减半，+6.20 -> ≤3）
2. **它把什么弄坏了？** —— 这一问决定方法的最终形态。

比较的对象是同 seed、同 RNG 的基线批（`$SD_OUT/batch`），逐条配对。

---

## 第 3 步的结论（诊断）

> **在 R× 外推时，NPA 的 KV 邻域固定为"一张基分辨率画布"，占整图比例降到 1/R²；
> 而 cross-attention 把整句 prompt 以全局强度送给每一个位置。
> 两者的组合使每个邻域独立地实例化了 prompt 主体。**

三条互相独立的证据（全部 seed 77，全局 RNG 钉死）：

| 实验 | 结果 | 说明 |
|---|---|---|
| 放大阶段去掉 prompt 主体词 | 幻影 **5 → 0**，次阈值检测 **8 → 1** | 文本条件是**必要**条件 |
| KV 邻域 ×2（query 分块不变） | 幻影 **5 → 2~3**，次阈值 **8 → 0** | 局部性是**另一个必要**条件 |
| 2048²（邻域占 1/4）vs 4096²（1/16） | **0 → 5** | **比例**是关键变量 |

### 由此得到的方法【约束】（不是方法）

两个条件都必要 ⇒ 打断任一即可修，但两条现成的路都有明确代价：

| 打断哪个 | 代价 |
|---|---|
| 文本条件（删主体词） | 真实场景用不了 |
| 局部性（放大邻域） | 显存 **+41%**、时间 **+37%** —— 吃掉 ScaleDiff 的核心卖点（快） |

> **可用的方法必须：保留局部性（不加计算），但让文本条件不再在每个邻域
> 都以全局强度作用。**

---

## 当前可信的证据面（其余都不能用）

**只有 lone 5 行 + empty 5 行，n=10，单 seed。** 另外 20 行的状态：

| 类 | 状态 |
|---|---|
| crowd | 指标方向错（prompt 要的就是很多个），且 **08 sheep 的羊群被干预删光了**（+18 -> +0，cov 只有 11%）。05/06 反而变差 |
| texture | GroundingDINO 对 leaf / rock / window 这类满屏纹理全程不给框，各分辨率都是 0，产生不了信息 |
| portrait / structure | **检测器仍漏检 6 行**（20/22/24/25/28/29 的 4096² 主体计数是 0）。关掉 max_aspect / max_area_frac 只救回 21 和 26 —— 预测失败，主因另有其人，最大嫌疑是退化框规则（"尺寸落在图像尺寸 3% 以内"对满画面主体和对无特征整图框一视同仁）。查清之前这 6 行的 excess=-1 是漏检不是模型行为 |

## 由 crowd 得到的方法约束（有原则，不是特例）

> **只在 prompt 声明了确定基数时施加干预。**

`CARD` 已经把这件事编码了（crowd/texture 是 None）。方法可表述为
"抑制超出 prompt 所声明基数的实例化"，没有基数就不介入。
这比"复数主体特殊处理"干净，而且直接解释了 08 sheep 为什么不该被动。

## 纪律（写下来是因为都违反过）

1. **看到图之前不提方法假设。** 这次会话猜过七次方法，七次全被占位打回 ——
   每一次都是跳过诊断这一环的后果。
2. **检测器参数已冻结**（在 seed 77 的阳性对照上定死）。之后再动，就是在结果上调参数。
   例外只有一种：从**机制**上发现的结构性缺陷，且修法在动手前就写死。
   已用掉一次 —— 表观尺度不对称，见下。
3. **不再造新工具**，除非第 4–7 步真的需要。
4. **没有阳性对照的阴性结果不能用。**
5. **占位核查从领域的公共节点扫**（DemoFusion，116 引用），不从候选在位者扫 ——
   在位者在这次会话里换了三次（Pixelsmith → LSRNA → ScaleDiff），
   就是因为只扫了候选自己的引用图。

---

## 已知的坑（重跑前必读）

- **计数器的表观尺度不对称 —— `delta` 被系统性做高。**
  `detect()` 的分块那一遍有 `if W > tile or H > tile`（tile=1024），
  所以 **1024² 的基图根本不分块**，只走整图一遍缩到 ~800px；
  而 4096² 走整图一遍 + 49 块原分辨率 tile。同一个相对大小 f 的物体，
  4096² 上检测器看到 `f x 3200` px，基图上只有 `f x 800` px —— **4 倍差**。
  于是 "many cars" 基图 0 辆、"a colony of penguins" 基图 0 只、
  "a single eagle" 基图 0 只，都不是模型没画，是那一遍看不见。
  修法（机制驱动，动手前定死）：`tile = width/4, stride = width/8`。
  4096² 下正好等于现行的 1024/512，**4096 的计数一个字不变**，只修基图和 2048。
  修完两档的表观尺寸都是 `f x 3200`。改完必须重跑阳性对照。
  其余漏检来源要做敏感度分析，不能回避：`min_score=0.50`（v1 那个 0.54 的
  幻影贴着地板）、`max_area_frac=0.25`、`max_aspect=2.0`。其中 min_score 是
  全套参数里唯一没有机制依据、纯靠 seed 77 标定的。
- **别为了看内部量而改前向的数值路径。** 录主体注意力图需要显式 softmax，
  v1 顺手把 phase 1 的输出也从 SDPA 换成了它 —— 数学等价、fp16 不等价。
  扩散第一步的扰动一路放大，30 条里 25 条的**基图**都变了，整批 A/B 作废。
  规矩：**要观测的量单独算，前向一个字节都不许动。**
  核查手段：`compare_counts.py` 第一段，基图 1024² 逐字节哈希。
- **ScaleDiff 的高分辨率输出不能从 seed 复现**：放大阶段的
  `torch.randn_like(latents_LFM)`（pipeline:547）没传 generator，走全局 RNG。
  基图可复现，4096² 不可。**任何 A/B 对照都要先 `torch.manual_seed`。**
  `run_one.py` / `batch_run.py` 还没加这个 —— 它们之前跑的图不可复现。
- `scalediff_probe/` 下不能有和标准库同名的文件（`inspect.py` 踩过）。
- `transformers` 必须 `<5`（diffusers 0.35.1 要 `FLAX_WEIGHTS_NAME`）。
- SDXL 缓存只有 fp16 变体，而 ScaleDiff 没传 `variant="fp16"` —— 我们的脚本自动补。
