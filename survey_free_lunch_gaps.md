# Free-lunch 模板：哪些任务还没有 training-free 版本

调研日期 2026-08-06。依据：`littlewhitesea/training-free-methods`（维护到 2026-03），
**195 条**已整理的 training-free 工作，按任务分类。这是"覆盖图"，空白从图里读。

## 模板（从 195 条里归纳出来的）

> 挑一个**目前必须训练**才能做的任务 X
> → 找到预训练模型里一个**可利用的结构性质**
> → 在**推理期**利用它 → 做到无需训练

FreeU 用"主干去噪 vs 跳连注入高频"；FreePCA 用长短帧的 PCA 分解；
MosaicDiff 用预训练动态做剪枝；SwitchCraft 用注意力控制切事件。
**每一篇的核心都是那个"结构性质"，不是那个任务。**

---

## 1. 覆盖密度：哪一格厚，哪一格薄

| 任务类 | 条目数 | 判断 |
|---|---|---|
| Image Generation | **80** | 饱和 |
| Image Manipulation | **40** | 饱和 |
| Video Generation | 29 | 密 |
| Style Transfer | 18 | 密 |
| Video Manipulation | 18 | 密 |
| **3D Generation** | **5** | 薄 |
| **3D Manipulation** | **2** | 很薄 |
| **4D Manipulation** | **2** | 很薄 |
| **Material Transfer** | **1** | 很薄 |

图像那两格已经细到"遮挡控制""光学错觉""几何图表""美学质量"这种粒度了，
**再找空白基本是找边角**。3D/4D 薄，但需要另一套基建。

---

## 2. 关键词扫描：195 条里一次都没出现的

| 关键词 | 命中 |
|---|---|
| `state change` / `object state` | **0** |
| `aspect`（体态） | **0** |
| `negation` | **0**（唯一的 `negat` 是 CFG 的 negative prompt，不是语言学否定） |
| `tense` | **0** |
| `physic` | **0** |
| `instructional` / `cooking` | **0** |
| count | 5（**CountDiffusion / CountLoop / CountCluster** —— 计数这条已被三篇占了） |
| material | 3 |

**计数那一格顺带解决了一个旧问题**：我们之前那个 T2I 蕴含数量方向，
训练自由这一侧也已经有三篇了。

---

## 3. 按模板逐条检验的候选空白

模板要求四条同时成立：**(a) 任务定义清楚 (b) 目前要训练 (c) 有可利用的结构性质 (d) 索引里没有**。

### ① 动作导致的物体状态变化（视频）—— 四条全中

| 条件 | 状态 |
|---|---|
| (a) 任务定义清楚 | ✅ **OSCBench（ACL 2026 主会）**已把它形式化，1119 条 prompt、6 个模型、人工+MLLM 双评测 |
| (b) 目前要训练 | ✅ 现有三个解法全部要训练：**GenHowTo**（CVPR 2024，COIN+ChangeIt 20 万三元组）、**ShowHowTo**（CVPR 2025，100 万视频→0.6M 序列）、**Show Me**（SSv2 112k + EK100 57.6k 两阶段 LoRA） |
| (c) 结构性质 | ✅ **我们自己诊断过**：模型把体态解成**初始帧的状态选择**——`has grated` 第 1 帧就是一堆丝，`is grating` 第 1 帧是整根。**它画得出末态，只是从不在时间上生成末态** |
| (d) 索引里没有 | ✅ `state change` / `object state` **命中 0** |

**而且训练自由的邻居都不在做这件事**：`Prompt2Progression`（ICCV 2025）做的是
**属性渐变**（年龄/胡子/天气），`CausalMotion` 做**物理合理性**，都不是动作导致的状态变化。

### ② 语言语义类的控制（否定 / 时体 / 量化）—— 缺 (c)

`negation` / `tense` / `aspect` 全部命中 0，任务也清楚（NegBench 等基准存在）。
但**"可利用的结构性质"我现在给不出**——不知道否定在预训练模型里对应什么可操纵的结构。
**缺这一条，就只是"还没人做"，不是"可以做"。**

### ③ 材质迁移 / 3D 编辑 / 4D 编辑 —— 缺 (c) 和基建

条目数 1 / 2 / 2，确实薄。但薄可能是因为难或因为需求小，而且 3D/4D 要另建基建。

---

## 4. ① 为什么值得单独看：它和我们已有的东西是重合的

| 需要什么 | 我们有没有 |
|---|---|
| 现成的基准和评测协议 | ✅ OSCBench 全套在仓库里（1119 prompt + `mllm_eval.py` 八维度评分 + 已发表的六模型基线） |
| 跑得动的基座 | ✅ Wan2.2-TI2V-5B 已在卡上，实测 208 s/条、峰值 11.66 GB |
| 已生成的视频 | ✅ 111 条 + 35 item 的筛查 |
| **诊断出的结构性质** | ✅ 见 (c)，而且有可视证据 |
| 训练数据 | **不需要**——这正是 training-free 的意思 |

前面三个方向分别死于**测量、动机、新颖性**。这一个：
- 动机从 OSCBench 借（ACL 2026 主会已发表的失败）
- 新颖性由"三个已有解法全要训练、而 training-free 那一侧是空的"支撑
- 算力可行（推理期方法，不训练）
- 测量用他们自己的 `mllm_eval.py`，和已发表数字同坐标系

---

## 5. 必须先做的三件事（**不在核实前推荐**）

1. **索引不是穷尽的。** 195 条是一个人维护的清单，`state change` 命中 0
   只说明**这个清单里没有**。要按"training-free + object state change / transformation
   + video"正经搜一轮，把 (d) 从"清单里没有"推到"已核实为空"。
2. **(c) 那个结构性质要从 n=1 变成一个数。** 目前只有一个 item、一个种子、
   一个 5B 小模型的视觉证据。至少要在 35 个已生成 item 上量化：
   **末态在第 1 帧就存在的比例是多少。**
3. **要能说清楚"利用它"具体是什么操作。** 现在只有"模型把体态解成初态选择"这个诊断，
   还没有对应的推理期干预。**没有这一条，就还不是一个方法。**
