# 方法文章这条路：占用核查与三个选项

调研日期：2026-08-06。触发：用户提出改做**方法文章**——基于 OSCBench 诊断出的问题做方法创新。

> **结论：动机问题确实被解决了（从 OSCBench 借），但我最先想到的那个方法杠杆已被占，
> 而且这条路把我们放到视觉社区的主场上、用一张卡去拼。**
> 推荐第三个选项：**分析文章**，理由见 §4。

---

## 1. OSCBench 自己诊断出的失败模式（读全文，非摘要）

| 发现 | 数字 |
|---|---|
| 语义贴合高、状态变化低 | subject 0.90–0.99 / object 0.77–0.92，但 **OSC accuracy 只有 0.47–0.79** |
| **novel 场景退化最狠** | 不常见但合理的动作—物体配对；compositional 好于 novel、差于 regular |
| 动作类别分化 | rolling / heating 好；**peeling / coating / pressing 差**——精细手物交互 + 渐变外观 |
| 真实感与 OSC 绑定 | OSC 失败常伴随残留视觉伪影 |

第一条是核心：**模型能把"有什么"落地，落不了"动作导致了什么"。**

规模：**1119 条 prompt**（863 regular / 159 novel / 95 compositional），6 个模型，
人工 + MLLM 双评测。

---

## 2. 我最先想到的方法：已被占，≥3 篇

设想是——把状态变化 prompt 拆成初态/末态描述，沿去噪轨迹调度文本条件，
强迫视频从一端走到另一端。文本侧、training-free、对准我们自己试跑发现的
"模型改初始状态而不是改过程"。

**这条路是满的：**

| 工作 | 覆盖了什么 |
|---|---|
| **From Prompt to Progression**（arXiv 2509.19690） | **给定描述初态和末态的两个 prompt，training-free 生成属性转变视频**。就是这个方案 |
| **CausalMotion**（arXiv 2606.14317） | 用 VLM 把 prompt 拆成因果一致的中间状态序列，生成关键帧引导去噪，training-free |
| **Show Me**（arXiv 2511.17839） | 把视频扩散模型改造成"动作—物体状态转换器" |
| **TVG**（arXiv 2408.13413） | training-free 转场视频生成 |
| **GenHowTo**（CVPR 2024） | 从教学视频学动作与状态转换三元组 |

顺带确认了两件事：**"改进视频生成的状态变化"是被承认重要的**（动机侧好消息），
**且是视觉社区在积极做的**（新颖性侧坏消息）。

另有一批 training-free 的 T2V 控制方法占着邻近地皮：Video-MSG、TempoControl、
Φ-Noise、AlcheMinT。方向不同（布局 / 相机 / 噪声相位），但说明这一片人很多。

---

## 3. 方法路线的真实代价（算过账）

我们实测：480p 121 帧 **208 s/条**，720p **536 s/条**。

| | 一个条件跑完全套 1119 条 |
|---|---|
| 480p | **2.7 天** |
| 720p（他们 Table 6 的设置） | **6.9 天** |

方法文章最少 6 个条件（baseline / 本方法 / 两个消融 / 一个竞争方法 / 第二个基座）
= **16 天**，而这只是最后一次跑。

**关键差别：基准文章生成一次，方法文章要生成几十次。** 方法不会一次就对。

还有三条：

1. **venue fit**：视频扩散的方法文章天然属于 CVPR/NeurIPS，除非介入点在文本侧。
2. **风险画像反转**：基准找不到效应还能当负结果；**方法打不过 baseline 就是零**。
3. **差距量级**：OSCBench 里闭源 Veo-3.1-Fast 是 0.786，开源 Wan-2.2 只有 0.560。
   training-free 方法把 0.560 抬到 0.62 是正常幅度，但审稿人会指着没合上的那段差距问。

---

## 4. 三个选项

### A. 方法文章，攻 novel 泛化

OSCBench 里退化最狠的一档。"peel potato"会、"peel 某个没见过的物体"不会——
这是**文本条件端的组合泛化失败**，是三条发现里最像 NLP 的一条。
设想的介入：在嵌入空间里把熟悉配对的状态变化方向迁移到新配对上。

- 占用状态：**未核实**。一轮检索没打到直接对应物（VideoTetris、Flow Equalization
  做的是多物体组合与布局，不是状态变化的泛化），但**一轮不算封口**。
- 代价：§3 全部适用。

### B. 方法文章，攻动作类别（peeling / coating / pressing）

- 这些失败**根源在视觉端**（精细手物交互、渐变外观），文本侧使不上力。
- **判断：不做。** 它会把我们直接推进 CVPR 的赛道，而且我们没有可用的杠杆。

### C. 分析文章：状态变化的信息在哪一环丢的 ← **推荐**

不新做基准、不比 baseline，做**定位**：文本编码器 → 交叉注意力 → 去噪器 → 时序模块，
状态变化语义在哪一环消失。

理由是它**同时满足四条约束**，而 A 只满足两条：

| | A（方法） | **C（分析）** |
|---|---|---|
| 动机 | 从 OSCBench 借 ✅ | 从 OSCBench 借 ✅ |
| 不用人工标注 | ✅ | ✅ |
| 单卡可行 | ❌ 16 天 × 多轮迭代 | ✅ 受控探针跑子集即可 |
| 风险画像 | ❌ **打不过就是零** | ✅ 定位到了就能写，不必赢过谁 |
| venue | ⚠️ 要死守文本侧 | ✅ **ACL 2026 的 theme 就是 Explainability** |

另外两条只有 C 吃得到：

- **我们已经有一个没人发表过的观察**：试跑里 `perf`/`result`/`phase_finish` 条件下
  **模型从第一帧就把末态画出来**——它把句子当静态场景描述读。
  这是一条可以拿去做因果定位的具体线索。
- **标定方法直接复用**：分析文章的命门是"你的测量可信吗"，
  而这正是 `calibrate/` 那套东西做的事。

**C 的风险要说清楚**：分析文章必须**真的找到东西**。
"我们探了一圈，文本编码器没问题"是弱论文。而且它同样**没做占用核查**。

---

## 5. 下一步

**不要重复前两次的错误——先核查再推荐。**

C 若要走，先做半天的占用封口：
- 有没有人做过 T2V 的机制可解释性 / 信息定位（对照 T2I 那边已有的
  "Mechanistic Interpretability of Text-to-Image Diffusion Models"，ACL 2026 Findings）
- 有没有人分析过 T2V 里动作语义 / 状态变化在哪一层丢失
- 交叉注意力分析在视频扩散上做到什么程度了

**在那之前，C 只是"看起来合适"，不是"已核实可做"。**
