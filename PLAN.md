# 我们在做什么（每次迷路先看这里）

日期 2026-08-07。

## 一句话

**写一篇和 ScaleDiff 同一种的方法论文：training-free 高分辨率外推，
修 ScaleDiff 自己在 Limitations 里承认、但没修的那条失效。**

我们和 ScaleDiff 的关系 = **StyleSSP 和 StyleID 的关系**。
不是重做它做过的事，是开在它的残余上。这就是谱系的走法。

| | ScaleDiff (NeurIPS 2025) | 我们 |
|---|---|---|
| 任务 | training-free 高分辨率外推 | 同 |
| 基座 | SDXL（+FLUX） | 同 |
| Intro 的失效 | U-Net 偏见 / patch 重叠贵 / RGB 上采样糊 | **4096² 幻觉出 prompt 主体** |
| Method | NPA + LFM + SG | **?（第 6 步）** |
| 评测 | FID/KID/IS + FIDp/KIDp/ISp + CLIP，1000 条 LAION | **同一套** + 幻影计数 |
| Limitations | *"repetitive artifacts may still occur in background regions"* | ← **我们的入口** |

**不做用户研究**（没有标注员），和 ScaleDiff / LSRNA / HiFlow 一样。

---

## 进度

```
[x] 0  选在位者     ScaleDiff。4096² 74.7s / 峰值 11.9GB（比论文的 A6000 还快）
[x] 1  找到失效     4096² 幻觉出 prompt 主体。原分辨率确认 3 个人，基图 0 个
[x] 2  造尺子       GroundingDINO 计数器。阳性对照通过，参数冻结
                    << 这是【工具】不是贡献。一度把它当贡献 —— 那次是真跑偏了
[x] 3  诊断病因
[x] 4  普遍性验证   30 条 prompt 基线跑完，全部 delta +7.03，18/30 有新增
[~] 6  设计方法     候选 = v1：同一次前向喂两套文本嵌入，按主体图逐位置混合
                    seed 77 单图：幻影 5->0，主角框几乎不动，+4% 时间
                    30 条批跑：跑完了，但**基图被污染，结果作废，要重跑** <<< 这里
[!] 5  占位核查     **AccDiffusion v2 有没有物体重复的定量指标？**
                    有 -> 尺子这一条塌一半，重跑批次会白费。排在重跑之前
[ ] 7  评测         ScaleDiff 的同一套指标
```

## 叙事（2026-08-08 改）

**不要把"修法"当贡献。** "把 AccDiffusion 的修法搬到 ScaleDiff 上"是
incremental port，一句话就能被打掉，而且打得对。借机制是这条线的常态
（StyleSSP 借 FlexiEdit、HiWave 借 Sadat、ScaleDiff 借 LSRNA），
但借了机制就必须有别的东西撑着。我们手上只有两样别人没有的：

1. **这条线上没有能看见这个失效的指标。** FID/KID/IS 把 4096² 压到 299²，
   物理上看不见多出来的一个人；FIDp/KIDp 是分布距离，不数物体。
   失效被普遍观察到（ScaleDiff 自己写在 Limitations 里）却从未被测量过。
2. **一条定量的律。** AccDiffusion v2 只给了定性机制（"同一句 prompt 施于
   每个局部视野"）。我们量到的是：幻影数由**邻域/画布比**支配，不由分辨率
   支配（1/4 -> 0 个，1/16 -> 5 个；邻域 x2 -> 5->2~3，代价 +37%/+41%）。
   而这个比值是整个家族共有的轴 —— DemoFusion 的 patch、AccDiffusion 的
   patch、Pixelsmith 的 slider、ScaleDiff 的注意力窗，都是同一个轴上的点。

> **高分辨率外推里，效率与全局一致性是同一个权衡的两端。
> 这是汇率，这是尺子，这是少付一点的方法。**

修法降格成"这条律可操作"的证明。代价：测量要做到整个家族上（三个都开源，
一张 4090 跑得下），而且尺子要被验证得比现在硬得多。

## 我们现在在跑的这个实验是什么

**v1 是候选方法。这一批 30 条是它的普遍性检验。**

单图（seed 77）证明了机制存在；一张图不是方法。批跑要同时回答两个问题，
第二个比第一个重要：

1. 幻影是不是普遍减少了？（`lone` 类 delta 至少减半，+6.20 -> ≤3）
2. **它把什么弄坏了？** —— 这一问决定方法的最终形态。

比较的对象是同 seed、同 RNG 的基线批（`$SD_OUT/batch`），逐条配对。

---

## 第 3 步的结论（诊断）

> **在 R× 外推时，NPA 的 KV 邻域固定为"一张基分辨率画布"，占整图比例降到 1/R²；
> 而 cross-attention 把整句 prompt 以全局强度送给每一个位置。
> 两者的组合使每个邻域独立地实例化了 prompt 主体。**

三条互相独立的证据（全部 seed 77，全局 RNG 钉死）：

| 实验 | 结果 | 说明 |
|---|---|---|
| 放大阶段去掉 prompt 主体词 | 幻影 **5 → 0**，次阈值检测 **8 → 1** | 文本条件是**必要**条件 |
| KV 邻域 ×2（query 分块不变） | 幻影 **5 → 2~3**，次阈值 **8 → 0** | 局部性是**另一个必要**条件 |
| 2048²（邻域占 1/4）vs 4096²（1/16） | **0 → 5** | **比例**是关键变量 |

### 由此得到的方法【约束】（不是方法）

两个条件都必要 ⇒ 打断任一即可修，但两条现成的路都有明确代价：

| 打断哪个 | 代价 |
|---|---|
| 文本条件（删主体词） | 真实场景用不了 |
| 局部性（放大邻域） | 显存 **+41%**、时间 **+37%** —— 吃掉 ScaleDiff 的核心卖点（快） |

> **可用的方法必须：保留局部性（不加计算），但让文本条件不再在每个邻域
> 都以全局强度作用。**

---

## 纪律（写下来是因为都违反过）

1. **看到图之前不提方法假设。** 这次会话猜过七次方法，七次全被占位打回 ——
   每一次都是跳过诊断这一环的后果。
2. **检测器参数已冻结**（在 seed 77 的阳性对照上定死）。之后再动，就是在结果上调参数。
   例外只有一种：从**机制**上发现的结构性缺陷，且修法在动手前就写死。
   已用掉一次 —— 表观尺度不对称，见下。
3. **不再造新工具**，除非第 4–7 步真的需要。
4. **没有阳性对照的阴性结果不能用。**
5. **占位核查从领域的公共节点扫**（DemoFusion，116 引用），不从候选在位者扫 ——
   在位者在这次会话里换了三次（Pixelsmith → LSRNA → ScaleDiff），
   就是因为只扫了候选自己的引用图。

---

## 已知的坑（重跑前必读）

- **计数器的表观尺度不对称 —— `delta` 被系统性做高。**
  `detect()` 的分块那一遍有 `if W > tile or H > tile`（tile=1024），
  所以 **1024² 的基图根本不分块**，只走整图一遍缩到 ~800px；
  而 4096² 走整图一遍 + 49 块原分辨率 tile。同一个相对大小 f 的物体，
  4096² 上检测器看到 `f x 3200` px，基图上只有 `f x 800` px —— **4 倍差**。
  于是 "many cars" 基图 0 辆、"a colony of penguins" 基图 0 只、
  "a single eagle" 基图 0 只，都不是模型没画，是那一遍看不见。
  修法（机制驱动，动手前定死）：`tile = width/4, stride = width/8`。
  4096² 下正好等于现行的 1024/512，**4096 的计数一个字不变**，只修基图和 2048。
  修完两档的表观尺寸都是 `f x 3200`。改完必须重跑阳性对照。
  其余漏检来源要做敏感度分析，不能回避：`min_score=0.50`（v1 那个 0.54 的
  幻影贴着地板）、`max_area_frac=0.25`、`max_aspect=2.0`。其中 min_score 是
  全套参数里唯一没有机制依据、纯靠 seed 77 标定的。
- **别为了看内部量而改前向的数值路径。** 录主体注意力图需要显式 softmax，
  v1 顺手把 phase 1 的输出也从 SDPA 换成了它 —— 数学等价、fp16 不等价。
  扩散第一步的扰动一路放大，30 条里 25 条的**基图**都变了，整批 A/B 作废。
  规矩：**要观测的量单独算，前向一个字节都不许动。**
  核查手段：`compare_counts.py` 第一段，基图 1024² 逐字节哈希。
- **ScaleDiff 的高分辨率输出不能从 seed 复现**：放大阶段的
  `torch.randn_like(latents_LFM)`（pipeline:547）没传 generator，走全局 RNG。
  基图可复现，4096² 不可。**任何 A/B 对照都要先 `torch.manual_seed`。**
  `run_one.py` / `batch_run.py` 还没加这个 —— 它们之前跑的图不可复现。
- `scalediff_probe/` 下不能有和标准库同名的文件（`inspect.py` 踩过）。
- `transformers` 必须 `<5`（diffusers 0.35.1 要 `FLAX_WEIGHTS_NAME`）。
- SDXL 缓存只有 fp16 变体，而 ScaleDiff 没传 `variant="fp16"` —— 我们的脚本自动补。
