# 审稿质疑清单：状态与对策

每条记四样：**质疑是什么、现在什么状态、怎么解、代价多少**。
"已关闭"指有实测数据或已实现的机制支撑，不是"我觉得没问题"。

论文的 limitation 一节直接从这份取材。

---

## 一、"你用的模型太弱，换个大模型结论就变了"

**状态：已大幅缓解，可再加强（花钱不占卡）**

| 手段 | 代价 | 强度 |
|---|---|---|
| **双锚 → 选择性缺陷**（已实现） | 已付 7% 算力 | ★★★★★ |
| 引 OSCBench 自己的数据 | 0 | ★★★ |
| 共享种子只有开源权重能做 | 0 | ★★★ |
| 闭源 API 参照子集 | 约 100–200 美元，**不占 GPU** | ★★★★ |

核心论证：结论的形式不是"模型做不到"，而是"**模型对词汇内容有反应
（`other_verb` 连工具都换了），对无关改写不敏感，却唯独对体态形态盲**"。
**选择性缺陷不会被"模型太小"解释掉，笼统的失败会。**

第二条支撑：OSCBench Table 4 里 Veo-3.1-Fast 只有 0.744 / 0.797 / 0.699，
**前沿闭源模型在物体状态变化上也远没到天花板**——不是靠 scale 解决的问题。

第三条：Veo / Kling 不暴露种子。**审稿人要求的那些模型做不了本文的核心设计。**
开源权重是前提条件，不是退而求其次。

## 二、"480p 不是官方设置"

**状态：已关闭**

- 37 个 item 的筛查确认：擦丝器的孔、碎屑、手指位置在 480p 下全部可辨，
  **标注问的那个问题答得了**
- 480p 是这些模型官方支持的运行模式
- OSCBench 自己 Table 6 就不齐（Open-Sora 是 768×768）
- 分辨率对 item 内所有条件相同，在组内对比中抵消

实测支撑见 `generate/BUDGET.md`。A14B 跑不动这件事论文里直说，
不含糊成"我们选择了 TI2V-5B"。

## 三、"一个介词就改变画面，那你的体态效应可能只是扰动"

**状态：从威胁变成被测量的量**

这是试跑发现的，不是审稿人才会提的。处理方式：

1. **不再把条件差异直接当体态效应**，所有效应除以 paraphrase 基线
   （与文本侧的 SNS 统计量同构）
2. 这个基线**本身是可报告的结果**：T2V 模型在保义改写下的不稳定性，
   共享种子、控制长度、对着一条语义轴量化
3. `filler`（长度配平）已加入，用来分离"长度效应"与"语义效应"——
   试跑中 `failed` 的构图崩坏据此判定为**长度效应**，不是否定语义

**代价已付**：16 → 17 个条件，两模型 +0.5 天。

## 四、"63% 的视频里动作根本没发生"

**状态：有解，待执行（9 小时）**

改成**两级结果**而不是当作数据缺陷：

1. **第一级**：动作执行率——模型到底会不会渲染这个事件。本身就是结果
2. **第二级**：在渲染成功的 item 里，culmination 有没有被编码

筛选只用 `prog`（基线条件），对体态对比外生，所以条件化是可辩护的。

两条已定位的原因：**`at a market stall` 9/9 全败**（市场是陈列食材的地方），
**成功的动词全都有专属器具**（打蛋器、平底锅、榨汁器、刨丝器、烤盘、擀面杖）。

**待执行**：全套 156 个 item 各跑一条 `prog`，9 小时，按执行率重选生成子集。
去掉市场摊位后剩 116 个，按实测 46% 算能挑出 53 个，比现在的 37 个还多——
详细的敏感性分析见下面"那条存亡判据是我算错了"。

## 五、"整篇没有人工评测"

**状态：机制已实现，一个缺口待补**

| 能建立 | 怎么建立 | 需要标注 |
|---|---|---|
| 判官**信度** | 同一视频判两遍 | 否 |
| 判官**特异度** | 拿视频问它不可能达成的末态 | 否 |
| **模型不稳定性** | 语义等价条件间不一致 − 判官噪声 | 否 |
| 判官**敏感度** | — | **是** |

敏感度拿不到，因为**没有已知正例**：设计里没有任何东西能认证
"这段视频确实达成了末态"。

**缺口的补法**（三选一或叠加）：

| 手段 | 代价 |
|---|---|
| 作者核验约 150 段随机样本，以 "we manually verified a random subsample" 报告 | **约 1 小时** |
| 两个不同 MLLM 判官交叉一致 | API 费用 |
| 外部已标注真实视频（ChangeIt / Ego4D）验证判官 | 待核实可行性 |

第一条最划算，而且**它不违背"不做人工标注"**——那说的是不建标注流程，
不是作者不能核验自己的数据。

**推论必须写进论文**：可辩护的主张是**条件之间的差异**，
不是**绝对达成率**——后者未经校准。

## 六、"自洽不等于准确"

**状态：部分关闭**

一个永远回答 `final: yes` 的判官在所有一致性检验上拿满分。挡法：

- `MUST_DIFFER`：`other_verb` 的 `final` 必须不同
- **已知负例**（`--known-negative`）：拿每段视频问**另一个动词**的末态，
  答案先验为 `no`。`stimuli.jsonl` 里本来就有 `other_verb_base`，
  所以**磁盘上每段视频都已经是一个已知负例**，不用多生成任何东西

这把特异度从单个格子扩到几十个。**剩下的敏感度缺口见第五条。**

## 七、"MLLM 判官不如专家人工判断"

**状态：可用 OSCBench 自己的数据正面回答**

他们 Table 3 报了人机相关，**并且报了人人之间的相关作为参照**——
这一行是回答这条质疑的关键：

| 维度 | GPT-5.2 vs 人 (ρ/τ) | **人 vs 人** (ρ/τ) |
|---|---|---|
| Scene Alignment | 0.505 / 0.485 | **0.517 / 0.492** |
| Realism | 0.318 / 0.276 | **0.711 / 0.613** |

两条读得出来的事实：

**1. "专家人工判断"不是金标准，它本身就是有噪声的测量。**
人与人之间在 Scene Alignment 上只有 τ = 0.49。质疑里那个隐含前提——
人工判断是不可动摇的基准——**被他们自己的数据否掉了**。

**2. 差距的大小取决于问题有多客观。** 在偏客观的 Scene Alignment 上，
GPT-5.2（0.485）**与人人一致度（0.492）持平**；在高度主观的 Realism 上，
人（0.613）远高于模型（0.276）。

**我们的三个问题在客观性上比 Scene Alignment 更靠前**：二元、
有成文的目标末态定义、单帧可判。**MLLM 与人的差距在这一端最小。**

### 结构性论证：盲判官比"看得见 prompt 的专家"偏差更小

这一条比上面的数字更重要。本设计的比较是 **item 内、判官对条件盲**：

- 判官看不到句子，只看到抽帧 + 动词原形 + 宾语 + 末态定义
- 同一 item 的 17 个条件被问的是**逐字相同的问题**
- 判官若有系统性偏差（比如"看到刀就倾向判达成"），
  **该偏差对所有条件相同，在条件差中抵消**

要紧的不是判官**准**，是判官**对条件无偏**——而盲判是从构造上保证这一点的。

**反过来，OSCBench 的人工评测者是能看到 prompt 的。**
在我们这种条件只差一个词的设计里，看得见 prompt 的专家会带着预期去看画面，
**偏差比盲判官更大**。这不是我们退而求其次，是这个设计**只能**盲判。

### 论文里怎么落地

**一个不能做的**：把我们的信度和他们的人人一致度**放进同一张表**。
四个维度都对不上——统计量（重测一致 vs Kendall τ）、任务
（3 个二元问题 vs 8 个 Likert 维度）、数据（我们的视频 vs 他们的视频）、
评分者构成（同一判官两遍 vs 不同的人之间）。同表并列意味着"可以直接比大小"，
而它们不能。硬放进去反而成为把柄。

**合法的做法是在正文里引用**，作为背景而非可比数：

> Inter-annotator agreement on comparable video-evaluation tasks is itself
> moderate — OSCBench reports mean inter-human τ = 0.49 on scene alignment —
> so human judgment is a noisy reference rather than a gold standard.

"人工判断本身有噪声"这个论证仍然成立，**只是不能用表格的形式说**。
我们自己的信度与特异度单独成表，只报自己的数。

**统计量必须是 Cohen's κ，不能只报原始一致率。**
这几个问题的答案分布是偏的（多数视频没到末态），两个都答 `no` 九成的评分者
**碰巧就能有 82% 的一致率**。`validate.py` 现在两个都报——
合成数据上出现过 `initial` 原始一致率 87.6% 而 κ = 0.005 的情形，
只看前者会把一个毫无信息的判官说成很可靠。

其余三条：

1. 两个不同 MLLM 交叉一致率
2. 作者核验 150 段随机样本（约 1 小时）
3. limitation 里写明：敏感度未校准，主张限于条件间差异

---

## 换方向吗

**不换。** 理由不是沉没成本，是这三条：

1. **上面六条没有一条是关于选题的**，全部是关于"如何评测"的。
   任何 T2V 评测方向都会撞上同一份清单，换选题不解决其中任何一条。
2. **每条都已经有实测数据或已实现的机制**，最贵的一条是 9 小时算力，
   其余按小时和美元计。
3. **真正天生零标注的方向只有纯文本侧**（探针、最小对立对），
   而我们这个设计**已经继承了那个性质**——控制条件的正确答案来自语义推导。
   换回纯文本等于放弃视频这一层，回到第五轮就判定"撑不起主会"的位置。

## 那条"存亡判据"是我算错了

我先前把全套筛查说成存亡判据。把数算出来之后，它不是。

全套 156 个 item 里，`at a market stall` 占 40 个，去掉后剩 **116**：

| 假设执行率 | 可用 item |
|---|---|
| **46%（实测厨房场景值）** | **53** |
| 37%（含市场摊位的总体率） | 42 |
| 25%（悲观） | 29 |
| 15%（极悲观） | 17 |

**要掉到比现在的 37 个还少，执行率得低于 32%；实测是 46%。**
要掉到撑不起 benchmark 的 20 个以下，得低于 17%——比观测到的任何数字都低一大截。

### 而且低执行率本身就是结果

若真只有 25%，那句话是：**T2V 模型在四分之三的情况下渲染的是"某个事件的场景"，
而不是那个事件。** 这是一个 156 个 item、有效度可测的判官支撑的实证发现，
比"体态编码不完整"更基础、更容易被引用。

| 执行率 | 论文形态 |
|---|---|
| >40% | 按原计划，体态编码是主结果 |
| 20–40% | 体态分析照做，执行率并列为主结果 |
| <20% | 头条变成"模型不渲染事件"，体态分析退为次要节 |

**没有一种结局是"白跑"。** 变的是哪个结果当头条，不是有没有结果。

### 与执行率无关、无论如何都保留的四项

1. **prompt suite**——四层过滤 + 回归测试 + 三轮分诊，本身是可发布资源
2. **共享种子的可识别性设计**——`max |Δpixel| = 0.000000`，方法贡献
3. **零标注的判官验证**（信度 + 已知负例特异度）——方法贡献
4. **保义改写下的不稳定性**——已经是实测结果

所以全套筛查的 9 小时买的不是"活不活"，是**哪个结果放前面、生成子集选哪些 item**。

## 执行顺序

1. 第二批 74 个视频跑完 → 抽帧 → 判官 `--repeat 2` + `--known-negative` → `validate.py`
2. `SCENES` 去掉 `at a market stall`，决定 `crust` / `pastry` 是否屏蔽
3. **全套 156 个 item 筛查（9 小时）** ← 决定哪个结果当头条、子集选谁
4. 定生成子集 → 跑全部 17 个条件（约 9 天）
5. 作者核验 150 段（1 小时）
6. 闭源 API 参照子集（可选，一两百美元）
