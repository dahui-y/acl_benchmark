# 为什么要做这个 benchmark —— 完整论证

方向：**语言能否控制「事件是否达成」**（以非达成 / 失败为核心）
配套文档：`idea_aspect.md`（设计细节与第六轮修正）、`README_ideas.md`（六轮取舍索引）

---

## 0. 一句话

> OSCBench 发现模型**该完成时完不成**。真正的问题在反面：
> **当语言明确要求它不要完成时，它能不能不完成？**
> 若两边都做不到，说明"达成"根本不是一个被条件化的变量——
> 模型只是在渲染动词的原型事件。**这个双向证据比任何单向发现都强。**

## 1. 现象：模型生成的是「动词的样子」，不是「动作的结果」

- `VBench-2.0` 记录了一类失败并归入常识范畴：**啃食物但食物没变少、走路但没有前进**（fake motion）
- `OSCBench` 系统量化了同一件事：语义对齐很高（subject 0.86–0.94、scene 0.74–0.89），
  但状态变化准确性仅 **0.38–0.79**

两者都在说：模型学到的是"动作看起来像什么"，不是"动作把物体变成什么"。

**但两者都只在成功那一侧取样。** 所有 prompt 默认动作应当完成。于是留下一个从未被问过的问题：

> 这种"完不成"，是**能力不足**（想完成而做不到），
> 还是**根本没把完成度当作变量**（动词一出现，就渲染该动词的典型完整事件）？

**区分这两种解释的实验方法，就是反过来要求它别完成。**

## 2. 语言层：达成与否是被语法系统编码的

不是措辞偏好，是语法：

| 语言形式 | 对达成的编码 |
|---|---|
| `is slicing` / `sliced` | 未完成体 / 完成体 |
| `tried to slice but failed` | **显式非达成** |
| `is about to slice` | 达成尚未开始 |
| `started slicing but stopped` | 过程中断（相位动词） |
| `slicing apples`（光杆复数） | 无确定终结点（atelic） |

**理论挂靠：未完成体悖论（Dowty）** —— `was crossing the street` **不蕴含** `crossed the street`。
过程发生、终点未达成，在语言中是一个有专门形式手段表达的、完全正常的语义配置。

**而这恰好就是"失败"的定义。**

## 3. 缺口核验

| 已有工作 | 做了什么 | 是否操纵语言中的达成标记 |
|---|---|---|
| OSCBench (ACL'26) | 状态变化准确性与一致性，1,120 prompt | **否** —— 全部现在进行时 |
| T2V-CompBench / GenAI-Bench / UniGenBench++ | 属性、关系、计数、逻辑组合性 | 否 |
| TC-Bench (ACL Findings'25) | 初态→末态的转变完成度 | 否 —— prompt 明示初末态以消歧 |
| VBench-2.0 | 18 子维度，含 fake motion 观察 | 否 —— 归为常识失败 |
| RoboTrustBench | 六维 13 项，含 **action completeness** | **否** —— 测 rollout 是否完成任务 |
| Perfect Times / ViLMA | 体貌与终结性 | **是，但在理解侧**，明确不涉及生成 |

> **"达成度"被测过；"语言能否控制达成度"没有。**
> 这是本工作唯一的新颖性主张，**必须在 related work 中写死**。

## 4. 谁受益（价值论证）

> 这一节是筛选的第一道关。第五轮曾推荐过一个"空但无价值"的方向（预设投射），
> 正是栽在答不出"谁受益"。本方向必须能答。

### 4.1 具身智能的失败数据缺口（最硬）

立场论文 *Good Embodied Reward Models Need Bad Behavior Data*（arXiv 2606.01036）
**明确呼吁**投入合成失败与不安全行为的方法。失败数据对 reward model 与 recovery policy
是刚需，但真实采集昂贵且危险。

现有合成方案全部依赖**动作空间与物理验证器**：
- `Dream2Fix`：从成功演示做关键帧级动作扰动 + 物理/视觉验证器筛选
- 反事实失败合成（arXiv 2603.13528）
- 从成功演示自动导出规划与执行失败（arXiv 2512.01946）

即：**必须先有仿真器与轨迹表示，才能造失败。**

- **若语言路径可行** → 成本塌一个量级，且可推广到**没有动作空间表示的领域**（烹饪、装配、护理）
- **若不可行** → 明确告知该社区：不要指望用 prompt 造失败数据，去做条件化

**两个结果都有用** —— 这是本方向"稳"的根源之一。

### 4.2 可控生成与视频编辑

"给我一个**已经切好**的苹果，不要切的过程"是内容生产的常规需求。
现在做不到，且没人量化过做不到的程度。

### 4.3 世界模型的表征证据

真正的世界模型必须能表示"事件未达成"这一状态。
若模型只能生成成功，说明它把**动作与结果绑死**了——
学到的是 `slicing-an-apple` 这个整体视觉模式，
而非"切"这个操作作用于"苹果"并产生**可变**结果。

这是关于**模型学到了什么表征**的直接证据，不只是一个性能数字。

## 5. 为什么投 ACL 而非 CVPR

核心自变量是**语法范畴**（体貌、终结性、构式），
核心问题是**语言的组合语义能否被生成模型使用**。视觉只是读出手段。

接收先例（同脉络）：OSCBench（ACL 2026 main）、TC-Bench（ACL Findings 2025）、
Implicit-VidSRL（ACL 2025）。

> 前提：主张必须锚在语言现象上，不能锚在画质上。**intro 第二段就要定死。**

## 6. 贡献形态（对标 OSCBench，多一层）

| | OSCBench | 本工作 |
|---|---|---|
| 资源 | 1,120 prompt + 动作/物体分类体系 | 达成度语言矩阵 + prompt 集 + 标注指南 |
| 评测 | 8 维 Likert × 6 模型 | **2 个二元问题** × 4 模型（认知负荷低一个量级） |
| 自动评测 | MLLM CoT + 相关性分析 | 同构 |
| **机制** | 无 | **文本编码器探针 —— 信息是否存在、在哪一层丢失** |
| **干预** | 无 | **免训练差向量注入 —— 能否部分修复** |

多出的机制层与干预层，是把论文从
"我们发现 X 很难"（**描述性**）抬到
"我们定位了 X 在哪失效并部分修复"（**解释性**）的关键。
ACL 对后者的评分系统性更高。

## 7. 预期发现

模型将动词映射到"典型完整事件"的视觉模式，语言中的达成标记不参与条件化
—— `tried to slice but failed` 会生成一次**成功**的切。

| 探针结果 | 叙事走向 |
|---|---|
| 体貌信息**存在但被生成阶段丢弃** | 可定位的机制结论；免训练干预有希望 |
| 编码器本身就不区分 | 转向架构分析（"文本编码器丢失达成信息"） |

**两种结果都能写。**

## 8. 诚实的风险

| 风险 | 处置 |
|---|---|
| **新颖性边界窄** —— RoboTrustBench 已测完成度 | 区分必须写得极清楚：它测"rollout 是否完成任务"，本工作测"语言能否指定不要完成" |
| **`has sliced` 类格子的 gold 可争议** —— 完成体只要求参照时刻结果成立，视频先过程后结果并不违规 | **主证据放在 `tried but failed` 这类目标无歧义的格子**；可争议的降为次要分析 |
| **商业模型的 prompt 改写层**会抹平标记 | 主实验放开源模型；闭源单列并标注 confound（详见 `idea_aspect.md` §7d P1） |
| **"模型忽略体貌"被判为不意外** | 解药是 §6 的机制层与干预层 |
| 5 秒时长无法区分进行与完成 | 判据锚"首帧 + 过程 + 末帧"三元组；主结论建立在不受时长影响的格子上 |

## 9. 下一步

**文本编码器探针**——纯文本、单卡数分钟、不需标注、两种结果都能写成一节。
它决定论文的叙事走向，是当前性价比最高的一步。

设计见 `idea_aspect.md` §7c。

## 参考

- OSCBench (ACL 2026): 本仓库 PDF
- VBench-2.0: https://arxiv.org/html/2503.21755v1
- TC-Bench (ACL Findings 2025): https://aclanthology.org/2025.findings-acl.241/
- RoboTrustBench: https://arxiv.org/abs/2606.01600
- Good Embodied Reward Models Need Bad Behavior Data: https://arxiv.org/pdf/2606.01036
- Counterfactual Failure Synthesis: https://arxiv.org/pdf/2603.13528
- Scaling Cross-Environment Failure Reasoning Data: https://arxiv.org/pdf/2512.01946
- Perfect Times: https://arxiv.org/abs/2506.00928
- ViLMA (ICLR 2024): https://arxiv.org/pdf/2311.07022
